%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% SZS status Unsatisfiable for GRP116-1.tptp

% SZS output start CNFRefutation for GRP116-1.tptp
cnf(single_axiom, axiom,
    (multiply(X,
       multiply(multiply(X, multiply(multiply(X, Y), Z)),
         multiply(identity, multiply(Z, Z)))) = Y)).

cnf(prove_order3, negated_conjecture, (multiply(identity, a) != a)).

cnf(refute_0_0, plain,
    (multiply(X,
       multiply(multiply(X,
           multiply(multiply(X,
               multiply(multiply(X, multiply(multiply(X, X_1), X_2)),
                 multiply(identity, multiply(X_2, X_2)))), Z)),
         multiply(identity, multiply(Z, Z)))) =
     multiply(multiply(X, multiply(multiply(X, X_1), X_2)),
       multiply(identity, multiply(X_2, X_2)))),
    inference(subst, [],
              [single_axiom :
               [bind(Y,
                     $fot(multiply(multiply(X,
                              multiply(multiply(X, X_1), X_2)),
                            multiply(identity, multiply(X_2, X_2)))))]])).

cnf(refute_0_1, plain,
    (multiply(X,
       multiply(multiply(X, multiply(multiply(X, X_1), X_2)),
         multiply(identity, multiply(X_2, X_2)))) = X_1),
    inference(subst, [],
              [single_axiom : [bind(Y, $fot(X_1)), bind(Z, $fot(X_2))]])).

cnf(refute_0_2, plain,
    (multiply(X,
       multiply(multiply(X, multiply(multiply(X, X_1), X_2)),
         multiply(identity, multiply(X_2, X_2)))) != X_1 |
     multiply(X,
       multiply(multiply(X,
           multiply(multiply(X,
               multiply(multiply(X, multiply(multiply(X, X_1), X_2)),
                 multiply(identity, multiply(X_2, X_2)))), Z)),
         multiply(identity, multiply(Z, Z)))) !=
     multiply(multiply(X, multiply(multiply(X, X_1), X_2)),
       multiply(identity, multiply(X_2, X_2))) |
     multiply(X,
       multiply(multiply(X, multiply(X_1, Z)),
         multiply(identity, multiply(Z, Z)))) =
     multiply(multiply(X, multiply(multiply(X, X_1), X_2)),
       multiply(identity, multiply(X_2, X_2)))),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(X,
                          multiply(multiply(X,
                              multiply(multiply(X,
                                  multiply(multiply(X,
                                      multiply(multiply(X, X_1), X_2)),
                                    multiply(identity,
                                      multiply(X_2, X_2)))), Z)),
                            multiply(identity, multiply(Z, Z)))),
                        multiply(multiply(X,
                            multiply(multiply(X, X_1), X_2)),
                          multiply(identity, multiply(X_2, X_2))))),
                 [0, 1, 0, 1, 0], $fot(X_1)]])).

cnf(refute_0_3, plain,
    (multiply(X,
       multiply(multiply(X,
           multiply(multiply(X,
               multiply(multiply(X, multiply(multiply(X, X_1), X_2)),
                 multiply(identity, multiply(X_2, X_2)))), Z)),
         multiply(identity, multiply(Z, Z)))) !=
     multiply(multiply(X, multiply(multiply(X, X_1), X_2)),
       multiply(identity, multiply(X_2, X_2))) |
     multiply(X,
       multiply(multiply(X, multiply(X_1, Z)),
         multiply(identity, multiply(Z, Z)))) =
     multiply(multiply(X, multiply(multiply(X, X_1), X_2)),
       multiply(identity, multiply(X_2, X_2)))),
    inference(resolve,
              [$cnf($equal(multiply(X,
                        multiply(multiply(X,
                            multiply(multiply(X, X_1), X_2)),
                          multiply(identity, multiply(X_2, X_2)))), X_1))],
              [refute_0_1, refute_0_2])).

cnf(refute_0_4, plain,
    (multiply(X,
       multiply(multiply(X, multiply(X_1, Z)),
         multiply(identity, multiply(Z, Z)))) =
     multiply(multiply(X, multiply(multiply(X, X_1), X_2)),
       multiply(identity, multiply(X_2, X_2)))),
    inference(resolve,
              [$cnf($equal(multiply(X,
                        multiply(multiply(X,
                            multiply(multiply(X,
                                multiply(multiply(X,
                                    multiply(multiply(X, X_1), X_2)),
                                  multiply(identity, multiply(X_2, X_2)))),
                              Z)), multiply(identity, multiply(Z, Z)))),
                      multiply(multiply(X,
                          multiply(multiply(X, X_1), X_2)),
                        multiply(identity, multiply(X_2, X_2)))))],
              [refute_0_0, refute_0_3])).

cnf(refute_0_5, plain, (X0 = X0),
    introduced(tautology, [refl, [$fot(X0)]])).

cnf(refute_0_6, plain, (X0 != X0 | X0 != Y0 | Y0 = X0),
    introduced(tautology,
               [equality, [$cnf($equal(X0, X0)), [0], $fot(Y0)]])).

cnf(refute_0_7, plain, (X0 != Y0 | Y0 = X0),
    inference(resolve, [$cnf($equal(X0, X0))], [refute_0_5, refute_0_6])).

cnf(refute_0_8, plain,
    (multiply(X,
       multiply(multiply(X, multiply(X_1, Z)),
         multiply(identity, multiply(Z, Z)))) !=
     multiply(multiply(X, multiply(multiply(X, X_1), X_2)),
       multiply(identity, multiply(X_2, X_2))) |
     multiply(multiply(X, multiply(multiply(X, X_1), X_2)),
       multiply(identity, multiply(X_2, X_2))) =
     multiply(X,
       multiply(multiply(X, multiply(X_1, Z)),
         multiply(identity, multiply(Z, Z))))),
    inference(subst, [],
              [refute_0_7 :
               [bind(X0,
                     $fot(multiply(X,
                            multiply(multiply(X, multiply(X_1, Z)),
                              multiply(identity, multiply(Z, Z)))))),
                bind(Y0,
                     $fot(multiply(multiply(X,
                              multiply(multiply(X, X_1), X_2)),
                            multiply(identity, multiply(X_2, X_2)))))]])).

cnf(refute_0_9, plain,
    (multiply(multiply(X, multiply(multiply(X, X_1), X_2)),
       multiply(identity, multiply(X_2, X_2))) =
     multiply(X,
       multiply(multiply(X, multiply(X_1, Z)),
         multiply(identity, multiply(Z, Z))))),
    inference(resolve,
              [$cnf($equal(multiply(X,
                        multiply(multiply(X, multiply(X_1, Z)),
                          multiply(identity, multiply(Z, Z)))),
                      multiply(multiply(X,
                          multiply(multiply(X, X_1), X_2)),
                        multiply(identity, multiply(X_2, X_2)))))],
              [refute_0_4, refute_0_8])).

cnf(refute_0_10, plain,
    (multiply(multiply(X, multiply(multiply(X, Y), Z)),
       multiply(identity, multiply(Z, Z))) =
     multiply(X,
       multiply(multiply(X, multiply(Y, Z)),
         multiply(identity, multiply(Z, Z))))),
    inference(subst, [],
              [refute_0_9 : [bind(X_1, $fot(Y)), bind(X_2, $fot(Z))]])).

cnf(refute_0_11, plain,
    (multiply(X,
       multiply(multiply(X, multiply(multiply(X, Y), Z)),
         multiply(identity, multiply(Z, Z)))) =
     multiply(X,
       multiply(multiply(X, multiply(multiply(X, Y), Z)),
         multiply(identity, multiply(Z, Z))))),
    introduced(tautology,
               [refl,
                [$fot(multiply(X,
                        multiply(multiply(X, multiply(multiply(X, Y), Z)),
                          multiply(identity, multiply(Z, Z)))))]])).

cnf(refute_0_12, plain,
    (multiply(X,
       multiply(multiply(X, multiply(multiply(X, Y), Z)),
         multiply(identity, multiply(Z, Z)))) !=
     multiply(X,
       multiply(multiply(X, multiply(multiply(X, Y), Z)),
         multiply(identity, multiply(Z, Z)))) |
     multiply(multiply(X, multiply(multiply(X, Y), Z)),
       multiply(identity, multiply(Z, Z))) !=
     multiply(X,
       multiply(multiply(X, multiply(Y, Z)),
         multiply(identity, multiply(Z, Z)))) |
     multiply(X,
       multiply(multiply(X, multiply(multiply(X, Y), Z)),
         multiply(identity, multiply(Z, Z)))) =
     multiply(X,
       multiply(X,
         multiply(multiply(X, multiply(Y, Z)),
           multiply(identity, multiply(Z, Z)))))),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(X,
                          multiply(multiply(X,
                              multiply(multiply(X, Y), Z)),
                            multiply(identity, multiply(Z, Z)))),
                        multiply(X,
                          multiply(multiply(X,
                              multiply(multiply(X, Y), Z)),
                            multiply(identity, multiply(Z, Z)))))), [1, 1],
                 $fot(multiply(X,
                        multiply(multiply(X, multiply(Y, Z)),
                          multiply(identity, multiply(Z, Z)))))]])).

cnf(refute_0_13, plain,
    (multiply(multiply(X, multiply(multiply(X, Y), Z)),
       multiply(identity, multiply(Z, Z))) !=
     multiply(X,
       multiply(multiply(X, multiply(Y, Z)),
         multiply(identity, multiply(Z, Z)))) |
     multiply(X,
       multiply(multiply(X, multiply(multiply(X, Y), Z)),
         multiply(identity, multiply(Z, Z)))) =
     multiply(X,
       multiply(X,
         multiply(multiply(X, multiply(Y, Z)),
           multiply(identity, multiply(Z, Z)))))),
    inference(resolve,
              [$cnf($equal(multiply(X,
                        multiply(multiply(X, multiply(multiply(X, Y), Z)),
                          multiply(identity, multiply(Z, Z)))),
                      multiply(X,
                        multiply(multiply(X, multiply(multiply(X, Y), Z)),
                          multiply(identity, multiply(Z, Z))))))],
              [refute_0_11, refute_0_12])).

cnf(refute_0_14, plain,
    (multiply(X,
       multiply(multiply(X, multiply(multiply(X, Y), Z)),
         multiply(identity, multiply(Z, Z)))) =
     multiply(X,
       multiply(X,
         multiply(multiply(X, multiply(Y, Z)),
           multiply(identity, multiply(Z, Z)))))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(X,
                          multiply(multiply(X, Y), Z)),
                        multiply(identity, multiply(Z, Z))),
                      multiply(X,
                        multiply(multiply(X, multiply(Y, Z)),
                          multiply(identity, multiply(Z, Z))))))],
              [refute_0_10, refute_0_13])).

cnf(refute_0_15, plain,
    (multiply(X,
       multiply(multiply(X, multiply(multiply(X, Y), Z)),
         multiply(identity, multiply(Z, Z)))) != Y |
     multiply(X,
       multiply(multiply(X, multiply(multiply(X, Y), Z)),
         multiply(identity, multiply(Z, Z)))) !=
     multiply(X,
       multiply(X,
         multiply(multiply(X, multiply(Y, Z)),
           multiply(identity, multiply(Z, Z))))) |
     multiply(X,
       multiply(X,
         multiply(multiply(X, multiply(Y, Z)),
           multiply(identity, multiply(Z, Z))))) = Y),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(X,
                          multiply(multiply(X,
                              multiply(multiply(X, Y), Z)),
                            multiply(identity, multiply(Z, Z)))), Y)), [0],
                 $fot(multiply(X,
                        multiply(X,
                          multiply(multiply(X, multiply(Y, Z)),
                            multiply(identity, multiply(Z, Z))))))]])).

cnf(refute_0_16, plain,
    (multiply(X,
       multiply(multiply(X, multiply(multiply(X, Y), Z)),
         multiply(identity, multiply(Z, Z)))) != Y |
     multiply(X,
       multiply(X,
         multiply(multiply(X, multiply(Y, Z)),
           multiply(identity, multiply(Z, Z))))) = Y),
    inference(resolve,
              [$cnf($equal(multiply(X,
                        multiply(multiply(X, multiply(multiply(X, Y), Z)),
                          multiply(identity, multiply(Z, Z)))),
                      multiply(X,
                        multiply(X,
                          multiply(multiply(X, multiply(Y, Z)),
                            multiply(identity, multiply(Z, Z)))))))],
              [refute_0_14, refute_0_15])).

cnf(refute_0_17, plain,
    (multiply(X,
       multiply(X,
         multiply(multiply(X, multiply(Y, Z)),
           multiply(identity, multiply(Z, Z))))) = Y),
    inference(resolve,
              [$cnf($equal(multiply(X,
                        multiply(multiply(X, multiply(multiply(X, Y), Z)),
                          multiply(identity, multiply(Z, Z)))), Y))],
              [single_axiom, refute_0_16])).

cnf(refute_0_18, plain,
    (multiply(Y,
       multiply(Y,
         multiply(multiply(Y,
             multiply(Y,
               multiply(multiply(Y, multiply(X_4, X_5)),
                 multiply(identity, multiply(X_5, X_5))))),
           multiply(identity,
             multiply(multiply(multiply(Y, multiply(X_4, X_5)),
                 multiply(identity, multiply(X_5, X_5))),
               multiply(multiply(Y, multiply(X_4, X_5)),
                 multiply(identity, multiply(X_5, X_5)))))))) = Y),
    inference(subst, [],
              [refute_0_17 :
               [bind(X, $fot(Y)),
                bind(Z,
                     $fot(multiply(multiply(Y, multiply(X_4, X_5)),
                            multiply(identity, multiply(X_5, X_5)))))]])).

cnf(refute_0_19, plain,
    (multiply(Y,
       multiply(Y,
         multiply(multiply(Y, multiply(X_4, X_5)),
           multiply(identity, multiply(X_5, X_5))))) = X_4),
    inference(subst, [],
              [refute_0_17 :
               [bind(X, $fot(Y)), bind(Y, $fot(X_4)),
                bind(Z, $fot(X_5))]])).

cnf(refute_0_20, plain,
    (multiply(Y,
       multiply(Y,
         multiply(multiply(Y,
             multiply(Y,
               multiply(multiply(Y, multiply(X_4, X_5)),
                 multiply(identity, multiply(X_5, X_5))))),
           multiply(identity,
             multiply(multiply(multiply(Y, multiply(X_4, X_5)),
                 multiply(identity, multiply(X_5, X_5))),
               multiply(multiply(Y, multiply(X_4, X_5)),
                 multiply(identity, multiply(X_5, X_5)))))))) != Y |
     multiply(Y,
       multiply(Y,
         multiply(multiply(Y, multiply(X_4, X_5)),
           multiply(identity, multiply(X_5, X_5))))) != X_4 |
     multiply(Y,
       multiply(Y,
         multiply(X_4,
           multiply(identity,
             multiply(multiply(multiply(Y, multiply(X_4, X_5)),
                 multiply(identity, multiply(X_5, X_5))),
               multiply(multiply(Y, multiply(X_4, X_5)),
                 multiply(identity, multiply(X_5, X_5)))))))) = Y),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(Y,
                          multiply(Y,
                            multiply(multiply(Y,
                                multiply(Y,
                                  multiply(multiply(Y, multiply(X_4, X_5)),
                                    multiply(identity,
                                      multiply(X_5, X_5))))),
                              multiply(identity,
                                multiply(multiply(multiply(Y,
                                      multiply(X_4, X_5)),
                                    multiply(identity,
                                      multiply(X_5, X_5))),
                                  multiply(multiply(Y, multiply(X_4, X_5)),
                                    multiply(identity,
                                      multiply(X_5, X_5)))))))), Y)),
                 [0, 1, 1, 0], $fot(X_4)]])).

cnf(refute_0_21, plain,
    (multiply(Y,
       multiply(Y,
         multiply(multiply(Y,
             multiply(Y,
               multiply(multiply(Y, multiply(X_4, X_5)),
                 multiply(identity, multiply(X_5, X_5))))),
           multiply(identity,
             multiply(multiply(multiply(Y, multiply(X_4, X_5)),
                 multiply(identity, multiply(X_5, X_5))),
               multiply(multiply(Y, multiply(X_4, X_5)),
                 multiply(identity, multiply(X_5, X_5)))))))) != Y |
     multiply(Y,
       multiply(Y,
         multiply(X_4,
           multiply(identity,
             multiply(multiply(multiply(Y, multiply(X_4, X_5)),
                 multiply(identity, multiply(X_5, X_5))),
               multiply(multiply(Y, multiply(X_4, X_5)),
                 multiply(identity, multiply(X_5, X_5)))))))) = Y),
    inference(resolve,
              [$cnf($equal(multiply(Y,
                        multiply(Y,
                          multiply(multiply(Y, multiply(X_4, X_5)),
                            multiply(identity, multiply(X_5, X_5))))),
                      X_4))], [refute_0_19, refute_0_20])).

cnf(refute_0_22, plain,
    (multiply(Y,
       multiply(Y,
         multiply(X_4,
           multiply(identity,
             multiply(multiply(multiply(Y, multiply(X_4, X_5)),
                 multiply(identity, multiply(X_5, X_5))),
               multiply(multiply(Y, multiply(X_4, X_5)),
                 multiply(identity, multiply(X_5, X_5)))))))) = Y),
    inference(resolve,
              [$cnf($equal(multiply(Y,
                        multiply(Y,
                          multiply(multiply(Y,
                              multiply(Y,
                                multiply(multiply(Y, multiply(X_4, X_5)),
                                  multiply(identity,
                                    multiply(X_5, X_5))))),
                            multiply(identity,
                              multiply(multiply(multiply(Y,
                                    multiply(X_4, X_5)),
                                  multiply(identity, multiply(X_5, X_5))),
                                multiply(multiply(Y, multiply(X_4, X_5)),
                                  multiply(identity,
                                    multiply(X_5, X_5)))))))), Y))],
              [refute_0_18, refute_0_21])).

cnf(refute_0_23, plain,
    (multiply(X_4,
       multiply(X_4,
         multiply(X_4,
           multiply(identity,
             multiply(multiply(multiply(X_4,
                   multiply(X_4,
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11)))))))),
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11))))))))),
               multiply(multiply(X_4,
                   multiply(X_4,
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11)))))))),
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11)))))))))))))) = X_4),
    inference(subst, [],
              [refute_0_22 :
               [bind(Y, $fot(X_4)),
                bind(X_5,
                     $fot(multiply(X_10,
                            multiply(identity,
                              multiply(multiply(multiply(X_4,
                                    multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11))),
                                multiply(multiply(X_4,
                                    multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11))))))))]])).

cnf(refute_0_24, plain,
    (multiply(X_4,
       multiply(X_4,
         multiply(X_10,
           multiply(identity,
             multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11))),
               multiply(multiply(X_4, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11)))))))) = X_4),
    inference(subst, [],
              [refute_0_22 :
               [bind(Y, $fot(X_4)), bind(X_4, $fot(X_10)),
                bind(X_5, $fot(X_11))]])).

cnf(refute_0_25, plain,
    (multiply(X_4,
       multiply(X_4,
         multiply(X_10,
           multiply(identity,
             multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11))),
               multiply(multiply(X_4, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11)))))))) != X_4 |
     multiply(X_4,
       multiply(X_4,
         multiply(X_4,
           multiply(identity,
             multiply(multiply(multiply(X_4,
                   multiply(X_4,
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11)))))))),
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11))))))))),
               multiply(multiply(X_4,
                   multiply(X_4,
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11)))))))),
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11)))))))))))))) != X_4 |
     multiply(X_4,
       multiply(X_4,
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11))))))))),
               multiply(multiply(X_4,
                   multiply(X_4,
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11)))))))),
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11)))))))))))))) = X_4),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(X_4,
                          multiply(X_4,
                            multiply(X_4,
                              multiply(identity,
                                multiply(multiply(multiply(X_4,
                                      multiply(X_4,
                                        multiply(X_10,
                                          multiply(identity,
                                            multiply(multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11, X_11))),
                                              multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11,
                                                    X_11)))))))),
                                    multiply(identity,
                                      multiply(multiply(X_10,
                                          multiply(identity,
                                            multiply(multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11, X_11))),
                                              multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11,
                                                    X_11)))))),
                                        multiply(X_10,
                                          multiply(identity,
                                            multiply(multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11, X_11))),
                                              multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11,
                                                    X_11))))))))),
                                  multiply(multiply(X_4,
                                      multiply(X_4,
                                        multiply(X_10,
                                          multiply(identity,
                                            multiply(multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11, X_11))),
                                              multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11,
                                                    X_11)))))))),
                                    multiply(identity,
                                      multiply(multiply(X_10,
                                          multiply(identity,
                                            multiply(multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11, X_11))),
                                              multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11,
                                                    X_11)))))),
                                        multiply(X_10,
                                          multiply(identity,
                                            multiply(multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11, X_11))),
                                              multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11,
                                                    X_11)))))))))))))),
                        X_4)), [0, 1, 1, 1, 1, 0, 0], $fot(X_4)]])).

cnf(refute_0_26, plain,
    (multiply(X_4,
       multiply(X_4,
         multiply(X_4,
           multiply(identity,
             multiply(multiply(multiply(X_4,
                   multiply(X_4,
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11)))))))),
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11))))))))),
               multiply(multiply(X_4,
                   multiply(X_4,
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11)))))))),
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11)))))))))))))) != X_4 |
     multiply(X_4,
       multiply(X_4,
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11))))))))),
               multiply(multiply(X_4,
                   multiply(X_4,
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11)))))))),
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11)))))))))))))) = X_4),
    inference(resolve,
              [$cnf($equal(multiply(X_4,
                        multiply(X_4,
                          multiply(X_10,
                            multiply(identity,
                              multiply(multiply(multiply(X_4,
                                    multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11))),
                                multiply(multiply(X_4,
                                    multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11)))))))), X_4))],
              [refute_0_24, refute_0_25])).

cnf(refute_0_27, plain,
    (multiply(X_4,
       multiply(X_4,
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11))))))))),
               multiply(multiply(X_4,
                   multiply(X_4,
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11)))))))),
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11)))))))))))))) = X_4),
    inference(resolve,
              [$cnf($equal(multiply(X_4,
                        multiply(X_4,
                          multiply(X_4,
                            multiply(identity,
                              multiply(multiply(multiply(X_4,
                                    multiply(X_4,
                                      multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11,
                                                  X_11)))))))),
                                  multiply(identity,
                                    multiply(multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11)))))),
                                      multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11,
                                                  X_11))))))))),
                                multiply(multiply(X_4,
                                    multiply(X_4,
                                      multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11,
                                                  X_11)))))))),
                                  multiply(identity,
                                    multiply(multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11)))))),
                                      multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11,
                                                  X_11)))))))))))))),
                      X_4))], [refute_0_23, refute_0_26])).

cnf(refute_0_28, plain,
    (multiply(multiply(X_4,
         multiply(X_4,
           multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11)))))))),
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11)))))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))))))))) =
     multiply(multiply(X_4,
         multiply(X_4,
           multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11)))))))),
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11)))))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11)))))))))),
    introduced(tautology,
               [refl,
                [$fot(multiply(multiply(X_4,
                          multiply(X_4,
                            multiply(X_10,
                              multiply(identity,
                                multiply(multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11))),
                                  multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11)))))))),
                        multiply(identity,
                          multiply(multiply(X_10,
                              multiply(identity,
                                multiply(multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11))),
                                  multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11)))))),
                            multiply(X_10,
                              multiply(identity,
                                multiply(multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11))),
                                  multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11))))))))))]])).

cnf(refute_0_29, plain,
    (multiply(X_4,
       multiply(X_4,
         multiply(X_10,
           multiply(identity,
             multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11))),
               multiply(multiply(X_4, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11)))))))) != X_4 |
     multiply(multiply(X_4,
         multiply(X_4,
           multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11)))))))),
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11)))))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))))))))) !=
     multiply(multiply(X_4,
         multiply(X_4,
           multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11)))))))),
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11)))))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))))))))) |
     multiply(multiply(X_4,
         multiply(X_4,
           multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11)))))))),
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11)))))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))))))))) =
     multiply(X_4,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11)))))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11)))))))))),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(multiply(X_4,
                            multiply(X_4,
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11)))))))),
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))))))))),
                        multiply(multiply(X_4,
                            multiply(X_4,
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11)))))))),
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))))))))))),
                 [1, 0], $fot(X_4)]])).

cnf(refute_0_30, plain,
    (multiply(X_4,
       multiply(X_4,
         multiply(X_10,
           multiply(identity,
             multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11))),
               multiply(multiply(X_4, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11)))))))) != X_4 |
     multiply(multiply(X_4,
         multiply(X_4,
           multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11)))))))),
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11)))))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))))))))) =
     multiply(X_4,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11)))))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11)))))))))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(X_4,
                          multiply(X_4,
                            multiply(X_10,
                              multiply(identity,
                                multiply(multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11))),
                                  multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11)))))))),
                        multiply(identity,
                          multiply(multiply(X_10,
                              multiply(identity,
                                multiply(multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11))),
                                  multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11)))))),
                            multiply(X_10,
                              multiply(identity,
                                multiply(multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11))),
                                  multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11))))))))),
                      multiply(multiply(X_4,
                          multiply(X_4,
                            multiply(X_10,
                              multiply(identity,
                                multiply(multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11))),
                                  multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11)))))))),
                        multiply(identity,
                          multiply(multiply(X_10,
                              multiply(identity,
                                multiply(multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11))),
                                  multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11)))))),
                            multiply(X_10,
                              multiply(identity,
                                multiply(multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11))),
                                  multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11)))))))))))],
              [refute_0_28, refute_0_29])).

cnf(refute_0_31, plain,
    (multiply(multiply(X_4,
         multiply(X_4,
           multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11)))))))),
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11)))))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))))))))) =
     multiply(X_4,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11)))))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11)))))))))),
    inference(resolve,
              [$cnf($equal(multiply(X_4,
                        multiply(X_4,
                          multiply(X_10,
                            multiply(identity,
                              multiply(multiply(multiply(X_4,
                                    multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11))),
                                multiply(multiply(X_4,
                                    multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11)))))))), X_4))],
              [refute_0_24, refute_0_30])).

cnf(refute_0_32, plain,
    (multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))))))))),
       multiply(multiply(X_4,
           multiply(X_4,
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))))),
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))))))) =
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))))))))),
       multiply(multiply(X_4,
           multiply(X_4,
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))))),
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))))))))))),
    introduced(tautology,
               [refl,
                [$fot(multiply(multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))))))))),
                        multiply(multiply(X_4,
                            multiply(X_4,
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11)))))))),
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11)))))))))))]])).

cnf(refute_0_33, plain,
    (multiply(multiply(X_4,
         multiply(X_4,
           multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11)))))))),
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11)))))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))))))))) !=
     multiply(X_4,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11)))))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))))))))) |
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))))))))),
       multiply(multiply(X_4,
           multiply(X_4,
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))))),
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))))))) !=
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))))))))),
       multiply(multiply(X_4,
           multiply(X_4,
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))))),
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))))))) |
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))))))))),
       multiply(multiply(X_4,
           multiply(X_4,
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))))),
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))))))) =
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))))))))))),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11)))))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))))))))),
                          multiply(multiply(X_4,
                              multiply(X_4,
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11)))))))),
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11)))))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11)))))))))),
                        multiply(multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11)))))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))))))))),
                          multiply(multiply(X_4,
                              multiply(X_4,
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11)))))))),
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11)))))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11)))))))))))),
                 [1, 1],
                 $fot(multiply(X_4,
                        multiply(identity,
                          multiply(multiply(X_10,
                              multiply(identity,
                                multiply(multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11))),
                                  multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11)))))),
                            multiply(X_10,
                              multiply(identity,
                                multiply(multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11))),
                                  multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11))))))))))]])).

cnf(refute_0_34, plain,
    (multiply(multiply(X_4,
         multiply(X_4,
           multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11)))))))),
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11)))))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))))))))) !=
     multiply(X_4,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11)))))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))))))))) |
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))))))))),
       multiply(multiply(X_4,
           multiply(X_4,
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))))),
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))))))) =
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))))))))))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))))))))),
                        multiply(multiply(X_4,
                            multiply(X_4,
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11)))))))),
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11)))))))))),
                      multiply(multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))))))))),
                        multiply(multiply(X_4,
                            multiply(X_4,
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11)))))))),
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))))))))))))],
              [refute_0_32, refute_0_33])).

cnf(refute_0_35, plain,
    (multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))))))))),
       multiply(multiply(X_4,
           multiply(X_4,
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))))),
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))))))) =
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))))))))))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(X_4,
                          multiply(X_4,
                            multiply(X_10,
                              multiply(identity,
                                multiply(multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11))),
                                  multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11)))))))),
                        multiply(identity,
                          multiply(multiply(X_10,
                              multiply(identity,
                                multiply(multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11))),
                                  multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11)))))),
                            multiply(X_10,
                              multiply(identity,
                                multiply(multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11))),
                                  multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11))))))))),
                      multiply(X_4,
                        multiply(identity,
                          multiply(multiply(X_10,
                              multiply(identity,
                                multiply(multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11))),
                                  multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11)))))),
                            multiply(X_10,
                              multiply(identity,
                                multiply(multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11))),
                                  multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11)))))))))))],
              [refute_0_31, refute_0_34])).

cnf(refute_0_36, plain,
    (multiply(identity,
       multiply(multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))))))))),
         multiply(multiply(X_4,
             multiply(X_4,
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))))),
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))))))))))) =
     multiply(identity,
       multiply(multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))))))))),
         multiply(multiply(X_4,
             multiply(X_4,
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))))),
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))))))))),
    introduced(tautology,
               [refl,
                [$fot(multiply(identity,
                        multiply(multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11)))))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))))))))),
                          multiply(multiply(X_4,
                              multiply(X_4,
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11)))))))),
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11)))))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11,
                                            X_11))))))))))))]])).

cnf(refute_0_37, plain,
    (multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))))))))),
       multiply(multiply(X_4,
           multiply(X_4,
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))))),
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))))))) !=
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))))))) |
     multiply(identity,
       multiply(multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))))))))),
         multiply(multiply(X_4,
             multiply(X_4,
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))))),
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))))))))))) !=
     multiply(identity,
       multiply(multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))))))))),
         multiply(multiply(X_4,
             multiply(X_4,
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))))),
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))))))))))) |
     multiply(identity,
       multiply(multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))))))))),
         multiply(multiply(X_4,
             multiply(X_4,
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))))),
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))))))))))) =
     multiply(identity,
       multiply(multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))))))))),
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))))))))),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(identity,
                          multiply(multiply(X_4,
                              multiply(identity,
                                multiply(multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11)))))),
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))))))))),
                            multiply(multiply(X_4,
                                multiply(X_4,
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11)))))))),
                              multiply(identity,
                                multiply(multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11)))))),
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))))))))))),
                        multiply(identity,
                          multiply(multiply(X_4,
                              multiply(identity,
                                multiply(multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11)))))),
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))))))))),
                            multiply(multiply(X_4,
                                multiply(X_4,
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11)))))))),
                              multiply(identity,
                                multiply(multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11)))))),
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11,
                                              X_11))))))))))))), [1, 1],
                 $fot(multiply(multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))))))))),
                        multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11)))))))))))]])).

cnf(refute_0_38, plain,
    (multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))))))))),
       multiply(multiply(X_4,
           multiply(X_4,
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))))),
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))))))) !=
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))))))) |
     multiply(identity,
       multiply(multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))))))))),
         multiply(multiply(X_4,
             multiply(X_4,
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))))),
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))))))))))) =
     multiply(identity,
       multiply(multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))))))))),
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))))))))),
    inference(resolve,
              [$cnf($equal(multiply(identity,
                        multiply(multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11)))))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))))))))),
                          multiply(multiply(X_4,
                              multiply(X_4,
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11)))))))),
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11)))))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))))))))))),
                      multiply(identity,
                        multiply(multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11)))))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))))))))),
                          multiply(multiply(X_4,
                              multiply(X_4,
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11)))))))),
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11)))))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11,
                                            X_11)))))))))))))],
              [refute_0_36, refute_0_37])).

cnf(refute_0_39, plain,
    (multiply(identity,
       multiply(multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))))))))),
         multiply(multiply(X_4,
             multiply(X_4,
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))))),
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))))))))))) =
     multiply(identity,
       multiply(multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))))))))),
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))))))))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))))))))),
                        multiply(multiply(X_4,
                            multiply(X_4,
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11)))))))),
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11)))))))))),
                      multiply(multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))))))))),
                        multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))))))))))))],
              [refute_0_35, refute_0_38])).

cnf(refute_0_40, plain,
    (multiply(X_4,
       multiply(identity,
         multiply(multiply(X_4,
             multiply(identity,
               multiply(multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11)))))),
                 multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))))))))),
           multiply(multiply(X_4,
               multiply(X_4,
                 multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11)))))))),
             multiply(identity,
               multiply(multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11)))))),
                 multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity,
                           multiply(X_11, X_11)))))))))))) =
     multiply(X_4,
       multiply(identity,
         multiply(multiply(X_4,
             multiply(identity,
               multiply(multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11)))))),
                 multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))))))))),
           multiply(multiply(X_4,
               multiply(X_4,
                 multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11)))))))),
             multiply(identity,
               multiply(multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11)))))),
                 multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity,
                           multiply(X_11, X_11))))))))))))),
    introduced(tautology,
               [refl,
                [$fot(multiply(X_4,
                        multiply(identity,
                          multiply(multiply(X_4,
                              multiply(identity,
                                multiply(multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11)))))),
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))))))))),
                            multiply(multiply(X_4,
                                multiply(X_4,
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11)))))))),
                              multiply(identity,
                                multiply(multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11)))))),
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11,
                                              X_11)))))))))))))]])).

cnf(refute_0_41, plain,
    (multiply(X_4,
       multiply(identity,
         multiply(multiply(X_4,
             multiply(identity,
               multiply(multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11)))))),
                 multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))))))))),
           multiply(multiply(X_4,
               multiply(X_4,
                 multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11)))))))),
             multiply(identity,
               multiply(multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11)))))),
                 multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity,
                           multiply(X_11, X_11)))))))))))) !=
     multiply(X_4,
       multiply(identity,
         multiply(multiply(X_4,
             multiply(identity,
               multiply(multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11)))))),
                 multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))))))))),
           multiply(multiply(X_4,
               multiply(X_4,
                 multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11)))))))),
             multiply(identity,
               multiply(multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11)))))),
                 multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity,
                           multiply(X_11, X_11)))))))))))) |
     multiply(identity,
       multiply(multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))))))))),
         multiply(multiply(X_4,
             multiply(X_4,
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))))),
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))))))))))) !=
     multiply(identity,
       multiply(multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))))))))),
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))))))))))) |
     multiply(X_4,
       multiply(identity,
         multiply(multiply(X_4,
             multiply(identity,
               multiply(multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11)))))),
                 multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))))))))),
           multiply(multiply(X_4,
               multiply(X_4,
                 multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11)))))))),
             multiply(identity,
               multiply(multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11)))))),
                 multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity,
                           multiply(X_11, X_11)))))))))))) =
     multiply(X_4,
       multiply(identity,
         multiply(multiply(X_4,
             multiply(identity,
               multiply(multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11)))))),
                 multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))))))))),
           multiply(X_4,
             multiply(identity,
               multiply(multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11)))))),
                 multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity,
                           multiply(X_11, X_11))))))))))))),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_4,
                                multiply(identity,
                                  multiply(multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))),
                                          multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11)))))),
                                    multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))),
                                          multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))))))))),
                              multiply(multiply(X_4,
                                  multiply(X_4,
                                    multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))),
                                          multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11)))))))),
                                multiply(identity,
                                  multiply(multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))),
                                          multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11)))))),
                                    multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))),
                                          multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11,
                                                X_11)))))))))))),
                        multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_4,
                                multiply(identity,
                                  multiply(multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))),
                                          multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11)))))),
                                    multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))),
                                          multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))))))))),
                              multiply(multiply(X_4,
                                  multiply(X_4,
                                    multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))),
                                          multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11)))))))),
                                multiply(identity,
                                  multiply(multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))),
                                          multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11)))))),
                                    multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))),
                                          multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11,
                                                X_11)))))))))))))), [1, 1],
                 $fot(multiply(identity,
                        multiply(multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11)))))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))))))))),
                          multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11)))))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11,
                                            X_11))))))))))))]])).

cnf(refute_0_42, plain,
    (multiply(identity,
       multiply(multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))))))))),
         multiply(multiply(X_4,
             multiply(X_4,
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))))),
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))))))))))) !=
     multiply(identity,
       multiply(multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))))))))),
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))))))))))) |
     multiply(X_4,
       multiply(identity,
         multiply(multiply(X_4,
             multiply(identity,
               multiply(multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11)))))),
                 multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))))))))),
           multiply(multiply(X_4,
               multiply(X_4,
                 multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11)))))))),
             multiply(identity,
               multiply(multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11)))))),
                 multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity,
                           multiply(X_11, X_11)))))))))))) =
     multiply(X_4,
       multiply(identity,
         multiply(multiply(X_4,
             multiply(identity,
               multiply(multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11)))))),
                 multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))))))))),
           multiply(X_4,
             multiply(identity,
               multiply(multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11)))))),
                 multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity,
                           multiply(X_11, X_11))))))))))))),
    inference(resolve,
              [$cnf($equal(multiply(X_4,
                        multiply(identity,
                          multiply(multiply(X_4,
                              multiply(identity,
                                multiply(multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11)))))),
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))))))))),
                            multiply(multiply(X_4,
                                multiply(X_4,
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11)))))))),
                              multiply(identity,
                                multiply(multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11)))))),
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11,
                                              X_11)))))))))))),
                      multiply(X_4,
                        multiply(identity,
                          multiply(multiply(X_4,
                              multiply(identity,
                                multiply(multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11)))))),
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))))))))),
                            multiply(multiply(X_4,
                                multiply(X_4,
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11)))))))),
                              multiply(identity,
                                multiply(multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11)))))),
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11,
                                              X_11))))))))))))))],
              [refute_0_40, refute_0_41])).

cnf(refute_0_43, plain,
    (multiply(X_4,
       multiply(identity,
         multiply(multiply(X_4,
             multiply(identity,
               multiply(multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11)))))),
                 multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))))))))),
           multiply(multiply(X_4,
               multiply(X_4,
                 multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11)))))))),
             multiply(identity,
               multiply(multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11)))))),
                 multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity,
                           multiply(X_11, X_11)))))))))))) =
     multiply(X_4,
       multiply(identity,
         multiply(multiply(X_4,
             multiply(identity,
               multiply(multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11)))))),
                 multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))))))))),
           multiply(X_4,
             multiply(identity,
               multiply(multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11)))))),
                 multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity,
                           multiply(X_11, X_11))))))))))))),
    inference(resolve,
              [$cnf($equal(multiply(identity,
                        multiply(multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11)))))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))))))))),
                          multiply(multiply(X_4,
                              multiply(X_4,
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11)))))))),
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11)))))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))))))))))),
                      multiply(identity,
                        multiply(multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11)))))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))))))))),
                          multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11)))))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11,
                                            X_11)))))))))))))],
              [refute_0_39, refute_0_42])).

cnf(refute_0_44, plain,
    (multiply(X_4,
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(identity,
                 multiply(multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11)))))),
                   multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))))))))),
             multiply(multiply(X_4,
                 multiply(X_4,
                   multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11)))))))),
               multiply(identity,
                 multiply(multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11)))))),
                   multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity,
                             multiply(X_11, X_11))))))))))))) =
     multiply(X_4,
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(identity,
                 multiply(multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11)))))),
                   multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))))))))),
             multiply(multiply(X_4,
                 multiply(X_4,
                   multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11)))))))),
               multiply(identity,
                 multiply(multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11)))))),
                   multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity,
                             multiply(X_11, X_11)))))))))))))),
    introduced(tautology,
               [refl,
                [$fot(multiply(X_4,
                        multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_4,
                                multiply(identity,
                                  multiply(multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))),
                                          multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11)))))),
                                    multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))),
                                          multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))))))))),
                              multiply(multiply(X_4,
                                  multiply(X_4,
                                    multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))),
                                          multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11)))))))),
                                multiply(identity,
                                  multiply(multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))),
                                          multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11)))))),
                                    multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))),
                                          multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11,
                                                X_11))))))))))))))]])).

cnf(refute_0_45, plain,
    (multiply(X_4,
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(identity,
                 multiply(multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11)))))),
                   multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))))))))),
             multiply(multiply(X_4,
                 multiply(X_4,
                   multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11)))))))),
               multiply(identity,
                 multiply(multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11)))))),
                   multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity,
                             multiply(X_11, X_11))))))))))))) !=
     multiply(X_4,
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(identity,
                 multiply(multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11)))))),
                   multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))))))))),
             multiply(multiply(X_4,
                 multiply(X_4,
                   multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11)))))))),
               multiply(identity,
                 multiply(multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11)))))),
                   multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity,
                             multiply(X_11, X_11))))))))))))) |
     multiply(X_4,
       multiply(identity,
         multiply(multiply(X_4,
             multiply(identity,
               multiply(multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11)))))),
                 multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))))))))),
           multiply(multiply(X_4,
               multiply(X_4,
                 multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11)))))))),
             multiply(identity,
               multiply(multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11)))))),
                 multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity,
                           multiply(X_11, X_11)))))))))))) !=
     multiply(X_4,
       multiply(identity,
         multiply(multiply(X_4,
             multiply(identity,
               multiply(multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11)))))),
                 multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))))))))),
           multiply(X_4,
             multiply(identity,
               multiply(multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11)))))),
                 multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity,
                           multiply(X_11, X_11)))))))))))) |
     multiply(X_4,
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(identity,
                 multiply(multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11)))))),
                   multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))))))))),
             multiply(multiply(X_4,
                 multiply(X_4,
                   multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11)))))))),
               multiply(identity,
                 multiply(multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11)))))),
                   multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity,
                             multiply(X_11, X_11))))))))))))) =
     multiply(X_4,
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(identity,
                 multiply(multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11)))))),
                   multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))))))))),
             multiply(X_4,
               multiply(identity,
                 multiply(multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11)))))),
                   multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity,
                             multiply(X_11, X_11)))))))))))))),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(X_4,
                          multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_4,
                                  multiply(identity,
                                    multiply(multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11)))))),
                                      multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11,
                                                  X_11))))))))),
                                multiply(multiply(X_4,
                                    multiply(X_4,
                                      multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11,
                                                  X_11)))))))),
                                  multiply(identity,
                                    multiply(multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11)))))),
                                      multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11,
                                                  X_11))))))))))))),
                        multiply(X_4,
                          multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_4,
                                  multiply(identity,
                                    multiply(multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11)))))),
                                      multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11,
                                                  X_11))))))))),
                                multiply(multiply(X_4,
                                    multiply(X_4,
                                      multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11,
                                                  X_11)))))))),
                                  multiply(identity,
                                    multiply(multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11)))))),
                                      multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11,
                                                  X_11))))))))))))))),
                 [1, 1],
                 $fot(multiply(X_4,
                        multiply(identity,
                          multiply(multiply(X_4,
                              multiply(identity,
                                multiply(multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11)))))),
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))))))))),
                            multiply(X_4,
                              multiply(identity,
                                multiply(multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11)))))),
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11,
                                              X_11)))))))))))))]])).

cnf(refute_0_46, plain,
    (multiply(X_4,
       multiply(identity,
         multiply(multiply(X_4,
             multiply(identity,
               multiply(multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11)))))),
                 multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))))))))),
           multiply(multiply(X_4,
               multiply(X_4,
                 multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11)))))))),
             multiply(identity,
               multiply(multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11)))))),
                 multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity,
                           multiply(X_11, X_11)))))))))))) !=
     multiply(X_4,
       multiply(identity,
         multiply(multiply(X_4,
             multiply(identity,
               multiply(multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11)))))),
                 multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))))))))),
           multiply(X_4,
             multiply(identity,
               multiply(multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11)))))),
                 multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity,
                           multiply(X_11, X_11)))))))))))) |
     multiply(X_4,
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(identity,
                 multiply(multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11)))))),
                   multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))))))))),
             multiply(multiply(X_4,
                 multiply(X_4,
                   multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11)))))))),
               multiply(identity,
                 multiply(multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11)))))),
                   multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity,
                             multiply(X_11, X_11))))))))))))) =
     multiply(X_4,
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(identity,
                 multiply(multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11)))))),
                   multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))))))))),
             multiply(X_4,
               multiply(identity,
                 multiply(multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11)))))),
                   multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity,
                             multiply(X_11, X_11)))))))))))))),
    inference(resolve,
              [$cnf($equal(multiply(X_4,
                        multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_4,
                                multiply(identity,
                                  multiply(multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))),
                                          multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11)))))),
                                    multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))),
                                          multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))))))))),
                              multiply(multiply(X_4,
                                  multiply(X_4,
                                    multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))),
                                          multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11)))))))),
                                multiply(identity,
                                  multiply(multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))),
                                          multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11)))))),
                                    multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))),
                                          multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11,
                                                X_11))))))))))))),
                      multiply(X_4,
                        multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_4,
                                multiply(identity,
                                  multiply(multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))),
                                          multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11)))))),
                                    multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))),
                                          multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))))))))),
                              multiply(multiply(X_4,
                                  multiply(X_4,
                                    multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))),
                                          multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11)))))))),
                                multiply(identity,
                                  multiply(multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))),
                                          multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11)))))),
                                    multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))),
                                          multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11,
                                                X_11)))))))))))))))],
              [refute_0_44, refute_0_45])).

cnf(refute_0_47, plain,
    (multiply(X_4,
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(identity,
                 multiply(multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11)))))),
                   multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))))))))),
             multiply(multiply(X_4,
                 multiply(X_4,
                   multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11)))))))),
               multiply(identity,
                 multiply(multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11)))))),
                   multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity,
                             multiply(X_11, X_11))))))))))))) =
     multiply(X_4,
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(identity,
                 multiply(multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11)))))),
                   multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))))))))),
             multiply(X_4,
               multiply(identity,
                 multiply(multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11)))))),
                   multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity,
                             multiply(X_11, X_11)))))))))))))),
    inference(resolve,
              [$cnf($equal(multiply(X_4,
                        multiply(identity,
                          multiply(multiply(X_4,
                              multiply(identity,
                                multiply(multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11)))))),
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))))))))),
                            multiply(multiply(X_4,
                                multiply(X_4,
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11)))))))),
                              multiply(identity,
                                multiply(multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11)))))),
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11,
                                              X_11)))))))))))),
                      multiply(X_4,
                        multiply(identity,
                          multiply(multiply(X_4,
                              multiply(identity,
                                multiply(multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11)))))),
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))))))))),
                            multiply(X_4,
                              multiply(identity,
                                multiply(multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11)))))),
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11,
                                              X_11))))))))))))))],
              [refute_0_43, refute_0_46])).

cnf(refute_0_48, plain,
    (multiply(X_4,
       multiply(X_4,
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11))))))))),
               multiply(multiply(X_4,
                   multiply(X_4,
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11)))))))),
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11)))))))))))))) =
     multiply(X_4,
       multiply(X_4,
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11))))))))),
               multiply(multiply(X_4,
                   multiply(X_4,
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11)))))))),
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11))))))))))))))),
    introduced(tautology,
               [refl,
                [$fot(multiply(X_4,
                        multiply(X_4,
                          multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_4,
                                  multiply(identity,
                                    multiply(multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11)))))),
                                      multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11,
                                                  X_11))))))))),
                                multiply(multiply(X_4,
                                    multiply(X_4,
                                      multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11,
                                                  X_11)))))))),
                                  multiply(identity,
                                    multiply(multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11)))))),
                                      multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11,
                                                  X_11)))))))))))))))]])).

cnf(refute_0_49, plain,
    (multiply(X_4,
       multiply(X_4,
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11))))))))),
               multiply(multiply(X_4,
                   multiply(X_4,
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11)))))))),
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11)))))))))))))) !=
     multiply(X_4,
       multiply(X_4,
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11))))))))),
               multiply(multiply(X_4,
                   multiply(X_4,
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11)))))))),
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11)))))))))))))) |
     multiply(X_4,
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(identity,
                 multiply(multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11)))))),
                   multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))))))))),
             multiply(multiply(X_4,
                 multiply(X_4,
                   multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11)))))))),
               multiply(identity,
                 multiply(multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11)))))),
                   multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity,
                             multiply(X_11, X_11))))))))))))) !=
     multiply(X_4,
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(identity,
                 multiply(multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11)))))),
                   multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))))))))),
             multiply(X_4,
               multiply(identity,
                 multiply(multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11)))))),
                   multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity,
                             multiply(X_11, X_11))))))))))))) |
     multiply(X_4,
       multiply(X_4,
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11))))))))),
               multiply(multiply(X_4,
                   multiply(X_4,
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11)))))))),
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11)))))))))))))) =
     multiply(X_4,
       multiply(X_4,
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11))))))))),
               multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11))))))))))))))),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(X_4,
                          multiply(X_4,
                            multiply(X_4,
                              multiply(identity,
                                multiply(multiply(X_4,
                                    multiply(identity,
                                      multiply(multiply(X_10,
                                          multiply(identity,
                                            multiply(multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11, X_11))),
                                              multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11,
                                                    X_11)))))),
                                        multiply(X_10,
                                          multiply(identity,
                                            multiply(multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11, X_11))),
                                              multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11,
                                                    X_11))))))))),
                                  multiply(multiply(X_4,
                                      multiply(X_4,
                                        multiply(X_10,
                                          multiply(identity,
                                            multiply(multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11, X_11))),
                                              multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11,
                                                    X_11)))))))),
                                    multiply(identity,
                                      multiply(multiply(X_10,
                                          multiply(identity,
                                            multiply(multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11, X_11))),
                                              multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11,
                                                    X_11)))))),
                                        multiply(X_10,
                                          multiply(identity,
                                            multiply(multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11, X_11))),
                                              multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11,
                                                    X_11)))))))))))))),
                        multiply(X_4,
                          multiply(X_4,
                            multiply(X_4,
                              multiply(identity,
                                multiply(multiply(X_4,
                                    multiply(identity,
                                      multiply(multiply(X_10,
                                          multiply(identity,
                                            multiply(multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11, X_11))),
                                              multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11,
                                                    X_11)))))),
                                        multiply(X_10,
                                          multiply(identity,
                                            multiply(multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11, X_11))),
                                              multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11,
                                                    X_11))))))))),
                                  multiply(multiply(X_4,
                                      multiply(X_4,
                                        multiply(X_10,
                                          multiply(identity,
                                            multiply(multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11, X_11))),
                                              multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11,
                                                    X_11)))))))),
                                    multiply(identity,
                                      multiply(multiply(X_10,
                                          multiply(identity,
                                            multiply(multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11, X_11))),
                                              multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11,
                                                    X_11)))))),
                                        multiply(X_10,
                                          multiply(identity,
                                            multiply(multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11, X_11))),
                                              multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11,
                                                    X_11)))))))))))))))),
                 [1, 1],
                 $fot(multiply(X_4,
                        multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_4,
                                multiply(identity,
                                  multiply(multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))),
                                          multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11)))))),
                                    multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))),
                                          multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))))))))),
                              multiply(X_4,
                                multiply(identity,
                                  multiply(multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))),
                                          multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11)))))),
                                    multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))),
                                          multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11,
                                                X_11))))))))))))))]])).

cnf(refute_0_50, plain,
    (multiply(X_4,
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(identity,
                 multiply(multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11)))))),
                   multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))))))))),
             multiply(multiply(X_4,
                 multiply(X_4,
                   multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11)))))))),
               multiply(identity,
                 multiply(multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11)))))),
                   multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity,
                             multiply(X_11, X_11))))))))))))) !=
     multiply(X_4,
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(identity,
                 multiply(multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11)))))),
                   multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))))))))),
             multiply(X_4,
               multiply(identity,
                 multiply(multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11)))))),
                   multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity,
                             multiply(X_11, X_11))))))))))))) |
     multiply(X_4,
       multiply(X_4,
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11))))))))),
               multiply(multiply(X_4,
                   multiply(X_4,
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11)))))))),
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11)))))))))))))) =
     multiply(X_4,
       multiply(X_4,
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11))))))))),
               multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11))))))))))))))),
    inference(resolve,
              [$cnf($equal(multiply(X_4,
                        multiply(X_4,
                          multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_4,
                                  multiply(identity,
                                    multiply(multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11)))))),
                                      multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11,
                                                  X_11))))))))),
                                multiply(multiply(X_4,
                                    multiply(X_4,
                                      multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11,
                                                  X_11)))))))),
                                  multiply(identity,
                                    multiply(multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11)))))),
                                      multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11,
                                                  X_11)))))))))))))),
                      multiply(X_4,
                        multiply(X_4,
                          multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_4,
                                  multiply(identity,
                                    multiply(multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11)))))),
                                      multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11,
                                                  X_11))))))))),
                                multiply(multiply(X_4,
                                    multiply(X_4,
                                      multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11,
                                                  X_11)))))))),
                                  multiply(identity,
                                    multiply(multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11)))))),
                                      multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11,
                                                  X_11))))))))))))))))],
              [refute_0_48, refute_0_49])).

cnf(refute_0_51, plain,
    (multiply(X_4,
       multiply(X_4,
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11))))))))),
               multiply(multiply(X_4,
                   multiply(X_4,
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11)))))))),
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11)))))))))))))) =
     multiply(X_4,
       multiply(X_4,
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11))))))))),
               multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11))))))))))))))),
    inference(resolve,
              [$cnf($equal(multiply(X_4,
                        multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_4,
                                multiply(identity,
                                  multiply(multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))),
                                          multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11)))))),
                                    multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))),
                                          multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))))))))),
                              multiply(multiply(X_4,
                                  multiply(X_4,
                                    multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))),
                                          multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11)))))))),
                                multiply(identity,
                                  multiply(multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))),
                                          multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11)))))),
                                    multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))),
                                          multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11,
                                                X_11))))))))))))),
                      multiply(X_4,
                        multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_4,
                                multiply(identity,
                                  multiply(multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))),
                                          multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11)))))),
                                    multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))),
                                          multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))))))))),
                              multiply(X_4,
                                multiply(identity,
                                  multiply(multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))),
                                          multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11)))))),
                                    multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))),
                                          multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11,
                                                X_11)))))))))))))))],
              [refute_0_47, refute_0_50])).

cnf(refute_0_52, plain,
    (multiply(X_4,
       multiply(X_4,
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11))))))))),
               multiply(multiply(X_4,
                   multiply(X_4,
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11)))))))),
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11)))))))))))))) != X_4 |
     multiply(X_4,
       multiply(X_4,
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11))))))))),
               multiply(multiply(X_4,
                   multiply(X_4,
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11)))))))),
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11)))))))))))))) !=
     multiply(X_4,
       multiply(X_4,
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11))))))))),
               multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11)))))))))))))) |
     multiply(X_4,
       multiply(X_4,
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11))))))))),
               multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11)))))))))))))) = X_4),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(X_4,
                          multiply(X_4,
                            multiply(X_4,
                              multiply(identity,
                                multiply(multiply(X_4,
                                    multiply(identity,
                                      multiply(multiply(X_10,
                                          multiply(identity,
                                            multiply(multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11, X_11))),
                                              multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11,
                                                    X_11)))))),
                                        multiply(X_10,
                                          multiply(identity,
                                            multiply(multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11, X_11))),
                                              multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11,
                                                    X_11))))))))),
                                  multiply(multiply(X_4,
                                      multiply(X_4,
                                        multiply(X_10,
                                          multiply(identity,
                                            multiply(multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11, X_11))),
                                              multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11,
                                                    X_11)))))))),
                                    multiply(identity,
                                      multiply(multiply(X_10,
                                          multiply(identity,
                                            multiply(multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11, X_11))),
                                              multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11,
                                                    X_11)))))),
                                        multiply(X_10,
                                          multiply(identity,
                                            multiply(multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11, X_11))),
                                              multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11,
                                                    X_11)))))))))))))),
                        X_4)), [0],
                 $fot(multiply(X_4,
                        multiply(X_4,
                          multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_4,
                                  multiply(identity,
                                    multiply(multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11)))))),
                                      multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11,
                                                  X_11))))))))),
                                multiply(X_4,
                                  multiply(identity,
                                    multiply(multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11)))))),
                                      multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11,
                                                  X_11)))))))))))))))]])).

cnf(refute_0_53, plain,
    (multiply(X_4,
       multiply(X_4,
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11))))))))),
               multiply(multiply(X_4,
                   multiply(X_4,
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11)))))))),
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11)))))))))))))) != X_4 |
     multiply(X_4,
       multiply(X_4,
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11))))))))),
               multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11)))))))))))))) = X_4),
    inference(resolve,
              [$cnf($equal(multiply(X_4,
                        multiply(X_4,
                          multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_4,
                                  multiply(identity,
                                    multiply(multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11)))))),
                                      multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11,
                                                  X_11))))))))),
                                multiply(multiply(X_4,
                                    multiply(X_4,
                                      multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11,
                                                  X_11)))))))),
                                  multiply(identity,
                                    multiply(multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11)))))),
                                      multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11,
                                                  X_11)))))))))))))),
                      multiply(X_4,
                        multiply(X_4,
                          multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_4,
                                  multiply(identity,
                                    multiply(multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11)))))),
                                      multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11,
                                                  X_11))))))))),
                                multiply(X_4,
                                  multiply(identity,
                                    multiply(multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11)))))),
                                      multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11,
                                                  X_11))))))))))))))))],
              [refute_0_51, refute_0_52])).

cnf(refute_0_54, plain,
    (multiply(X_4,
       multiply(X_4,
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11))))))))),
               multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11)))))))))))))) = X_4),
    inference(resolve,
              [$cnf($equal(multiply(X_4,
                        multiply(X_4,
                          multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_4,
                                  multiply(identity,
                                    multiply(multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11)))))),
                                      multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11,
                                                  X_11))))))))),
                                multiply(multiply(X_4,
                                    multiply(X_4,
                                      multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11,
                                                  X_11)))))))),
                                  multiply(identity,
                                    multiply(multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11)))))),
                                      multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11,
                                                  X_11)))))))))))))),
                      X_4))], [refute_0_27, refute_0_53])).

cnf(refute_0_55, plain,
    (multiply(X,
       multiply(multiply(X,
           multiply(multiply(X, multiply(multiply(X, X_1), X_2)),
             multiply(identity, multiply(X_2, X_2)))),
         multiply(identity,
           multiply(multiply(identity, multiply(X_2, X_2)),
             multiply(identity, multiply(X_2, X_2)))))) =
     multiply(multiply(X, X_1), X_2)),
    inference(subst, [],
              [single_axiom :
               [bind(Y, $fot(multiply(multiply(X, X_1), X_2))),
                bind(Z, $fot(multiply(identity, multiply(X_2, X_2))))]])).

cnf(refute_0_56, plain,
    (multiply(X,
       multiply(multiply(X, multiply(multiply(X, X_1), X_2)),
         multiply(identity, multiply(X_2, X_2)))) != X_1 |
     multiply(X,
       multiply(multiply(X,
           multiply(multiply(X, multiply(multiply(X, X_1), X_2)),
             multiply(identity, multiply(X_2, X_2)))),
         multiply(identity,
           multiply(multiply(identity, multiply(X_2, X_2)),
             multiply(identity, multiply(X_2, X_2)))))) !=
     multiply(multiply(X, X_1), X_2) |
     multiply(X,
       multiply(X_1,
         multiply(identity,
           multiply(multiply(identity, multiply(X_2, X_2)),
             multiply(identity, multiply(X_2, X_2)))))) =
     multiply(multiply(X, X_1), X_2)),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(X,
                          multiply(multiply(X,
                              multiply(multiply(X,
                                  multiply(multiply(X, X_1), X_2)),
                                multiply(identity, multiply(X_2, X_2)))),
                            multiply(identity,
                              multiply(multiply(identity,
                                  multiply(X_2, X_2)),
                                multiply(identity, multiply(X_2, X_2)))))),
                        multiply(multiply(X, X_1), X_2))), [0, 1, 0],
                 $fot(X_1)]])).

cnf(refute_0_57, plain,
    (multiply(X,
       multiply(multiply(X,
           multiply(multiply(X, multiply(multiply(X, X_1), X_2)),
             multiply(identity, multiply(X_2, X_2)))),
         multiply(identity,
           multiply(multiply(identity, multiply(X_2, X_2)),
             multiply(identity, multiply(X_2, X_2)))))) !=
     multiply(multiply(X, X_1), X_2) |
     multiply(X,
       multiply(X_1,
         multiply(identity,
           multiply(multiply(identity, multiply(X_2, X_2)),
             multiply(identity, multiply(X_2, X_2)))))) =
     multiply(multiply(X, X_1), X_2)),
    inference(resolve,
              [$cnf($equal(multiply(X,
                        multiply(multiply(X,
                            multiply(multiply(X, X_1), X_2)),
                          multiply(identity, multiply(X_2, X_2)))), X_1))],
              [refute_0_1, refute_0_56])).

cnf(refute_0_58, plain,
    (multiply(X,
       multiply(X_1,
         multiply(identity,
           multiply(multiply(identity, multiply(X_2, X_2)),
             multiply(identity, multiply(X_2, X_2)))))) =
     multiply(multiply(X, X_1), X_2)),
    inference(resolve,
              [$cnf($equal(multiply(X,
                        multiply(multiply(X,
                            multiply(multiply(X,
                                multiply(multiply(X, X_1), X_2)),
                              multiply(identity, multiply(X_2, X_2)))),
                          multiply(identity,
                            multiply(multiply(identity,
                                multiply(X_2, X_2)),
                              multiply(identity, multiply(X_2, X_2)))))),
                      multiply(multiply(X, X_1), X_2)))],
              [refute_0_55, refute_0_57])).

cnf(refute_0_59, plain,
    (multiply(X,
       multiply(X_1,
         multiply(identity,
           multiply(multiply(identity,
               multiply(multiply(identity, multiply(X_14, X_14)),
                 multiply(identity, multiply(X_14, X_14)))),
             multiply(identity,
               multiply(multiply(identity, multiply(X_14, X_14)),
                 multiply(identity, multiply(X_14, X_14)))))))) =
     multiply(multiply(X, X_1), multiply(identity, multiply(X_14, X_14)))),
    inference(subst, [],
              [refute_0_58 :
               [bind(X_2,
                     $fot(multiply(identity, multiply(X_14, X_14))))]])).

cnf(refute_0_60, plain,
    (multiply(identity,
       multiply(multiply(identity,
           multiply(multiply(identity, multiply(X_14, X_14)),
             multiply(identity, multiply(X_14, X_14)))),
         multiply(identity,
           multiply(multiply(identity, multiply(X_14, X_14)),
             multiply(identity, multiply(X_14, X_14)))))) =
     multiply(multiply(identity,
         multiply(identity,
           multiply(multiply(identity, multiply(X_14, X_14)),
             multiply(identity, multiply(X_14, X_14))))), X_14)),
    inference(subst, [],
              [refute_0_58 :
               [bind(X, $fot(identity)),
                bind(X_1,
                     $fot(multiply(identity,
                            multiply(multiply(identity,
                                multiply(X_14, X_14)),
                              multiply(identity, multiply(X_14, X_14)))))),
                bind(X_2, $fot(X_14))]])).

cnf(refute_0_61, plain,
    (multiply(X,
       multiply(X_1,
         multiply(identity,
           multiply(multiply(identity,
               multiply(multiply(identity, multiply(X_14, X_14)),
                 multiply(identity, multiply(X_14, X_14)))),
             multiply(identity,
               multiply(multiply(identity, multiply(X_14, X_14)),
                 multiply(identity, multiply(X_14, X_14)))))))) !=
     multiply(multiply(X, X_1), multiply(identity, multiply(X_14, X_14))) |
     multiply(identity,
       multiply(multiply(identity,
           multiply(multiply(identity, multiply(X_14, X_14)),
             multiply(identity, multiply(X_14, X_14)))),
         multiply(identity,
           multiply(multiply(identity, multiply(X_14, X_14)),
             multiply(identity, multiply(X_14, X_14)))))) !=
     multiply(multiply(identity,
         multiply(identity,
           multiply(multiply(identity, multiply(X_14, X_14)),
             multiply(identity, multiply(X_14, X_14))))), X_14) |
     multiply(X,
       multiply(X_1,
         multiply(multiply(identity,
             multiply(identity,
               multiply(multiply(identity, multiply(X_14, X_14)),
                 multiply(identity, multiply(X_14, X_14))))), X_14))) =
     multiply(multiply(X, X_1), multiply(identity, multiply(X_14, X_14)))),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(X,
                          multiply(X_1,
                            multiply(identity,
                              multiply(multiply(identity,
                                  multiply(multiply(identity,
                                      multiply(X_14, X_14)),
                                    multiply(identity,
                                      multiply(X_14, X_14)))),
                                multiply(identity,
                                  multiply(multiply(identity,
                                      multiply(X_14, X_14)),
                                    multiply(identity,
                                      multiply(X_14, X_14)))))))),
                        multiply(multiply(X, X_1),
                          multiply(identity, multiply(X_14, X_14))))),
                 [0, 1, 1],
                 $fot(multiply(multiply(identity,
                          multiply(identity,
                            multiply(multiply(identity,
                                multiply(X_14, X_14)),
                              multiply(identity, multiply(X_14, X_14))))),
                        X_14))]])).

cnf(refute_0_62, plain,
    (multiply(X,
       multiply(X_1,
         multiply(identity,
           multiply(multiply(identity,
               multiply(multiply(identity, multiply(X_14, X_14)),
                 multiply(identity, multiply(X_14, X_14)))),
             multiply(identity,
               multiply(multiply(identity, multiply(X_14, X_14)),
                 multiply(identity, multiply(X_14, X_14)))))))) !=
     multiply(multiply(X, X_1), multiply(identity, multiply(X_14, X_14))) |
     multiply(X,
       multiply(X_1,
         multiply(multiply(identity,
             multiply(identity,
               multiply(multiply(identity, multiply(X_14, X_14)),
                 multiply(identity, multiply(X_14, X_14))))), X_14))) =
     multiply(multiply(X, X_1), multiply(identity, multiply(X_14, X_14)))),
    inference(resolve,
              [$cnf($equal(multiply(identity,
                        multiply(multiply(identity,
                            multiply(multiply(identity,
                                multiply(X_14, X_14)),
                              multiply(identity, multiply(X_14, X_14)))),
                          multiply(identity,
                            multiply(multiply(identity,
                                multiply(X_14, X_14)),
                              multiply(identity, multiply(X_14, X_14)))))),
                      multiply(multiply(identity,
                          multiply(identity,
                            multiply(multiply(identity,
                                multiply(X_14, X_14)),
                              multiply(identity, multiply(X_14, X_14))))),
                        X_14)))], [refute_0_60, refute_0_61])).

cnf(refute_0_63, plain,
    (multiply(X,
       multiply(X_1,
         multiply(multiply(identity,
             multiply(identity,
               multiply(multiply(identity, multiply(X_14, X_14)),
                 multiply(identity, multiply(X_14, X_14))))), X_14))) =
     multiply(multiply(X, X_1), multiply(identity, multiply(X_14, X_14)))),
    inference(resolve,
              [$cnf($equal(multiply(X,
                        multiply(X_1,
                          multiply(identity,
                            multiply(multiply(identity,
                                multiply(multiply(identity,
                                    multiply(X_14, X_14)),
                                  multiply(identity,
                                    multiply(X_14, X_14)))),
                              multiply(identity,
                                multiply(multiply(identity,
                                    multiply(X_14, X_14)),
                                  multiply(identity,
                                    multiply(X_14, X_14)))))))),
                      multiply(multiply(X, X_1),
                        multiply(identity, multiply(X_14, X_14)))))],
              [refute_0_59, refute_0_62])).

cnf(refute_0_64, plain,
    (multiply(identity,
       multiply(identity,
         multiply(multiply(identity, multiply(X_14, X_14)),
           multiply(identity, multiply(X_14, X_14))))) = X_14),
    inference(subst, [],
              [refute_0_17 :
               [bind(X, $fot(identity)), bind(Y, $fot(X_14)),
                bind(Z, $fot(X_14))]])).

cnf(refute_0_65, plain,
    (multiply(multiply(identity,
         multiply(identity,
           multiply(multiply(identity, multiply(X_14, X_14)),
             multiply(identity, multiply(X_14, X_14))))), X_14) =
     multiply(multiply(identity,
         multiply(identity,
           multiply(multiply(identity, multiply(X_14, X_14)),
             multiply(identity, multiply(X_14, X_14))))), X_14)),
    introduced(tautology,
               [refl,
                [$fot(multiply(multiply(identity,
                          multiply(identity,
                            multiply(multiply(identity,
                                multiply(X_14, X_14)),
                              multiply(identity, multiply(X_14, X_14))))),
                        X_14))]])).

cnf(refute_0_66, plain,
    (multiply(multiply(identity,
         multiply(identity,
           multiply(multiply(identity, multiply(X_14, X_14)),
             multiply(identity, multiply(X_14, X_14))))), X_14) !=
     multiply(multiply(identity,
         multiply(identity,
           multiply(multiply(identity, multiply(X_14, X_14)),
             multiply(identity, multiply(X_14, X_14))))), X_14) |
     multiply(identity,
       multiply(identity,
         multiply(multiply(identity, multiply(X_14, X_14)),
           multiply(identity, multiply(X_14, X_14))))) != X_14 |
     multiply(multiply(identity,
         multiply(identity,
           multiply(multiply(identity, multiply(X_14, X_14)),
             multiply(identity, multiply(X_14, X_14))))), X_14) =
     multiply(X_14, X_14)),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(multiply(identity,
                            multiply(identity,
                              multiply(multiply(identity,
                                  multiply(X_14, X_14)),
                                multiply(identity,
                                  multiply(X_14, X_14))))), X_14),
                        multiply(multiply(identity,
                            multiply(identity,
                              multiply(multiply(identity,
                                  multiply(X_14, X_14)),
                                multiply(identity,
                                  multiply(X_14, X_14))))), X_14))),
                 [1, 0], $fot(X_14)]])).

cnf(refute_0_67, plain,
    (multiply(identity,
       multiply(identity,
         multiply(multiply(identity, multiply(X_14, X_14)),
           multiply(identity, multiply(X_14, X_14))))) != X_14 |
     multiply(multiply(identity,
         multiply(identity,
           multiply(multiply(identity, multiply(X_14, X_14)),
             multiply(identity, multiply(X_14, X_14))))), X_14) =
     multiply(X_14, X_14)),
    inference(resolve,
              [$cnf($equal(multiply(multiply(identity,
                          multiply(identity,
                            multiply(multiply(identity,
                                multiply(X_14, X_14)),
                              multiply(identity, multiply(X_14, X_14))))),
                        X_14),
                      multiply(multiply(identity,
                          multiply(identity,
                            multiply(multiply(identity,
                                multiply(X_14, X_14)),
                              multiply(identity, multiply(X_14, X_14))))),
                        X_14)))], [refute_0_65, refute_0_66])).

cnf(refute_0_68, plain,
    (multiply(multiply(identity,
         multiply(identity,
           multiply(multiply(identity, multiply(X_14, X_14)),
             multiply(identity, multiply(X_14, X_14))))), X_14) =
     multiply(X_14, X_14)),
    inference(resolve,
              [$cnf($equal(multiply(identity,
                        multiply(identity,
                          multiply(multiply(identity,
                              multiply(X_14, X_14)),
                            multiply(identity, multiply(X_14, X_14))))),
                      X_14))], [refute_0_64, refute_0_67])).

cnf(refute_0_69, plain,
    (multiply(X_1,
       multiply(multiply(identity,
           multiply(identity,
             multiply(multiply(identity, multiply(X_14, X_14)),
               multiply(identity, multiply(X_14, X_14))))), X_14)) =
     multiply(X_1,
       multiply(multiply(identity,
           multiply(identity,
             multiply(multiply(identity, multiply(X_14, X_14)),
               multiply(identity, multiply(X_14, X_14))))), X_14))),
    introduced(tautology,
               [refl,
                [$fot(multiply(X_1,
                        multiply(multiply(identity,
                            multiply(identity,
                              multiply(multiply(identity,
                                  multiply(X_14, X_14)),
                                multiply(identity,
                                  multiply(X_14, X_14))))), X_14)))]])).

cnf(refute_0_70, plain,
    (multiply(X_1,
       multiply(multiply(identity,
           multiply(identity,
             multiply(multiply(identity, multiply(X_14, X_14)),
               multiply(identity, multiply(X_14, X_14))))), X_14)) !=
     multiply(X_1,
       multiply(multiply(identity,
           multiply(identity,
             multiply(multiply(identity, multiply(X_14, X_14)),
               multiply(identity, multiply(X_14, X_14))))), X_14)) |
     multiply(multiply(identity,
         multiply(identity,
           multiply(multiply(identity, multiply(X_14, X_14)),
             multiply(identity, multiply(X_14, X_14))))), X_14) !=
     multiply(X_14, X_14) |
     multiply(X_1,
       multiply(multiply(identity,
           multiply(identity,
             multiply(multiply(identity, multiply(X_14, X_14)),
               multiply(identity, multiply(X_14, X_14))))), X_14)) =
     multiply(X_1, multiply(X_14, X_14))),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(X_1,
                          multiply(multiply(identity,
                              multiply(identity,
                                multiply(multiply(identity,
                                    multiply(X_14, X_14)),
                                  multiply(identity,
                                    multiply(X_14, X_14))))), X_14)),
                        multiply(X_1,
                          multiply(multiply(identity,
                              multiply(identity,
                                multiply(multiply(identity,
                                    multiply(X_14, X_14)),
                                  multiply(identity,
                                    multiply(X_14, X_14))))), X_14)))),
                 [1, 1], $fot(multiply(X_14, X_14))]])).

cnf(refute_0_71, plain,
    (multiply(multiply(identity,
         multiply(identity,
           multiply(multiply(identity, multiply(X_14, X_14)),
             multiply(identity, multiply(X_14, X_14))))), X_14) !=
     multiply(X_14, X_14) |
     multiply(X_1,
       multiply(multiply(identity,
           multiply(identity,
             multiply(multiply(identity, multiply(X_14, X_14)),
               multiply(identity, multiply(X_14, X_14))))), X_14)) =
     multiply(X_1, multiply(X_14, X_14))),
    inference(resolve,
              [$cnf($equal(multiply(X_1,
                        multiply(multiply(identity,
                            multiply(identity,
                              multiply(multiply(identity,
                                  multiply(X_14, X_14)),
                                multiply(identity,
                                  multiply(X_14, X_14))))), X_14)),
                      multiply(X_1,
                        multiply(multiply(identity,
                            multiply(identity,
                              multiply(multiply(identity,
                                  multiply(X_14, X_14)),
                                multiply(identity,
                                  multiply(X_14, X_14))))), X_14))))],
              [refute_0_69, refute_0_70])).

cnf(refute_0_72, plain,
    (multiply(X_1,
       multiply(multiply(identity,
           multiply(identity,
             multiply(multiply(identity, multiply(X_14, X_14)),
               multiply(identity, multiply(X_14, X_14))))), X_14)) =
     multiply(X_1, multiply(X_14, X_14))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(identity,
                          multiply(identity,
                            multiply(multiply(identity,
                                multiply(X_14, X_14)),
                              multiply(identity, multiply(X_14, X_14))))),
                        X_14), multiply(X_14, X_14)))],
              [refute_0_68, refute_0_71])).

cnf(refute_0_73, plain,
    (multiply(X,
       multiply(X_1,
         multiply(multiply(identity,
             multiply(identity,
               multiply(multiply(identity, multiply(X_14, X_14)),
                 multiply(identity, multiply(X_14, X_14))))), X_14))) =
     multiply(X,
       multiply(X_1,
         multiply(multiply(identity,
             multiply(identity,
               multiply(multiply(identity, multiply(X_14, X_14)),
                 multiply(identity, multiply(X_14, X_14))))), X_14)))),
    introduced(tautology,
               [refl,
                [$fot(multiply(X,
                        multiply(X_1,
                          multiply(multiply(identity,
                              multiply(identity,
                                multiply(multiply(identity,
                                    multiply(X_14, X_14)),
                                  multiply(identity,
                                    multiply(X_14, X_14))))), X_14))))]])).

cnf(refute_0_74, plain,
    (multiply(X,
       multiply(X_1,
         multiply(multiply(identity,
             multiply(identity,
               multiply(multiply(identity, multiply(X_14, X_14)),
                 multiply(identity, multiply(X_14, X_14))))), X_14))) !=
     multiply(X,
       multiply(X_1,
         multiply(multiply(identity,
             multiply(identity,
               multiply(multiply(identity, multiply(X_14, X_14)),
                 multiply(identity, multiply(X_14, X_14))))), X_14))) |
     multiply(X_1,
       multiply(multiply(identity,
           multiply(identity,
             multiply(multiply(identity, multiply(X_14, X_14)),
               multiply(identity, multiply(X_14, X_14))))), X_14)) !=
     multiply(X_1, multiply(X_14, X_14)) |
     multiply(X,
       multiply(X_1,
         multiply(multiply(identity,
             multiply(identity,
               multiply(multiply(identity, multiply(X_14, X_14)),
                 multiply(identity, multiply(X_14, X_14))))), X_14))) =
     multiply(X, multiply(X_1, multiply(X_14, X_14)))),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(X,
                          multiply(X_1,
                            multiply(multiply(identity,
                                multiply(identity,
                                  multiply(multiply(identity,
                                      multiply(X_14, X_14)),
                                    multiply(identity,
                                      multiply(X_14, X_14))))), X_14))),
                        multiply(X,
                          multiply(X_1,
                            multiply(multiply(identity,
                                multiply(identity,
                                  multiply(multiply(identity,
                                      multiply(X_14, X_14)),
                                    multiply(identity,
                                      multiply(X_14, X_14))))), X_14))))),
                 [1, 1], $fot(multiply(X_1, multiply(X_14, X_14)))]])).

cnf(refute_0_75, plain,
    (multiply(X_1,
       multiply(multiply(identity,
           multiply(identity,
             multiply(multiply(identity, multiply(X_14, X_14)),
               multiply(identity, multiply(X_14, X_14))))), X_14)) !=
     multiply(X_1, multiply(X_14, X_14)) |
     multiply(X,
       multiply(X_1,
         multiply(multiply(identity,
             multiply(identity,
               multiply(multiply(identity, multiply(X_14, X_14)),
                 multiply(identity, multiply(X_14, X_14))))), X_14))) =
     multiply(X, multiply(X_1, multiply(X_14, X_14)))),
    inference(resolve,
              [$cnf($equal(multiply(X,
                        multiply(X_1,
                          multiply(multiply(identity,
                              multiply(identity,
                                multiply(multiply(identity,
                                    multiply(X_14, X_14)),
                                  multiply(identity,
                                    multiply(X_14, X_14))))), X_14))),
                      multiply(X,
                        multiply(X_1,
                          multiply(multiply(identity,
                              multiply(identity,
                                multiply(multiply(identity,
                                    multiply(X_14, X_14)),
                                  multiply(identity,
                                    multiply(X_14, X_14))))), X_14)))))],
              [refute_0_73, refute_0_74])).

cnf(refute_0_76, plain,
    (multiply(X,
       multiply(X_1,
         multiply(multiply(identity,
             multiply(identity,
               multiply(multiply(identity, multiply(X_14, X_14)),
                 multiply(identity, multiply(X_14, X_14))))), X_14))) =
     multiply(X, multiply(X_1, multiply(X_14, X_14)))),
    inference(resolve,
              [$cnf($equal(multiply(X_1,
                        multiply(multiply(identity,
                            multiply(identity,
                              multiply(multiply(identity,
                                  multiply(X_14, X_14)),
                                multiply(identity,
                                  multiply(X_14, X_14))))), X_14)),
                      multiply(X_1, multiply(X_14, X_14))))],
              [refute_0_72, refute_0_75])).

cnf(refute_0_77, plain,
    (multiply(X,
       multiply(X_1,
         multiply(multiply(identity,
             multiply(identity,
               multiply(multiply(identity, multiply(X_14, X_14)),
                 multiply(identity, multiply(X_14, X_14))))), X_14))) !=
     multiply(X, multiply(X_1, multiply(X_14, X_14))) |
     multiply(X,
       multiply(X_1,
         multiply(multiply(identity,
             multiply(identity,
               multiply(multiply(identity, multiply(X_14, X_14)),
                 multiply(identity, multiply(X_14, X_14))))), X_14))) !=
     multiply(multiply(X, X_1), multiply(identity, multiply(X_14, X_14))) |
     multiply(X, multiply(X_1, multiply(X_14, X_14))) =
     multiply(multiply(X, X_1), multiply(identity, multiply(X_14, X_14)))),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(X,
                          multiply(X_1,
                            multiply(multiply(identity,
                                multiply(identity,
                                  multiply(multiply(identity,
                                      multiply(X_14, X_14)),
                                    multiply(identity,
                                      multiply(X_14, X_14))))), X_14))),
                        multiply(multiply(X, X_1),
                          multiply(identity, multiply(X_14, X_14))))), [0],
                 $fot(multiply(X,
                        multiply(X_1, multiply(X_14, X_14))))]])).

cnf(refute_0_78, plain,
    (multiply(X,
       multiply(X_1,
         multiply(multiply(identity,
             multiply(identity,
               multiply(multiply(identity, multiply(X_14, X_14)),
                 multiply(identity, multiply(X_14, X_14))))), X_14))) !=
     multiply(multiply(X, X_1), multiply(identity, multiply(X_14, X_14))) |
     multiply(X, multiply(X_1, multiply(X_14, X_14))) =
     multiply(multiply(X, X_1), multiply(identity, multiply(X_14, X_14)))),
    inference(resolve,
              [$cnf($equal(multiply(X,
                        multiply(X_1,
                          multiply(multiply(identity,
                              multiply(identity,
                                multiply(multiply(identity,
                                    multiply(X_14, X_14)),
                                  multiply(identity,
                                    multiply(X_14, X_14))))), X_14))),
                      multiply(X, multiply(X_1, multiply(X_14, X_14)))))],
              [refute_0_76, refute_0_77])).

cnf(refute_0_79, plain,
    (multiply(X, multiply(X_1, multiply(X_14, X_14))) =
     multiply(multiply(X, X_1), multiply(identity, multiply(X_14, X_14)))),
    inference(resolve,
              [$cnf($equal(multiply(X,
                        multiply(X_1,
                          multiply(multiply(identity,
                              multiply(identity,
                                multiply(multiply(identity,
                                    multiply(X_14, X_14)),
                                  multiply(identity,
                                    multiply(X_14, X_14))))), X_14))),
                      multiply(multiply(X, X_1),
                        multiply(identity, multiply(X_14, X_14)))))],
              [refute_0_63, refute_0_78])).

cnf(refute_0_80, plain,
    (multiply(X, multiply(X_1, multiply(X_14, X_14))) !=
     multiply(multiply(X, X_1), multiply(identity, multiply(X_14, X_14))) |
     multiply(multiply(X, X_1), multiply(identity, multiply(X_14, X_14))) =
     multiply(X, multiply(X_1, multiply(X_14, X_14)))),
    inference(subst, [],
              [refute_0_7 :
               [bind(X0,
                     $fot(multiply(X,
                            multiply(X_1, multiply(X_14, X_14))))),
                bind(Y0,
                     $fot(multiply(multiply(X, X_1),
                            multiply(identity,
                              multiply(X_14, X_14)))))]])).

cnf(refute_0_81, plain,
    (multiply(multiply(X, X_1), multiply(identity, multiply(X_14, X_14))) =
     multiply(X, multiply(X_1, multiply(X_14, X_14)))),
    inference(resolve,
              [$cnf($equal(multiply(X,
                        multiply(X_1, multiply(X_14, X_14))),
                      multiply(multiply(X, X_1),
                        multiply(identity, multiply(X_14, X_14)))))],
              [refute_0_79, refute_0_80])).

cnf(refute_0_82, plain,
    (multiply(multiply(X_4, multiply(X_10, X_11)),
       multiply(identity, multiply(X_11, X_11))) =
     multiply(X_4, multiply(multiply(X_10, X_11), multiply(X_11, X_11)))),
    inference(subst, [],
              [refute_0_81 :
               [bind(X, $fot(X_4)), bind(X_1, $fot(multiply(X_10, X_11))),
                bind(X_14, $fot(X_11))]])).

cnf(refute_0_83, plain,
    (multiply(multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(multiply(X_4, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11)))) =
     multiply(multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(multiply(X_4, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11))))),
    introduced(tautology,
               [refl,
                [$fot(multiply(multiply(X_4,
                          multiply(multiply(X_10, X_11),
                            multiply(X_11, X_11))),
                        multiply(multiply(X_4, multiply(X_10, X_11)),
                          multiply(identity, multiply(X_11, X_11)))))]])).

cnf(refute_0_84, plain,
    (multiply(multiply(X_4, multiply(X_10, X_11)),
       multiply(identity, multiply(X_11, X_11))) !=
     multiply(X_4, multiply(multiply(X_10, X_11), multiply(X_11, X_11))) |
     multiply(multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(multiply(X_4, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11)))) !=
     multiply(multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(multiply(X_4, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11)))) |
     multiply(multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(multiply(X_4, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11)))) =
     multiply(multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))))),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(multiply(X_4,
                            multiply(multiply(X_10, X_11),
                              multiply(X_11, X_11))),
                          multiply(multiply(X_4, multiply(X_10, X_11)),
                            multiply(identity, multiply(X_11, X_11)))),
                        multiply(multiply(X_4,
                            multiply(multiply(X_10, X_11),
                              multiply(X_11, X_11))),
                          multiply(multiply(X_4, multiply(X_10, X_11)),
                            multiply(identity, multiply(X_11, X_11)))))),
                 [1, 1],
                 $fot(multiply(X_4,
                        multiply(multiply(X_10, X_11),
                          multiply(X_11, X_11))))]])).

cnf(refute_0_85, plain,
    (multiply(multiply(X_4, multiply(X_10, X_11)),
       multiply(identity, multiply(X_11, X_11))) !=
     multiply(X_4, multiply(multiply(X_10, X_11), multiply(X_11, X_11))) |
     multiply(multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(multiply(X_4, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11)))) =
     multiply(multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(X_4,
                          multiply(multiply(X_10, X_11),
                            multiply(X_11, X_11))),
                        multiply(multiply(X_4, multiply(X_10, X_11)),
                          multiply(identity, multiply(X_11, X_11)))),
                      multiply(multiply(X_4,
                          multiply(multiply(X_10, X_11),
                            multiply(X_11, X_11))),
                        multiply(multiply(X_4, multiply(X_10, X_11)),
                          multiply(identity, multiply(X_11, X_11))))))],
              [refute_0_83, refute_0_84])).

cnf(refute_0_86, plain,
    (multiply(multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(multiply(X_4, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11)))) =
     multiply(multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(X_4, multiply(X_10, X_11)),
                        multiply(identity, multiply(X_11, X_11))),
                      multiply(X_4,
                        multiply(multiply(X_10, X_11),
                          multiply(X_11, X_11)))))],
              [refute_0_82, refute_0_85])).

cnf(refute_0_87, plain,
    (multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11))),
       multiply(multiply(X_4, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11)))) =
     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11))),
       multiply(multiply(X_4, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11))))),
    introduced(tautology,
               [refl,
                [$fot(multiply(multiply(multiply(X_4,
                            multiply(X_10, X_11)),
                          multiply(identity, multiply(X_11, X_11))),
                        multiply(multiply(X_4, multiply(X_10, X_11)),
                          multiply(identity, multiply(X_11, X_11)))))]])).

cnf(refute_0_88, plain,
    (multiply(multiply(X_4, multiply(X_10, X_11)),
       multiply(identity, multiply(X_11, X_11))) !=
     multiply(X_4, multiply(multiply(X_10, X_11), multiply(X_11, X_11))) |
     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11))),
       multiply(multiply(X_4, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11)))) !=
     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11))),
       multiply(multiply(X_4, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11)))) |
     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11))),
       multiply(multiply(X_4, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11)))) =
     multiply(multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(multiply(X_4, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11))))),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(multiply(multiply(X_4,
                              multiply(X_10, X_11)),
                            multiply(identity, multiply(X_11, X_11))),
                          multiply(multiply(X_4, multiply(X_10, X_11)),
                            multiply(identity, multiply(X_11, X_11)))),
                        multiply(multiply(multiply(X_4,
                              multiply(X_10, X_11)),
                            multiply(identity, multiply(X_11, X_11))),
                          multiply(multiply(X_4, multiply(X_10, X_11)),
                            multiply(identity, multiply(X_11, X_11)))))),
                 [1, 0],
                 $fot(multiply(X_4,
                        multiply(multiply(X_10, X_11),
                          multiply(X_11, X_11))))]])).

cnf(refute_0_89, plain,
    (multiply(multiply(X_4, multiply(X_10, X_11)),
       multiply(identity, multiply(X_11, X_11))) !=
     multiply(X_4, multiply(multiply(X_10, X_11), multiply(X_11, X_11))) |
     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11))),
       multiply(multiply(X_4, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11)))) =
     multiply(multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(multiply(X_4, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11))))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(multiply(X_4,
                            multiply(X_10, X_11)),
                          multiply(identity, multiply(X_11, X_11))),
                        multiply(multiply(X_4, multiply(X_10, X_11)),
                          multiply(identity, multiply(X_11, X_11)))),
                      multiply(multiply(multiply(X_4,
                            multiply(X_10, X_11)),
                          multiply(identity, multiply(X_11, X_11))),
                        multiply(multiply(X_4, multiply(X_10, X_11)),
                          multiply(identity, multiply(X_11, X_11))))))],
              [refute_0_87, refute_0_88])).

cnf(refute_0_90, plain,
    (multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11))),
       multiply(multiply(X_4, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11)))) =
     multiply(multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(multiply(X_4, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11))))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(X_4, multiply(X_10, X_11)),
                        multiply(identity, multiply(X_11, X_11))),
                      multiply(X_4,
                        multiply(multiply(X_10, X_11),
                          multiply(X_11, X_11)))))],
              [refute_0_82, refute_0_89])).

cnf(refute_0_91, plain, (Y0 != X0 | Y0 != Z0 | X0 = Z0),
    introduced(tautology,
               [equality, [$cnf($equal(Y0, Z0)), [0], $fot(X0)]])).

cnf(refute_0_92, plain, (X0 != Y0 | Y0 != Z0 | X0 = Z0),
    inference(resolve, [$cnf($equal(Y0, X0))], [refute_0_7, refute_0_91])).

cnf(refute_0_93, plain,
    (multiply(multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(multiply(X_4, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11)))) !=
     multiply(multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11)))) |
     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11))),
       multiply(multiply(X_4, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11)))) !=
     multiply(multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(multiply(X_4, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11)))) |
     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11))),
       multiply(multiply(X_4, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11)))) =
     multiply(multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))))),
    inference(subst, [],
              [refute_0_92 :
               [bind(X0,
                     $fot(multiply(multiply(multiply(X_4,
                                multiply(X_10, X_11)),
                              multiply(identity, multiply(X_11, X_11))),
                            multiply(multiply(X_4, multiply(X_10, X_11)),
                              multiply(identity, multiply(X_11, X_11)))))),
                bind(Y0,
                     $fot(multiply(multiply(X_4,
                              multiply(multiply(X_10, X_11),
                                multiply(X_11, X_11))),
                            multiply(multiply(X_4, multiply(X_10, X_11)),
                              multiply(identity, multiply(X_11, X_11)))))),
                bind(Z0,
                     $fot(multiply(multiply(X_4,
                              multiply(multiply(X_10, X_11),
                                multiply(X_11, X_11))),
                            multiply(X_4,
                              multiply(multiply(X_10, X_11),
                                multiply(X_11, X_11))))))]])).

cnf(refute_0_94, plain,
    (multiply(multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(multiply(X_4, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11)))) !=
     multiply(multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11)))) |
     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11))),
       multiply(multiply(X_4, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11)))) =
     multiply(multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(multiply(X_4,
                            multiply(X_10, X_11)),
                          multiply(identity, multiply(X_11, X_11))),
                        multiply(multiply(X_4, multiply(X_10, X_11)),
                          multiply(identity, multiply(X_11, X_11)))),
                      multiply(multiply(X_4,
                          multiply(multiply(X_10, X_11),
                            multiply(X_11, X_11))),
                        multiply(multiply(X_4, multiply(X_10, X_11)),
                          multiply(identity, multiply(X_11, X_11))))))],
              [refute_0_90, refute_0_93])).

cnf(refute_0_95, plain,
    (multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11))),
       multiply(multiply(X_4, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11)))) =
     multiply(multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(X_4,
                          multiply(multiply(X_10, X_11),
                            multiply(X_11, X_11))),
                        multiply(multiply(X_4, multiply(X_10, X_11)),
                          multiply(identity, multiply(X_11, X_11)))),
                      multiply(multiply(X_4,
                          multiply(multiply(X_10, X_11),
                            multiply(X_11, X_11))),
                        multiply(X_4,
                          multiply(multiply(X_10, X_11),
                            multiply(X_11, X_11))))))],
              [refute_0_86, refute_0_94])).

cnf(refute_0_96, plain,
    (multiply(identity,
       multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
           multiply(identity, multiply(X_11, X_11))),
         multiply(multiply(X_4, multiply(X_10, X_11)),
           multiply(identity, multiply(X_11, X_11))))) =
     multiply(identity,
       multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
           multiply(identity, multiply(X_11, X_11))),
         multiply(multiply(X_4, multiply(X_10, X_11)),
           multiply(identity, multiply(X_11, X_11)))))),
    introduced(tautology,
               [refl,
                [$fot(multiply(identity,
                        multiply(multiply(multiply(X_4,
                              multiply(X_10, X_11)),
                            multiply(identity, multiply(X_11, X_11))),
                          multiply(multiply(X_4, multiply(X_10, X_11)),
                            multiply(identity,
                              multiply(X_11, X_11))))))]])).

cnf(refute_0_97, plain,
    (multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11))),
       multiply(multiply(X_4, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11)))) !=
     multiply(multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11)))) |
     multiply(identity,
       multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
           multiply(identity, multiply(X_11, X_11))),
         multiply(multiply(X_4, multiply(X_10, X_11)),
           multiply(identity, multiply(X_11, X_11))))) !=
     multiply(identity,
       multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
           multiply(identity, multiply(X_11, X_11))),
         multiply(multiply(X_4, multiply(X_10, X_11)),
           multiply(identity, multiply(X_11, X_11))))) |
     multiply(identity,
       multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
           multiply(identity, multiply(X_11, X_11))),
         multiply(multiply(X_4, multiply(X_10, X_11)),
           multiply(identity, multiply(X_11, X_11))))) =
     multiply(identity,
       multiply(multiply(X_4,
           multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
         multiply(X_4,
           multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(identity,
                          multiply(multiply(multiply(X_4,
                                multiply(X_10, X_11)),
                              multiply(identity, multiply(X_11, X_11))),
                            multiply(multiply(X_4, multiply(X_10, X_11)),
                              multiply(identity, multiply(X_11, X_11))))),
                        multiply(identity,
                          multiply(multiply(multiply(X_4,
                                multiply(X_10, X_11)),
                              multiply(identity, multiply(X_11, X_11))),
                            multiply(multiply(X_4, multiply(X_10, X_11)),
                              multiply(identity,
                                multiply(X_11, X_11))))))), [1, 1],
                 $fot(multiply(multiply(X_4,
                          multiply(multiply(X_10, X_11),
                            multiply(X_11, X_11))),
                        multiply(X_4,
                          multiply(multiply(X_10, X_11),
                            multiply(X_11, X_11)))))]])).

cnf(refute_0_98, plain,
    (multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11))),
       multiply(multiply(X_4, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11)))) !=
     multiply(multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11)))) |
     multiply(identity,
       multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
           multiply(identity, multiply(X_11, X_11))),
         multiply(multiply(X_4, multiply(X_10, X_11)),
           multiply(identity, multiply(X_11, X_11))))) =
     multiply(identity,
       multiply(multiply(X_4,
           multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
         multiply(X_4,
           multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
    inference(resolve,
              [$cnf($equal(multiply(identity,
                        multiply(multiply(multiply(X_4,
                              multiply(X_10, X_11)),
                            multiply(identity, multiply(X_11, X_11))),
                          multiply(multiply(X_4, multiply(X_10, X_11)),
                            multiply(identity, multiply(X_11, X_11))))),
                      multiply(identity,
                        multiply(multiply(multiply(X_4,
                              multiply(X_10, X_11)),
                            multiply(identity, multiply(X_11, X_11))),
                          multiply(multiply(X_4, multiply(X_10, X_11)),
                            multiply(identity, multiply(X_11, X_11)))))))],
              [refute_0_96, refute_0_97])).

cnf(refute_0_99, plain,
    (multiply(identity,
       multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
           multiply(identity, multiply(X_11, X_11))),
         multiply(multiply(X_4, multiply(X_10, X_11)),
           multiply(identity, multiply(X_11, X_11))))) =
     multiply(identity,
       multiply(multiply(X_4,
           multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
         multiply(X_4,
           multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(multiply(X_4,
                            multiply(X_10, X_11)),
                          multiply(identity, multiply(X_11, X_11))),
                        multiply(multiply(X_4, multiply(X_10, X_11)),
                          multiply(identity, multiply(X_11, X_11)))),
                      multiply(multiply(X_4,
                          multiply(multiply(X_10, X_11),
                            multiply(X_11, X_11))),
                        multiply(X_4,
                          multiply(multiply(X_10, X_11),
                            multiply(X_11, X_11))))))],
              [refute_0_95, refute_0_98])).

cnf(refute_0_100, plain,
    (multiply(X_10,
       multiply(identity,
         multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
             multiply(identity, multiply(X_11, X_11))),
           multiply(multiply(X_4, multiply(X_10, X_11)),
             multiply(identity, multiply(X_11, X_11)))))) =
     multiply(X_10,
       multiply(identity,
         multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
             multiply(identity, multiply(X_11, X_11))),
           multiply(multiply(X_4, multiply(X_10, X_11)),
             multiply(identity, multiply(X_11, X_11))))))),
    introduced(tautology,
               [refl,
                [$fot(multiply(X_10,
                        multiply(identity,
                          multiply(multiply(multiply(X_4,
                                multiply(X_10, X_11)),
                              multiply(identity, multiply(X_11, X_11))),
                            multiply(multiply(X_4, multiply(X_10, X_11)),
                              multiply(identity,
                                multiply(X_11, X_11)))))))]])).

cnf(refute_0_101, plain,
    (multiply(X_10,
       multiply(identity,
         multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
             multiply(identity, multiply(X_11, X_11))),
           multiply(multiply(X_4, multiply(X_10, X_11)),
             multiply(identity, multiply(X_11, X_11)))))) !=
     multiply(X_10,
       multiply(identity,
         multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
             multiply(identity, multiply(X_11, X_11))),
           multiply(multiply(X_4, multiply(X_10, X_11)),
             multiply(identity, multiply(X_11, X_11)))))) |
     multiply(identity,
       multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
           multiply(identity, multiply(X_11, X_11))),
         multiply(multiply(X_4, multiply(X_10, X_11)),
           multiply(identity, multiply(X_11, X_11))))) !=
     multiply(identity,
       multiply(multiply(X_4,
           multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
         multiply(X_4,
           multiply(multiply(X_10, X_11), multiply(X_11, X_11))))) |
     multiply(X_10,
       multiply(identity,
         multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
             multiply(identity, multiply(X_11, X_11))),
           multiply(multiply(X_4, multiply(X_10, X_11)),
             multiply(identity, multiply(X_11, X_11)))))) =
     multiply(X_10,
       multiply(identity,
         multiply(multiply(X_4,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
           multiply(X_4,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11))))))),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(X_10,
                          multiply(identity,
                            multiply(multiply(multiply(X_4,
                                  multiply(X_10, X_11)),
                                multiply(identity, multiply(X_11, X_11))),
                              multiply(multiply(X_4, multiply(X_10, X_11)),
                                multiply(identity,
                                  multiply(X_11, X_11)))))),
                        multiply(X_10,
                          multiply(identity,
                            multiply(multiply(multiply(X_4,
                                  multiply(X_10, X_11)),
                                multiply(identity, multiply(X_11, X_11))),
                              multiply(multiply(X_4, multiply(X_10, X_11)),
                                multiply(identity,
                                  multiply(X_11, X_11)))))))), [1, 1],
                 $fot(multiply(identity,
                        multiply(multiply(X_4,
                            multiply(multiply(X_10, X_11),
                              multiply(X_11, X_11))),
                          multiply(X_4,
                            multiply(multiply(X_10, X_11),
                              multiply(X_11, X_11))))))]])).

cnf(refute_0_102, plain,
    (multiply(identity,
       multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
           multiply(identity, multiply(X_11, X_11))),
         multiply(multiply(X_4, multiply(X_10, X_11)),
           multiply(identity, multiply(X_11, X_11))))) !=
     multiply(identity,
       multiply(multiply(X_4,
           multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
         multiply(X_4,
           multiply(multiply(X_10, X_11), multiply(X_11, X_11))))) |
     multiply(X_10,
       multiply(identity,
         multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
             multiply(identity, multiply(X_11, X_11))),
           multiply(multiply(X_4, multiply(X_10, X_11)),
             multiply(identity, multiply(X_11, X_11)))))) =
     multiply(X_10,
       multiply(identity,
         multiply(multiply(X_4,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
           multiply(X_4,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11))))))),
    inference(resolve,
              [$cnf($equal(multiply(X_10,
                        multiply(identity,
                          multiply(multiply(multiply(X_4,
                                multiply(X_10, X_11)),
                              multiply(identity, multiply(X_11, X_11))),
                            multiply(multiply(X_4, multiply(X_10, X_11)),
                              multiply(identity, multiply(X_11, X_11)))))),
                      multiply(X_10,
                        multiply(identity,
                          multiply(multiply(multiply(X_4,
                                multiply(X_10, X_11)),
                              multiply(identity, multiply(X_11, X_11))),
                            multiply(multiply(X_4, multiply(X_10, X_11)),
                              multiply(identity,
                                multiply(X_11, X_11))))))))],
              [refute_0_100, refute_0_101])).

cnf(refute_0_103, plain,
    (multiply(X_10,
       multiply(identity,
         multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
             multiply(identity, multiply(X_11, X_11))),
           multiply(multiply(X_4, multiply(X_10, X_11)),
             multiply(identity, multiply(X_11, X_11)))))) =
     multiply(X_10,
       multiply(identity,
         multiply(multiply(X_4,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
           multiply(X_4,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11))))))),
    inference(resolve,
              [$cnf($equal(multiply(identity,
                        multiply(multiply(multiply(X_4,
                              multiply(X_10, X_11)),
                            multiply(identity, multiply(X_11, X_11))),
                          multiply(multiply(X_4, multiply(X_10, X_11)),
                            multiply(identity, multiply(X_11, X_11))))),
                      multiply(identity,
                        multiply(multiply(X_4,
                            multiply(multiply(X_10, X_11),
                              multiply(X_11, X_11))),
                          multiply(X_4,
                            multiply(multiply(X_10, X_11),
                              multiply(X_11, X_11)))))))],
              [refute_0_99, refute_0_102])).

cnf(refute_0_104, plain,
    (multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))))))) =
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11)))))))),
    introduced(tautology,
               [refl,
                [$fot(multiply(multiply(X_10,
                          multiply(identity,
                            multiply(multiply(X_4,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))),
                              multiply(X_4,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11)))))),
                        multiply(X_10,
                          multiply(identity,
                            multiply(multiply(multiply(X_4,
                                  multiply(X_10, X_11)),
                                multiply(identity, multiply(X_11, X_11))),
                              multiply(multiply(X_4, multiply(X_10, X_11)),
                                multiply(identity,
                                  multiply(X_11, X_11))))))))]])).

cnf(refute_0_105, plain,
    (multiply(X_10,
       multiply(identity,
         multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
             multiply(identity, multiply(X_11, X_11))),
           multiply(multiply(X_4, multiply(X_10, X_11)),
             multiply(identity, multiply(X_11, X_11)))))) !=
     multiply(X_10,
       multiply(identity,
         multiply(multiply(X_4,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
           multiply(X_4,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))) |
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))))))) !=
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))))))) |
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))))))) =
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))))),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(multiply(X_10,
                            multiply(identity,
                              multiply(multiply(X_4,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11))),
                                multiply(X_4,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11)))))),
                          multiply(X_10,
                            multiply(identity,
                              multiply(multiply(multiply(X_4,
                                    multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11))),
                                multiply(multiply(X_4,
                                    multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11))))))),
                        multiply(multiply(X_10,
                            multiply(identity,
                              multiply(multiply(X_4,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11))),
                                multiply(X_4,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11)))))),
                          multiply(X_10,
                            multiply(identity,
                              multiply(multiply(multiply(X_4,
                                    multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11))),
                                multiply(multiply(X_4,
                                    multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11))))))))), [1, 1],
                 $fot(multiply(X_10,
                        multiply(identity,
                          multiply(multiply(X_4,
                              multiply(multiply(X_10, X_11),
                                multiply(X_11, X_11))),
                            multiply(X_4,
                              multiply(multiply(X_10, X_11),
                                multiply(X_11, X_11)))))))]])).

cnf(refute_0_106, plain,
    (multiply(X_10,
       multiply(identity,
         multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
             multiply(identity, multiply(X_11, X_11))),
           multiply(multiply(X_4, multiply(X_10, X_11)),
             multiply(identity, multiply(X_11, X_11)))))) !=
     multiply(X_10,
       multiply(identity,
         multiply(multiply(X_4,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
           multiply(X_4,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))) |
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))))))) =
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(X_10,
                          multiply(identity,
                            multiply(multiply(X_4,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))),
                              multiply(X_4,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11)))))),
                        multiply(X_10,
                          multiply(identity,
                            multiply(multiply(multiply(X_4,
                                  multiply(X_10, X_11)),
                                multiply(identity, multiply(X_11, X_11))),
                              multiply(multiply(X_4, multiply(X_10, X_11)),
                                multiply(identity,
                                  multiply(X_11, X_11))))))),
                      multiply(multiply(X_10,
                          multiply(identity,
                            multiply(multiply(X_4,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))),
                              multiply(X_4,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11)))))),
                        multiply(X_10,
                          multiply(identity,
                            multiply(multiply(multiply(X_4,
                                  multiply(X_10, X_11)),
                                multiply(identity, multiply(X_11, X_11))),
                              multiply(multiply(X_4, multiply(X_10, X_11)),
                                multiply(identity,
                                  multiply(X_11, X_11)))))))))],
              [refute_0_104, refute_0_105])).

cnf(refute_0_107, plain,
    (multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))))))) =
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))))),
    inference(resolve,
              [$cnf($equal(multiply(X_10,
                        multiply(identity,
                          multiply(multiply(multiply(X_4,
                                multiply(X_10, X_11)),
                              multiply(identity, multiply(X_11, X_11))),
                            multiply(multiply(X_4, multiply(X_10, X_11)),
                              multiply(identity, multiply(X_11, X_11)))))),
                      multiply(X_10,
                        multiply(identity,
                          multiply(multiply(X_4,
                              multiply(multiply(X_10, X_11),
                                multiply(X_11, X_11))),
                            multiply(X_4,
                              multiply(multiply(X_10, X_11),
                                multiply(X_11, X_11))))))))],
              [refute_0_103, refute_0_106])).

cnf(refute_0_108, plain,
    (multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))))))) =
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11)))))))),
    introduced(tautology,
               [refl,
                [$fot(multiply(multiply(X_10,
                          multiply(identity,
                            multiply(multiply(multiply(X_4,
                                  multiply(X_10, X_11)),
                                multiply(identity, multiply(X_11, X_11))),
                              multiply(multiply(X_4, multiply(X_10, X_11)),
                                multiply(identity,
                                  multiply(X_11, X_11)))))),
                        multiply(X_10,
                          multiply(identity,
                            multiply(multiply(multiply(X_4,
                                  multiply(X_10, X_11)),
                                multiply(identity, multiply(X_11, X_11))),
                              multiply(multiply(X_4, multiply(X_10, X_11)),
                                multiply(identity,
                                  multiply(X_11, X_11))))))))]])).

cnf(refute_0_109, plain,
    (multiply(X_10,
       multiply(identity,
         multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
             multiply(identity, multiply(X_11, X_11))),
           multiply(multiply(X_4, multiply(X_10, X_11)),
             multiply(identity, multiply(X_11, X_11)))))) !=
     multiply(X_10,
       multiply(identity,
         multiply(multiply(X_4,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
           multiply(X_4,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))) |
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))))))) !=
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))))))) |
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))))))) =
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11)))))))),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(multiply(X_10,
                            multiply(identity,
                              multiply(multiply(multiply(X_4,
                                    multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11))),
                                multiply(multiply(X_4,
                                    multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11)))))),
                          multiply(X_10,
                            multiply(identity,
                              multiply(multiply(multiply(X_4,
                                    multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11))),
                                multiply(multiply(X_4,
                                    multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11))))))),
                        multiply(multiply(X_10,
                            multiply(identity,
                              multiply(multiply(multiply(X_4,
                                    multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11))),
                                multiply(multiply(X_4,
                                    multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11)))))),
                          multiply(X_10,
                            multiply(identity,
                              multiply(multiply(multiply(X_4,
                                    multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11))),
                                multiply(multiply(X_4,
                                    multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11))))))))), [1, 0],
                 $fot(multiply(X_10,
                        multiply(identity,
                          multiply(multiply(X_4,
                              multiply(multiply(X_10, X_11),
                                multiply(X_11, X_11))),
                            multiply(X_4,
                              multiply(multiply(X_10, X_11),
                                multiply(X_11, X_11)))))))]])).

cnf(refute_0_110, plain,
    (multiply(X_10,
       multiply(identity,
         multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
             multiply(identity, multiply(X_11, X_11))),
           multiply(multiply(X_4, multiply(X_10, X_11)),
             multiply(identity, multiply(X_11, X_11)))))) !=
     multiply(X_10,
       multiply(identity,
         multiply(multiply(X_4,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
           multiply(X_4,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))) |
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))))))) =
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11)))))))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(X_10,
                          multiply(identity,
                            multiply(multiply(multiply(X_4,
                                  multiply(X_10, X_11)),
                                multiply(identity, multiply(X_11, X_11))),
                              multiply(multiply(X_4, multiply(X_10, X_11)),
                                multiply(identity,
                                  multiply(X_11, X_11)))))),
                        multiply(X_10,
                          multiply(identity,
                            multiply(multiply(multiply(X_4,
                                  multiply(X_10, X_11)),
                                multiply(identity, multiply(X_11, X_11))),
                              multiply(multiply(X_4, multiply(X_10, X_11)),
                                multiply(identity,
                                  multiply(X_11, X_11))))))),
                      multiply(multiply(X_10,
                          multiply(identity,
                            multiply(multiply(multiply(X_4,
                                  multiply(X_10, X_11)),
                                multiply(identity, multiply(X_11, X_11))),
                              multiply(multiply(X_4, multiply(X_10, X_11)),
                                multiply(identity,
                                  multiply(X_11, X_11)))))),
                        multiply(X_10,
                          multiply(identity,
                            multiply(multiply(multiply(X_4,
                                  multiply(X_10, X_11)),
                                multiply(identity, multiply(X_11, X_11))),
                              multiply(multiply(X_4, multiply(X_10, X_11)),
                                multiply(identity,
                                  multiply(X_11, X_11)))))))))],
              [refute_0_108, refute_0_109])).

cnf(refute_0_111, plain,
    (multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))))))) =
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11)))))))),
    inference(resolve,
              [$cnf($equal(multiply(X_10,
                        multiply(identity,
                          multiply(multiply(multiply(X_4,
                                multiply(X_10, X_11)),
                              multiply(identity, multiply(X_11, X_11))),
                            multiply(multiply(X_4, multiply(X_10, X_11)),
                              multiply(identity, multiply(X_11, X_11)))))),
                      multiply(X_10,
                        multiply(identity,
                          multiply(multiply(X_4,
                              multiply(multiply(X_10, X_11),
                                multiply(X_11, X_11))),
                            multiply(X_4,
                              multiply(multiply(X_10, X_11),
                                multiply(X_11, X_11))))))))],
              [refute_0_103, refute_0_110])).

cnf(refute_0_112, plain,
    (multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))))))) !=
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))))))) |
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))))))) !=
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))))))) |
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))))))) =
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))))),
    inference(subst, [],
              [refute_0_92 :
               [bind(X0,
                     $fot(multiply(multiply(X_10,
                              multiply(identity,
                                multiply(multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11))),
                                  multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11)))))),
                            multiply(X_10,
                              multiply(identity,
                                multiply(multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11))),
                                  multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11))))))))),
                bind(Y0,
                     $fot(multiply(multiply(X_10,
                              multiply(identity,
                                multiply(multiply(X_4,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11))),
                                  multiply(X_4,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11)))))),
                            multiply(X_10,
                              multiply(identity,
                                multiply(multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11))),
                                  multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11))))))))),
                bind(Z0,
                     $fot(multiply(multiply(X_10,
                              multiply(identity,
                                multiply(multiply(X_4,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11))),
                                  multiply(X_4,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11)))))),
                            multiply(X_10,
                              multiply(identity,
                                multiply(multiply(X_4,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11))),
                                  multiply(X_4,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11)))))))))]])).

cnf(refute_0_113, plain,
    (multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))))))) !=
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))))))) |
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))))))) =
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(X_10,
                          multiply(identity,
                            multiply(multiply(multiply(X_4,
                                  multiply(X_10, X_11)),
                                multiply(identity, multiply(X_11, X_11))),
                              multiply(multiply(X_4, multiply(X_10, X_11)),
                                multiply(identity,
                                  multiply(X_11, X_11)))))),
                        multiply(X_10,
                          multiply(identity,
                            multiply(multiply(multiply(X_4,
                                  multiply(X_10, X_11)),
                                multiply(identity, multiply(X_11, X_11))),
                              multiply(multiply(X_4, multiply(X_10, X_11)),
                                multiply(identity,
                                  multiply(X_11, X_11))))))),
                      multiply(multiply(X_10,
                          multiply(identity,
                            multiply(multiply(X_4,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))),
                              multiply(X_4,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11)))))),
                        multiply(X_10,
                          multiply(identity,
                            multiply(multiply(multiply(X_4,
                                  multiply(X_10, X_11)),
                                multiply(identity, multiply(X_11, X_11))),
                              multiply(multiply(X_4, multiply(X_10, X_11)),
                                multiply(identity,
                                  multiply(X_11, X_11)))))))))],
              [refute_0_111, refute_0_112])).

cnf(refute_0_114, plain,
    (multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))))))) =
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(X_10,
                          multiply(identity,
                            multiply(multiply(X_4,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))),
                              multiply(X_4,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11)))))),
                        multiply(X_10,
                          multiply(identity,
                            multiply(multiply(multiply(X_4,
                                  multiply(X_10, X_11)),
                                multiply(identity, multiply(X_11, X_11))),
                              multiply(multiply(X_4, multiply(X_10, X_11)),
                                multiply(identity,
                                  multiply(X_11, X_11))))))),
                      multiply(multiply(X_10,
                          multiply(identity,
                            multiply(multiply(X_4,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))),
                              multiply(X_4,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11)))))),
                        multiply(X_10,
                          multiply(identity,
                            multiply(multiply(X_4,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))),
                              multiply(X_4,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11)))))))))],
              [refute_0_107, refute_0_113])).

cnf(refute_0_115, plain,
    (multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11))),
               multiply(multiply(X_4, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11)))))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11))),
               multiply(multiply(X_4, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11)))))))) =
     multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11))),
               multiply(multiply(X_4, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11)))))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11))),
               multiply(multiply(X_4, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11))))))))),
    introduced(tautology,
               [refl,
                [$fot(multiply(identity,
                        multiply(multiply(X_10,
                            multiply(identity,
                              multiply(multiply(multiply(X_4,
                                    multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11))),
                                multiply(multiply(X_4,
                                    multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11)))))),
                          multiply(X_10,
                            multiply(identity,
                              multiply(multiply(multiply(X_4,
                                    multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11))),
                                multiply(multiply(X_4,
                                    multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11)))))))))]])).

cnf(refute_0_116, plain,
    (multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))))))) !=
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))))))) |
     multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11))),
               multiply(multiply(X_4, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11)))))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11))),
               multiply(multiply(X_4, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11)))))))) !=
     multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11))),
               multiply(multiply(X_4, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11)))))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11))),
               multiply(multiply(X_4, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11)))))))) |
     multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11))),
               multiply(multiply(X_4, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11)))))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11))),
               multiply(multiply(X_4, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11)))))))) =
     multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
               multiply(X_4,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
               multiply(X_4,
                 multiply(multiply(X_10, X_11),
                   multiply(X_11, X_11))))))))),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(identity,
                          multiply(multiply(X_10,
                              multiply(identity,
                                multiply(multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11))),
                                  multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11)))))),
                            multiply(X_10,
                              multiply(identity,
                                multiply(multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11))),
                                  multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11)))))))),
                        multiply(identity,
                          multiply(multiply(X_10,
                              multiply(identity,
                                multiply(multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11))),
                                  multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11)))))),
                            multiply(X_10,
                              multiply(identity,
                                multiply(multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11))),
                                  multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11)))))))))),
                 [1, 1],
                 $fot(multiply(multiply(X_10,
                          multiply(identity,
                            multiply(multiply(X_4,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))),
                              multiply(X_4,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11)))))),
                        multiply(X_10,
                          multiply(identity,
                            multiply(multiply(X_4,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))),
                              multiply(X_4,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))))))))]])).

cnf(refute_0_117, plain,
    (multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(X_4, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))))))) !=
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))))))) |
     multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11))),
               multiply(multiply(X_4, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11)))))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11))),
               multiply(multiply(X_4, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11)))))))) =
     multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
               multiply(X_4,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
               multiply(X_4,
                 multiply(multiply(X_10, X_11),
                   multiply(X_11, X_11))))))))),
    inference(resolve,
              [$cnf($equal(multiply(identity,
                        multiply(multiply(X_10,
                            multiply(identity,
                              multiply(multiply(multiply(X_4,
                                    multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11))),
                                multiply(multiply(X_4,
                                    multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11)))))),
                          multiply(X_10,
                            multiply(identity,
                              multiply(multiply(multiply(X_4,
                                    multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11))),
                                multiply(multiply(X_4,
                                    multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11)))))))),
                      multiply(identity,
                        multiply(multiply(X_10,
                            multiply(identity,
                              multiply(multiply(multiply(X_4,
                                    multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11))),
                                multiply(multiply(X_4,
                                    multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11)))))),
                          multiply(X_10,
                            multiply(identity,
                              multiply(multiply(multiply(X_4,
                                    multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11))),
                                multiply(multiply(X_4,
                                    multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11))))))))))],
              [refute_0_115, refute_0_116])).

cnf(refute_0_118, plain,
    (multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11))),
               multiply(multiply(X_4, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11)))))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11))),
               multiply(multiply(X_4, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11)))))))) =
     multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
               multiply(X_4,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
               multiply(X_4,
                 multiply(multiply(X_10, X_11),
                   multiply(X_11, X_11))))))))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(X_10,
                          multiply(identity,
                            multiply(multiply(multiply(X_4,
                                  multiply(X_10, X_11)),
                                multiply(identity, multiply(X_11, X_11))),
                              multiply(multiply(X_4, multiply(X_10, X_11)),
                                multiply(identity,
                                  multiply(X_11, X_11)))))),
                        multiply(X_10,
                          multiply(identity,
                            multiply(multiply(multiply(X_4,
                                  multiply(X_10, X_11)),
                                multiply(identity, multiply(X_11, X_11))),
                              multiply(multiply(X_4, multiply(X_10, X_11)),
                                multiply(identity,
                                  multiply(X_11, X_11))))))),
                      multiply(multiply(X_10,
                          multiply(identity,
                            multiply(multiply(X_4,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))),
                              multiply(X_4,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11)))))),
                        multiply(X_10,
                          multiply(identity,
                            multiply(multiply(X_4,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))),
                              multiply(X_4,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11)))))))))],
              [refute_0_114, refute_0_117])).

cnf(refute_0_119, plain,
    (multiply(X_4,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11)))))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))))))))) =
     multiply(X_4,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11)))))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11)))))))))),
    introduced(tautology,
               [refl,
                [$fot(multiply(X_4,
                        multiply(identity,
                          multiply(multiply(X_10,
                              multiply(identity,
                                multiply(multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11))),
                                  multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11)))))),
                            multiply(X_10,
                              multiply(identity,
                                multiply(multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11))),
                                  multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11))))))))))]])).

cnf(refute_0_120, plain,
    (multiply(X_4,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11)))))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))))))))) !=
     multiply(X_4,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11)))))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))))))))) |
     multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11))),
               multiply(multiply(X_4, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11)))))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11))),
               multiply(multiply(X_4, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11)))))))) !=
     multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
               multiply(X_4,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
               multiply(X_4,
                 multiply(multiply(X_10, X_11),
                   multiply(X_11, X_11)))))))) |
     multiply(X_4,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11)))))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))))))))) =
     multiply(X_4,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4,
                   multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                 multiply(X_4,
                   multiply(multiply(X_10, X_11),
                     multiply(X_11, X_11)))))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4,
                   multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                 multiply(X_4,
                   multiply(multiply(X_10, X_11),
                     multiply(X_11, X_11)))))))))),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))))))))),
                        multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))))))))))),
                 [1, 1],
                 $fot(multiply(identity,
                        multiply(multiply(X_10,
                            multiply(identity,
                              multiply(multiply(X_4,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11))),
                                multiply(X_4,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11)))))),
                          multiply(X_10,
                            multiply(identity,
                              multiply(multiply(X_4,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11))),
                                multiply(X_4,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11)))))))))]])).

cnf(refute_0_121, plain,
    (multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11))),
               multiply(multiply(X_4, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11)))))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11))),
               multiply(multiply(X_4, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11)))))))) !=
     multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
               multiply(X_4,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
               multiply(X_4,
                 multiply(multiply(X_10, X_11),
                   multiply(X_11, X_11)))))))) |
     multiply(X_4,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11)))))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))))))))) =
     multiply(X_4,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4,
                   multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                 multiply(X_4,
                   multiply(multiply(X_10, X_11),
                     multiply(X_11, X_11)))))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4,
                   multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                 multiply(X_4,
                   multiply(multiply(X_10, X_11),
                     multiply(X_11, X_11)))))))))),
    inference(resolve,
              [$cnf($equal(multiply(X_4,
                        multiply(identity,
                          multiply(multiply(X_10,
                              multiply(identity,
                                multiply(multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11))),
                                  multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11)))))),
                            multiply(X_10,
                              multiply(identity,
                                multiply(multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11))),
                                  multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11))))))))),
                      multiply(X_4,
                        multiply(identity,
                          multiply(multiply(X_10,
                              multiply(identity,
                                multiply(multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11))),
                                  multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11)))))),
                            multiply(X_10,
                              multiply(identity,
                                multiply(multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11))),
                                  multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11)))))))))))],
              [refute_0_119, refute_0_120])).

cnf(refute_0_122, plain,
    (multiply(X_4,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11)))))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))))))))) =
     multiply(X_4,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4,
                   multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                 multiply(X_4,
                   multiply(multiply(X_10, X_11),
                     multiply(X_11, X_11)))))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4,
                   multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                 multiply(X_4,
                   multiply(multiply(X_10, X_11),
                     multiply(X_11, X_11)))))))))),
    inference(resolve,
              [$cnf($equal(multiply(identity,
                        multiply(multiply(X_10,
                            multiply(identity,
                              multiply(multiply(multiply(X_4,
                                    multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11))),
                                multiply(multiply(X_4,
                                    multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11)))))),
                          multiply(X_10,
                            multiply(identity,
                              multiply(multiply(multiply(X_4,
                                    multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11))),
                                multiply(multiply(X_4,
                                    multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11)))))))),
                      multiply(identity,
                        multiply(multiply(X_10,
                            multiply(identity,
                              multiply(multiply(X_4,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11))),
                                multiply(X_4,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11)))))),
                          multiply(X_10,
                            multiply(identity,
                              multiply(multiply(X_4,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11))),
                                multiply(X_4,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11))))))))))],
              [refute_0_118, refute_0_121])).

cnf(refute_0_123, plain,
    (multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11))))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))))))) =
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11))))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))))))))))),
    introduced(tautology,
               [refl,
                [$fot(multiply(multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))),
                                    multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))),
                                    multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))))))))),
                        multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11)))))))))))]])).

cnf(refute_0_124, plain,
    (multiply(X_4,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11)))))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))))))))) !=
     multiply(X_4,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4,
                   multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                 multiply(X_4,
                   multiply(multiply(X_10, X_11),
                     multiply(X_11, X_11)))))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4,
                   multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                 multiply(X_4,
                   multiply(multiply(X_10, X_11),
                     multiply(X_11, X_11))))))))) |
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11))))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))))))) !=
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11))))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))))))) |
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11))))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))))))) =
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11))))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11))))))))))),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))),
                                      multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11)))))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))),
                                      multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))))))))),
                          multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11)))))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11)))))))))),
                        multiply(multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))),
                                      multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11)))))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))),
                                      multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))))))))),
                          multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11)))))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11)))))))))))),
                 [1, 1],
                 $fot(multiply(X_4,
                        multiply(identity,
                          multiply(multiply(X_10,
                              multiply(identity,
                                multiply(multiply(X_4,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11))),
                                  multiply(X_4,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11)))))),
                            multiply(X_10,
                              multiply(identity,
                                multiply(multiply(X_4,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11))),
                                  multiply(X_4,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11))))))))))]])).

cnf(refute_0_125, plain,
    (multiply(X_4,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11)))))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))))))))) !=
     multiply(X_4,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4,
                   multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                 multiply(X_4,
                   multiply(multiply(X_10, X_11),
                     multiply(X_11, X_11)))))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4,
                   multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                 multiply(X_4,
                   multiply(multiply(X_10, X_11),
                     multiply(X_11, X_11))))))))) |
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11))))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))))))) =
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11))))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11))))))))))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))),
                                    multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))),
                                    multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))))))))),
                        multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11)))))))))),
                      multiply(multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))),
                                    multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))),
                                    multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))))))))),
                        multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))))))))))))],
              [refute_0_123, refute_0_124])).

cnf(refute_0_126, plain,
    (multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11))))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))))))) =
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11))))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11))))))))))),
    inference(resolve,
              [$cnf($equal(multiply(X_4,
                        multiply(identity,
                          multiply(multiply(X_10,
                              multiply(identity,
                                multiply(multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11))),
                                  multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11)))))),
                            multiply(X_10,
                              multiply(identity,
                                multiply(multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11))),
                                  multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11))))))))),
                      multiply(X_4,
                        multiply(identity,
                          multiply(multiply(X_10,
                              multiply(identity,
                                multiply(multiply(X_4,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11))),
                                  multiply(X_4,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11)))))),
                            multiply(X_10,
                              multiply(identity,
                                multiply(multiply(X_4,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11))),
                                  multiply(X_4,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11)))))))))))],
              [refute_0_122, refute_0_125])).

cnf(refute_0_127, plain,
    (multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))))))) =
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))))))))))),
    introduced(tautology,
               [refl,
                [$fot(multiply(multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))))))))),
                        multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11)))))))))))]])).

cnf(refute_0_128, plain,
    (multiply(X_4,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11)))))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))))))))) !=
     multiply(X_4,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4,
                   multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                 multiply(X_4,
                   multiply(multiply(X_10, X_11),
                     multiply(X_11, X_11)))))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4,
                   multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                 multiply(X_4,
                   multiply(multiply(X_10, X_11),
                     multiply(X_11, X_11))))))))) |
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))))))) !=
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))))))) |
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))))))) =
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11))))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))))))))))),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11)))))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))))))))),
                          multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11)))))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11)))))))))),
                        multiply(multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11)))))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))))))))),
                          multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11)))))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11)))))))))))),
                 [1, 0],
                 $fot(multiply(X_4,
                        multiply(identity,
                          multiply(multiply(X_10,
                              multiply(identity,
                                multiply(multiply(X_4,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11))),
                                  multiply(X_4,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11)))))),
                            multiply(X_10,
                              multiply(identity,
                                multiply(multiply(X_4,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11))),
                                  multiply(X_4,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11))))))))))]])).

cnf(refute_0_129, plain,
    (multiply(X_4,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11)))))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(X_4, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))))))))) !=
     multiply(X_4,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4,
                   multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                 multiply(X_4,
                   multiply(multiply(X_10, X_11),
                     multiply(X_11, X_11)))))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4,
                   multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                 multiply(X_4,
                   multiply(multiply(X_10, X_11),
                     multiply(X_11, X_11))))))))) |
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))))))) =
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11))))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))))))))))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))))))))),
                        multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11)))))))))),
                      multiply(multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))))))))),
                        multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))))))))))))],
              [refute_0_127, refute_0_128])).

cnf(refute_0_130, plain,
    (multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))))))) =
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11))))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))))))))))),
    inference(resolve,
              [$cnf($equal(multiply(X_4,
                        multiply(identity,
                          multiply(multiply(X_10,
                              multiply(identity,
                                multiply(multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11))),
                                  multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11)))))),
                            multiply(X_10,
                              multiply(identity,
                                multiply(multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11))),
                                  multiply(multiply(X_4,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11))))))))),
                      multiply(X_4,
                        multiply(identity,
                          multiply(multiply(X_10,
                              multiply(identity,
                                multiply(multiply(X_4,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11))),
                                  multiply(X_4,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11)))))),
                            multiply(X_10,
                              multiply(identity,
                                multiply(multiply(X_4,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11))),
                                  multiply(X_4,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11)))))))))))],
              [refute_0_122, refute_0_129])).

cnf(refute_0_131, plain,
    (multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11))))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))))))) !=
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11))))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))))))) |
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))))))) !=
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11))))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))))))) |
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))))))) =
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11))))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11))))))))))),
    inference(subst, [],
              [refute_0_92 :
               [bind(X0,
                     $fot(multiply(multiply(X_4,
                              multiply(identity,
                                multiply(multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11)))))),
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))))))))),
                            multiply(X_4,
                              multiply(identity,
                                multiply(multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11)))))),
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11,
                                              X_11)))))))))))),
                bind(Y0,
                     $fot(multiply(multiply(X_4,
                              multiply(identity,
                                multiply(multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(X_4,
                                          multiply(multiply(X_10, X_11),
                                            multiply(X_11, X_11))),
                                        multiply(X_4,
                                          multiply(multiply(X_10, X_11),
                                            multiply(X_11, X_11)))))),
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(X_4,
                                          multiply(multiply(X_10, X_11),
                                            multiply(X_11, X_11))),
                                        multiply(X_4,
                                          multiply(multiply(X_10, X_11),
                                            multiply(X_11, X_11))))))))),
                            multiply(X_4,
                              multiply(identity,
                                multiply(multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11)))))),
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11,
                                              X_11)))))))))))),
                bind(Z0,
                     $fot(multiply(multiply(X_4,
                              multiply(identity,
                                multiply(multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(X_4,
                                          multiply(multiply(X_10, X_11),
                                            multiply(X_11, X_11))),
                                        multiply(X_4,
                                          multiply(multiply(X_10, X_11),
                                            multiply(X_11, X_11)))))),
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(X_4,
                                          multiply(multiply(X_10, X_11),
                                            multiply(X_11, X_11))),
                                        multiply(X_4,
                                          multiply(multiply(X_10, X_11),
                                            multiply(X_11, X_11))))))))),
                            multiply(X_4,
                              multiply(identity,
                                multiply(multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(X_4,
                                          multiply(multiply(X_10, X_11),
                                            multiply(X_11, X_11))),
                                        multiply(X_4,
                                          multiply(multiply(X_10, X_11),
                                            multiply(X_11, X_11)))))),
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(X_4,
                                          multiply(multiply(X_10, X_11),
                                            multiply(X_11, X_11))),
                                        multiply(X_4,
                                          multiply(multiply(X_10, X_11),
                                            multiply(X_11,
                                              X_11))))))))))))]])).

cnf(refute_0_132, plain,
    (multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11))))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))))))) !=
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11))))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))))))) |
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))))))) =
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11))))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11))))))))))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))))))))),
                        multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11)))))))))),
                      multiply(multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))),
                                    multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))),
                                    multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))))))))),
                        multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))))))))))))],
              [refute_0_130, refute_0_131])).

cnf(refute_0_133, plain,
    (multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))))))) =
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11))))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11))))))))))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))),
                                    multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))),
                                    multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))))))))),
                        multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11)))))))))),
                      multiply(multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))),
                                    multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))),
                                    multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))))))))),
                        multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))),
                                    multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))),
                                    multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))))))))))))],
              [refute_0_126, refute_0_132])).

cnf(refute_0_134, plain,
    (multiply(identity,
       multiply(multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))))))))),
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))))))))))) =
     multiply(identity,
       multiply(multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))))))))),
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))))))))),
    introduced(tautology,
               [refl,
                [$fot(multiply(identity,
                        multiply(multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11)))))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))))))))),
                          multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11)))))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11,
                                            X_11))))))))))))]])).

cnf(refute_0_135, plain,
    (multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))))))) !=
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11))))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))))))) |
     multiply(identity,
       multiply(multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))))))))),
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))))))))))) !=
     multiply(identity,
       multiply(multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))))))))),
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))))))))))) |
     multiply(identity,
       multiply(multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))))))))),
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))))))))))) =
     multiply(identity,
       multiply(multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))))))))),
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11)))))))))))),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(identity,
                          multiply(multiply(X_4,
                              multiply(identity,
                                multiply(multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11)))))),
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))))))))),
                            multiply(X_4,
                              multiply(identity,
                                multiply(multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11)))))),
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))))))))))),
                        multiply(identity,
                          multiply(multiply(X_4,
                              multiply(identity,
                                multiply(multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11)))))),
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))))))))),
                            multiply(X_4,
                              multiply(identity,
                                multiply(multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11)))))),
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11,
                                              X_11))))))))))))), [1, 1],
                 $fot(multiply(multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))),
                                    multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))),
                                    multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))))))))),
                        multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))),
                                    multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))),
                                    multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11)))))))))))]])).

cnf(refute_0_136, plain,
    (multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(X_4, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))))))) !=
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11))))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))))))) |
     multiply(identity,
       multiply(multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))))))))),
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))))))))))) =
     multiply(identity,
       multiply(multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))))))))),
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11)))))))))))),
    inference(resolve,
              [$cnf($equal(multiply(identity,
                        multiply(multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11)))))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))))))))),
                          multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11)))))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))))))))))),
                      multiply(identity,
                        multiply(multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11)))))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))))))))),
                          multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11)))))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11,
                                            X_11)))))))))))))],
              [refute_0_134, refute_0_135])).

cnf(refute_0_137, plain,
    (multiply(identity,
       multiply(multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))))))))),
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))))))))))) =
     multiply(identity,
       multiply(multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))))))))),
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11)))))))))))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))))))))),
                        multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(X_4,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11)))))))))),
                      multiply(multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))),
                                    multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))),
                                    multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))))))))),
                        multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))),
                                    multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))),
                                    multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))))))))))))],
              [refute_0_133, refute_0_136])).

cnf(refute_0_138, plain,
    (multiply(X_4,
       multiply(identity,
         multiply(multiply(X_4,
             multiply(identity,
               multiply(multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11)))))),
                 multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))))))))),
           multiply(X_4,
             multiply(identity,
               multiply(multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11)))))),
                 multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity,
                           multiply(X_11, X_11)))))))))))) =
     multiply(X_4,
       multiply(identity,
         multiply(multiply(X_4,
             multiply(identity,
               multiply(multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11)))))),
                 multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))))))))),
           multiply(X_4,
             multiply(identity,
               multiply(multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11)))))),
                 multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity,
                           multiply(X_11, X_11))))))))))))),
    introduced(tautology,
               [refl,
                [$fot(multiply(X_4,
                        multiply(identity,
                          multiply(multiply(X_4,
                              multiply(identity,
                                multiply(multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11)))))),
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))))))))),
                            multiply(X_4,
                              multiply(identity,
                                multiply(multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11)))))),
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11,
                                              X_11)))))))))))))]])).

cnf(refute_0_139, plain,
    (multiply(X_4,
       multiply(identity,
         multiply(multiply(X_4,
             multiply(identity,
               multiply(multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11)))))),
                 multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))))))))),
           multiply(X_4,
             multiply(identity,
               multiply(multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11)))))),
                 multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity,
                           multiply(X_11, X_11)))))))))))) !=
     multiply(X_4,
       multiply(identity,
         multiply(multiply(X_4,
             multiply(identity,
               multiply(multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11)))))),
                 multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))))))))),
           multiply(X_4,
             multiply(identity,
               multiply(multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11)))))),
                 multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity,
                           multiply(X_11, X_11)))))))))))) |
     multiply(identity,
       multiply(multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))))))))),
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))))))))))) !=
     multiply(identity,
       multiply(multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))))))))),
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))))))))))) |
     multiply(X_4,
       multiply(identity,
         multiply(multiply(X_4,
             multiply(identity,
               multiply(multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11)))))),
                 multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))))))))),
           multiply(X_4,
             multiply(identity,
               multiply(multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11)))))),
                 multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity,
                           multiply(X_11, X_11)))))))))))) =
     multiply(X_4,
       multiply(identity,
         multiply(multiply(X_4,
             multiply(identity,
               multiply(multiply(X_10,
                   multiply(identity,
                     multiply(multiply(X_4,
                         multiply(multiply(X_10, X_11),
                           multiply(X_11, X_11))),
                       multiply(X_4,
                         multiply(multiply(X_10, X_11),
                           multiply(X_11, X_11)))))),
                 multiply(X_10,
                   multiply(identity,
                     multiply(multiply(X_4,
                         multiply(multiply(X_10, X_11),
                           multiply(X_11, X_11))),
                       multiply(X_4,
                         multiply(multiply(X_10, X_11),
                           multiply(X_11, X_11))))))))),
           multiply(X_4,
             multiply(identity,
               multiply(multiply(X_10,
                   multiply(identity,
                     multiply(multiply(X_4,
                         multiply(multiply(X_10, X_11),
                           multiply(X_11, X_11))),
                       multiply(X_4,
                         multiply(multiply(X_10, X_11),
                           multiply(X_11, X_11)))))),
                 multiply(X_10,
                   multiply(identity,
                     multiply(multiply(X_4,
                         multiply(multiply(X_10, X_11),
                           multiply(X_11, X_11))),
                       multiply(X_4,
                         multiply(multiply(X_10, X_11),
                           multiply(X_11, X_11))))))))))))),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_4,
                                multiply(identity,
                                  multiply(multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))),
                                          multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11)))))),
                                    multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))),
                                          multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))))))))),
                              multiply(X_4,
                                multiply(identity,
                                  multiply(multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))),
                                          multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11)))))),
                                    multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))),
                                          multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11,
                                                X_11)))))))))))),
                        multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_4,
                                multiply(identity,
                                  multiply(multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))),
                                          multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11)))))),
                                    multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))),
                                          multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))))))))),
                              multiply(X_4,
                                multiply(identity,
                                  multiply(multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))),
                                          multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11)))))),
                                    multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))),
                                          multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11,
                                                X_11)))))))))))))), [1, 1],
                 $fot(multiply(identity,
                        multiply(multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))),
                                      multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11)))))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))),
                                      multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))))))))),
                          multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))),
                                      multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11)))))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))),
                                      multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11,
                                            X_11))))))))))))]])).

cnf(refute_0_140, plain,
    (multiply(identity,
       multiply(multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))))))))),
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(X_4, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))))))))))) !=
     multiply(identity,
       multiply(multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))))))))),
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))))))))))) |
     multiply(X_4,
       multiply(identity,
         multiply(multiply(X_4,
             multiply(identity,
               multiply(multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11)))))),
                 multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))))))))),
           multiply(X_4,
             multiply(identity,
               multiply(multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11)))))),
                 multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity,
                           multiply(X_11, X_11)))))))))))) =
     multiply(X_4,
       multiply(identity,
         multiply(multiply(X_4,
             multiply(identity,
               multiply(multiply(X_10,
                   multiply(identity,
                     multiply(multiply(X_4,
                         multiply(multiply(X_10, X_11),
                           multiply(X_11, X_11))),
                       multiply(X_4,
                         multiply(multiply(X_10, X_11),
                           multiply(X_11, X_11)))))),
                 multiply(X_10,
                   multiply(identity,
                     multiply(multiply(X_4,
                         multiply(multiply(X_10, X_11),
                           multiply(X_11, X_11))),
                       multiply(X_4,
                         multiply(multiply(X_10, X_11),
                           multiply(X_11, X_11))))))))),
           multiply(X_4,
             multiply(identity,
               multiply(multiply(X_10,
                   multiply(identity,
                     multiply(multiply(X_4,
                         multiply(multiply(X_10, X_11),
                           multiply(X_11, X_11))),
                       multiply(X_4,
                         multiply(multiply(X_10, X_11),
                           multiply(X_11, X_11)))))),
                 multiply(X_10,
                   multiply(identity,
                     multiply(multiply(X_4,
                         multiply(multiply(X_10, X_11),
                           multiply(X_11, X_11))),
                       multiply(X_4,
                         multiply(multiply(X_10, X_11),
                           multiply(X_11, X_11))))))))))))),
    inference(resolve,
              [$cnf($equal(multiply(X_4,
                        multiply(identity,
                          multiply(multiply(X_4,
                              multiply(identity,
                                multiply(multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11)))))),
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))))))))),
                            multiply(X_4,
                              multiply(identity,
                                multiply(multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11)))))),
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11,
                                              X_11)))))))))))),
                      multiply(X_4,
                        multiply(identity,
                          multiply(multiply(X_4,
                              multiply(identity,
                                multiply(multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11)))))),
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))))))))),
                            multiply(X_4,
                              multiply(identity,
                                multiply(multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11)))))),
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11,
                                              X_11))))))))))))))],
              [refute_0_138, refute_0_139])).

cnf(refute_0_141, plain,
    (multiply(X_4,
       multiply(identity,
         multiply(multiply(X_4,
             multiply(identity,
               multiply(multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11)))))),
                 multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))))))))),
           multiply(X_4,
             multiply(identity,
               multiply(multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11)))))),
                 multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity,
                           multiply(X_11, X_11)))))))))))) =
     multiply(X_4,
       multiply(identity,
         multiply(multiply(X_4,
             multiply(identity,
               multiply(multiply(X_10,
                   multiply(identity,
                     multiply(multiply(X_4,
                         multiply(multiply(X_10, X_11),
                           multiply(X_11, X_11))),
                       multiply(X_4,
                         multiply(multiply(X_10, X_11),
                           multiply(X_11, X_11)))))),
                 multiply(X_10,
                   multiply(identity,
                     multiply(multiply(X_4,
                         multiply(multiply(X_10, X_11),
                           multiply(X_11, X_11))),
                       multiply(X_4,
                         multiply(multiply(X_10, X_11),
                           multiply(X_11, X_11))))))))),
           multiply(X_4,
             multiply(identity,
               multiply(multiply(X_10,
                   multiply(identity,
                     multiply(multiply(X_4,
                         multiply(multiply(X_10, X_11),
                           multiply(X_11, X_11))),
                       multiply(X_4,
                         multiply(multiply(X_10, X_11),
                           multiply(X_11, X_11)))))),
                 multiply(X_10,
                   multiply(identity,
                     multiply(multiply(X_4,
                         multiply(multiply(X_10, X_11),
                           multiply(X_11, X_11))),
                       multiply(X_4,
                         multiply(multiply(X_10, X_11),
                           multiply(X_11, X_11))))))))))))),
    inference(resolve,
              [$cnf($equal(multiply(identity,
                        multiply(multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11)))))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))))))))),
                          multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11)))))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(X_4,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))))))))))),
                      multiply(identity,
                        multiply(multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))),
                                      multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11)))))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))),
                                      multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))))))))),
                          multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))),
                                      multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11)))))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))),
                                      multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11,
                                            X_11)))))))))))))],
              [refute_0_137, refute_0_140])).

cnf(refute_0_142, plain,
    (multiply(X_4,
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(identity,
                 multiply(multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11)))))),
                   multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))))))))),
             multiply(X_4,
               multiply(identity,
                 multiply(multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11)))))),
                   multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity,
                             multiply(X_11, X_11))))))))))))) =
     multiply(X_4,
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(identity,
                 multiply(multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11)))))),
                   multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))))))))),
             multiply(X_4,
               multiply(identity,
                 multiply(multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11)))))),
                   multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity,
                             multiply(X_11, X_11)))))))))))))),
    introduced(tautology,
               [refl,
                [$fot(multiply(X_4,
                        multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_4,
                                multiply(identity,
                                  multiply(multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))),
                                          multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11)))))),
                                    multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))),
                                          multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))))))))),
                              multiply(X_4,
                                multiply(identity,
                                  multiply(multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))),
                                          multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11)))))),
                                    multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))),
                                          multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11,
                                                X_11))))))))))))))]])).

cnf(refute_0_143, plain,
    (multiply(X_4,
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(identity,
                 multiply(multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11)))))),
                   multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))))))))),
             multiply(X_4,
               multiply(identity,
                 multiply(multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11)))))),
                   multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity,
                             multiply(X_11, X_11))))))))))))) !=
     multiply(X_4,
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(identity,
                 multiply(multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11)))))),
                   multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))))))))),
             multiply(X_4,
               multiply(identity,
                 multiply(multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11)))))),
                   multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity,
                             multiply(X_11, X_11))))))))))))) |
     multiply(X_4,
       multiply(identity,
         multiply(multiply(X_4,
             multiply(identity,
               multiply(multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11)))))),
                 multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))))))))),
           multiply(X_4,
             multiply(identity,
               multiply(multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11)))))),
                 multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity,
                           multiply(X_11, X_11)))))))))))) !=
     multiply(X_4,
       multiply(identity,
         multiply(multiply(X_4,
             multiply(identity,
               multiply(multiply(X_10,
                   multiply(identity,
                     multiply(multiply(X_4,
                         multiply(multiply(X_10, X_11),
                           multiply(X_11, X_11))),
                       multiply(X_4,
                         multiply(multiply(X_10, X_11),
                           multiply(X_11, X_11)))))),
                 multiply(X_10,
                   multiply(identity,
                     multiply(multiply(X_4,
                         multiply(multiply(X_10, X_11),
                           multiply(X_11, X_11))),
                       multiply(X_4,
                         multiply(multiply(X_10, X_11),
                           multiply(X_11, X_11))))))))),
           multiply(X_4,
             multiply(identity,
               multiply(multiply(X_10,
                   multiply(identity,
                     multiply(multiply(X_4,
                         multiply(multiply(X_10, X_11),
                           multiply(X_11, X_11))),
                       multiply(X_4,
                         multiply(multiply(X_10, X_11),
                           multiply(X_11, X_11)))))),
                 multiply(X_10,
                   multiply(identity,
                     multiply(multiply(X_4,
                         multiply(multiply(X_10, X_11),
                           multiply(X_11, X_11))),
                       multiply(X_4,
                         multiply(multiply(X_10, X_11),
                           multiply(X_11, X_11)))))))))))) |
     multiply(X_4,
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(identity,
                 multiply(multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11)))))),
                   multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))))))))),
             multiply(X_4,
               multiply(identity,
                 multiply(multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11)))))),
                   multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity,
                             multiply(X_11, X_11))))))))))))) =
     multiply(X_4,
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(identity,
                 multiply(multiply(X_10,
                     multiply(identity,
                       multiply(multiply(X_4,
                           multiply(multiply(X_10, X_11),
                             multiply(X_11, X_11))),
                         multiply(X_4,
                           multiply(multiply(X_10, X_11),
                             multiply(X_11, X_11)))))),
                   multiply(X_10,
                     multiply(identity,
                       multiply(multiply(X_4,
                           multiply(multiply(X_10, X_11),
                             multiply(X_11, X_11))),
                         multiply(X_4,
                           multiply(multiply(X_10, X_11),
                             multiply(X_11, X_11))))))))),
             multiply(X_4,
               multiply(identity,
                 multiply(multiply(X_10,
                     multiply(identity,
                       multiply(multiply(X_4,
                           multiply(multiply(X_10, X_11),
                             multiply(X_11, X_11))),
                         multiply(X_4,
                           multiply(multiply(X_10, X_11),
                             multiply(X_11, X_11)))))),
                   multiply(X_10,
                     multiply(identity,
                       multiply(multiply(X_4,
                           multiply(multiply(X_10, X_11),
                             multiply(X_11, X_11))),
                         multiply(X_4,
                           multiply(multiply(X_10, X_11),
                             multiply(X_11, X_11)))))))))))))),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(X_4,
                          multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_4,
                                  multiply(identity,
                                    multiply(multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11)))))),
                                      multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11,
                                                  X_11))))))))),
                                multiply(X_4,
                                  multiply(identity,
                                    multiply(multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11)))))),
                                      multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11,
                                                  X_11))))))))))))),
                        multiply(X_4,
                          multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_4,
                                  multiply(identity,
                                    multiply(multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11)))))),
                                      multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11,
                                                  X_11))))))))),
                                multiply(X_4,
                                  multiply(identity,
                                    multiply(multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11)))))),
                                      multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11,
                                                  X_11))))))))))))))),
                 [1, 1],
                 $fot(multiply(X_4,
                        multiply(identity,
                          multiply(multiply(X_4,
                              multiply(identity,
                                multiply(multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(X_4,
                                          multiply(multiply(X_10, X_11),
                                            multiply(X_11, X_11))),
                                        multiply(X_4,
                                          multiply(multiply(X_10, X_11),
                                            multiply(X_11, X_11)))))),
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(X_4,
                                          multiply(multiply(X_10, X_11),
                                            multiply(X_11, X_11))),
                                        multiply(X_4,
                                          multiply(multiply(X_10, X_11),
                                            multiply(X_11, X_11))))))))),
                            multiply(X_4,
                              multiply(identity,
                                multiply(multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(X_4,
                                          multiply(multiply(X_10, X_11),
                                            multiply(X_11, X_11))),
                                        multiply(X_4,
                                          multiply(multiply(X_10, X_11),
                                            multiply(X_11, X_11)))))),
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(X_4,
                                          multiply(multiply(X_10, X_11),
                                            multiply(X_11, X_11))),
                                        multiply(X_4,
                                          multiply(multiply(X_10, X_11),
                                            multiply(X_11,
                                              X_11)))))))))))))]])).

cnf(refute_0_144, plain,
    (multiply(X_4,
       multiply(identity,
         multiply(multiply(X_4,
             multiply(identity,
               multiply(multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11)))))),
                 multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))))))))),
           multiply(X_4,
             multiply(identity,
               multiply(multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11)))))),
                 multiply(X_10,
                   multiply(identity,
                     multiply(multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity, multiply(X_11, X_11))),
                       multiply(multiply(X_4, multiply(X_10, X_11)),
                         multiply(identity,
                           multiply(X_11, X_11)))))))))))) !=
     multiply(X_4,
       multiply(identity,
         multiply(multiply(X_4,
             multiply(identity,
               multiply(multiply(X_10,
                   multiply(identity,
                     multiply(multiply(X_4,
                         multiply(multiply(X_10, X_11),
                           multiply(X_11, X_11))),
                       multiply(X_4,
                         multiply(multiply(X_10, X_11),
                           multiply(X_11, X_11)))))),
                 multiply(X_10,
                   multiply(identity,
                     multiply(multiply(X_4,
                         multiply(multiply(X_10, X_11),
                           multiply(X_11, X_11))),
                       multiply(X_4,
                         multiply(multiply(X_10, X_11),
                           multiply(X_11, X_11))))))))),
           multiply(X_4,
             multiply(identity,
               multiply(multiply(X_10,
                   multiply(identity,
                     multiply(multiply(X_4,
                         multiply(multiply(X_10, X_11),
                           multiply(X_11, X_11))),
                       multiply(X_4,
                         multiply(multiply(X_10, X_11),
                           multiply(X_11, X_11)))))),
                 multiply(X_10,
                   multiply(identity,
                     multiply(multiply(X_4,
                         multiply(multiply(X_10, X_11),
                           multiply(X_11, X_11))),
                       multiply(X_4,
                         multiply(multiply(X_10, X_11),
                           multiply(X_11, X_11)))))))))))) |
     multiply(X_4,
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(identity,
                 multiply(multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11)))))),
                   multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))))))))),
             multiply(X_4,
               multiply(identity,
                 multiply(multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11)))))),
                   multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity,
                             multiply(X_11, X_11))))))))))))) =
     multiply(X_4,
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(identity,
                 multiply(multiply(X_10,
                     multiply(identity,
                       multiply(multiply(X_4,
                           multiply(multiply(X_10, X_11),
                             multiply(X_11, X_11))),
                         multiply(X_4,
                           multiply(multiply(X_10, X_11),
                             multiply(X_11, X_11)))))),
                   multiply(X_10,
                     multiply(identity,
                       multiply(multiply(X_4,
                           multiply(multiply(X_10, X_11),
                             multiply(X_11, X_11))),
                         multiply(X_4,
                           multiply(multiply(X_10, X_11),
                             multiply(X_11, X_11))))))))),
             multiply(X_4,
               multiply(identity,
                 multiply(multiply(X_10,
                     multiply(identity,
                       multiply(multiply(X_4,
                           multiply(multiply(X_10, X_11),
                             multiply(X_11, X_11))),
                         multiply(X_4,
                           multiply(multiply(X_10, X_11),
                             multiply(X_11, X_11)))))),
                   multiply(X_10,
                     multiply(identity,
                       multiply(multiply(X_4,
                           multiply(multiply(X_10, X_11),
                             multiply(X_11, X_11))),
                         multiply(X_4,
                           multiply(multiply(X_10, X_11),
                             multiply(X_11, X_11)))))))))))))),
    inference(resolve,
              [$cnf($equal(multiply(X_4,
                        multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_4,
                                multiply(identity,
                                  multiply(multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))),
                                          multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11)))))),
                                    multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))),
                                          multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))))))))),
                              multiply(X_4,
                                multiply(identity,
                                  multiply(multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))),
                                          multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11)))))),
                                    multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))),
                                          multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11,
                                                X_11))))))))))))),
                      multiply(X_4,
                        multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_4,
                                multiply(identity,
                                  multiply(multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))),
                                          multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11)))))),
                                    multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))),
                                          multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))))))))),
                              multiply(X_4,
                                multiply(identity,
                                  multiply(multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))),
                                          multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11)))))),
                                    multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))),
                                          multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11,
                                                X_11)))))))))))))))],
              [refute_0_142, refute_0_143])).

cnf(refute_0_145, plain,
    (multiply(X_4,
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(identity,
                 multiply(multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11)))))),
                   multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))))))))),
             multiply(X_4,
               multiply(identity,
                 multiply(multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11)))))),
                   multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity,
                             multiply(X_11, X_11))))))))))))) =
     multiply(X_4,
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(identity,
                 multiply(multiply(X_10,
                     multiply(identity,
                       multiply(multiply(X_4,
                           multiply(multiply(X_10, X_11),
                             multiply(X_11, X_11))),
                         multiply(X_4,
                           multiply(multiply(X_10, X_11),
                             multiply(X_11, X_11)))))),
                   multiply(X_10,
                     multiply(identity,
                       multiply(multiply(X_4,
                           multiply(multiply(X_10, X_11),
                             multiply(X_11, X_11))),
                         multiply(X_4,
                           multiply(multiply(X_10, X_11),
                             multiply(X_11, X_11))))))))),
             multiply(X_4,
               multiply(identity,
                 multiply(multiply(X_10,
                     multiply(identity,
                       multiply(multiply(X_4,
                           multiply(multiply(X_10, X_11),
                             multiply(X_11, X_11))),
                         multiply(X_4,
                           multiply(multiply(X_10, X_11),
                             multiply(X_11, X_11)))))),
                   multiply(X_10,
                     multiply(identity,
                       multiply(multiply(X_4,
                           multiply(multiply(X_10, X_11),
                             multiply(X_11, X_11))),
                         multiply(X_4,
                           multiply(multiply(X_10, X_11),
                             multiply(X_11, X_11)))))))))))))),
    inference(resolve,
              [$cnf($equal(multiply(X_4,
                        multiply(identity,
                          multiply(multiply(X_4,
                              multiply(identity,
                                multiply(multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11)))))),
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))))))))),
                            multiply(X_4,
                              multiply(identity,
                                multiply(multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11)))))),
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(X_4,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11,
                                              X_11)))))))))))),
                      multiply(X_4,
                        multiply(identity,
                          multiply(multiply(X_4,
                              multiply(identity,
                                multiply(multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(X_4,
                                          multiply(multiply(X_10, X_11),
                                            multiply(X_11, X_11))),
                                        multiply(X_4,
                                          multiply(multiply(X_10, X_11),
                                            multiply(X_11, X_11)))))),
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(X_4,
                                          multiply(multiply(X_10, X_11),
                                            multiply(X_11, X_11))),
                                        multiply(X_4,
                                          multiply(multiply(X_10, X_11),
                                            multiply(X_11, X_11))))))))),
                            multiply(X_4,
                              multiply(identity,
                                multiply(multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(X_4,
                                          multiply(multiply(X_10, X_11),
                                            multiply(X_11, X_11))),
                                        multiply(X_4,
                                          multiply(multiply(X_10, X_11),
                                            multiply(X_11, X_11)))))),
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(X_4,
                                          multiply(multiply(X_10, X_11),
                                            multiply(X_11, X_11))),
                                        multiply(X_4,
                                          multiply(multiply(X_10, X_11),
                                            multiply(X_11,
                                              X_11))))))))))))))],
              [refute_0_141, refute_0_144])).

cnf(refute_0_146, plain,
    (multiply(X_4,
       multiply(X_4,
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11))))))))),
               multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11)))))))))))))) =
     multiply(X_4,
       multiply(X_4,
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11))))))))),
               multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11))))))))))))))),
    introduced(tautology,
               [refl,
                [$fot(multiply(X_4,
                        multiply(X_4,
                          multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_4,
                                  multiply(identity,
                                    multiply(multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11)))))),
                                      multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11,
                                                  X_11))))))))),
                                multiply(X_4,
                                  multiply(identity,
                                    multiply(multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11)))))),
                                      multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11,
                                                  X_11)))))))))))))))]])).

cnf(refute_0_147, plain,
    (multiply(X_4,
       multiply(X_4,
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11))))))))),
               multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11)))))))))))))) !=
     multiply(X_4,
       multiply(X_4,
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11))))))))),
               multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11)))))))))))))) |
     multiply(X_4,
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(identity,
                 multiply(multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11)))))),
                   multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))))))))),
             multiply(X_4,
               multiply(identity,
                 multiply(multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11)))))),
                   multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity,
                             multiply(X_11, X_11))))))))))))) !=
     multiply(X_4,
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(identity,
                 multiply(multiply(X_10,
                     multiply(identity,
                       multiply(multiply(X_4,
                           multiply(multiply(X_10, X_11),
                             multiply(X_11, X_11))),
                         multiply(X_4,
                           multiply(multiply(X_10, X_11),
                             multiply(X_11, X_11)))))),
                   multiply(X_10,
                     multiply(identity,
                       multiply(multiply(X_4,
                           multiply(multiply(X_10, X_11),
                             multiply(X_11, X_11))),
                         multiply(X_4,
                           multiply(multiply(X_10, X_11),
                             multiply(X_11, X_11))))))))),
             multiply(X_4,
               multiply(identity,
                 multiply(multiply(X_10,
                     multiply(identity,
                       multiply(multiply(X_4,
                           multiply(multiply(X_10, X_11),
                             multiply(X_11, X_11))),
                         multiply(X_4,
                           multiply(multiply(X_10, X_11),
                             multiply(X_11, X_11)))))),
                   multiply(X_10,
                     multiply(identity,
                       multiply(multiply(X_4,
                           multiply(multiply(X_10, X_11),
                             multiply(X_11, X_11))),
                         multiply(X_4,
                           multiply(multiply(X_10, X_11),
                             multiply(X_11, X_11))))))))))))) |
     multiply(X_4,
       multiply(X_4,
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11))))))))),
               multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11)))))))))))))) =
     multiply(X_4,
       multiply(X_4,
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))),
                           multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))),
                           multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))))))))),
               multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))),
                           multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))),
                           multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))))))))))))))),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(X_4,
                          multiply(X_4,
                            multiply(X_4,
                              multiply(identity,
                                multiply(multiply(X_4,
                                    multiply(identity,
                                      multiply(multiply(X_10,
                                          multiply(identity,
                                            multiply(multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11, X_11))),
                                              multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11,
                                                    X_11)))))),
                                        multiply(X_10,
                                          multiply(identity,
                                            multiply(multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11, X_11))),
                                              multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11,
                                                    X_11))))))))),
                                  multiply(X_4,
                                    multiply(identity,
                                      multiply(multiply(X_10,
                                          multiply(identity,
                                            multiply(multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11, X_11))),
                                              multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11,
                                                    X_11)))))),
                                        multiply(X_10,
                                          multiply(identity,
                                            multiply(multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11, X_11))),
                                              multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11,
                                                    X_11)))))))))))))),
                        multiply(X_4,
                          multiply(X_4,
                            multiply(X_4,
                              multiply(identity,
                                multiply(multiply(X_4,
                                    multiply(identity,
                                      multiply(multiply(X_10,
                                          multiply(identity,
                                            multiply(multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11, X_11))),
                                              multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11,
                                                    X_11)))))),
                                        multiply(X_10,
                                          multiply(identity,
                                            multiply(multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11, X_11))),
                                              multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11,
                                                    X_11))))))))),
                                  multiply(X_4,
                                    multiply(identity,
                                      multiply(multiply(X_10,
                                          multiply(identity,
                                            multiply(multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11, X_11))),
                                              multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11,
                                                    X_11)))))),
                                        multiply(X_10,
                                          multiply(identity,
                                            multiply(multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11, X_11))),
                                              multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11,
                                                    X_11)))))))))))))))),
                 [1, 1],
                 $fot(multiply(X_4,
                        multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_4,
                                multiply(identity,
                                  multiply(multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(X_4,
                                            multiply(multiply(X_10, X_11),
                                              multiply(X_11, X_11))),
                                          multiply(X_4,
                                            multiply(multiply(X_10, X_11),
                                              multiply(X_11, X_11)))))),
                                    multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(X_4,
                                            multiply(multiply(X_10, X_11),
                                              multiply(X_11, X_11))),
                                          multiply(X_4,
                                            multiply(multiply(X_10, X_11),
                                              multiply(X_11, X_11))))))))),
                              multiply(X_4,
                                multiply(identity,
                                  multiply(multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(X_4,
                                            multiply(multiply(X_10, X_11),
                                              multiply(X_11, X_11))),
                                          multiply(X_4,
                                            multiply(multiply(X_10, X_11),
                                              multiply(X_11, X_11)))))),
                                    multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(X_4,
                                            multiply(multiply(X_10, X_11),
                                              multiply(X_11, X_11))),
                                          multiply(X_4,
                                            multiply(multiply(X_10, X_11),
                                              multiply(X_11,
                                                X_11))))))))))))))]])).

cnf(refute_0_148, plain,
    (multiply(X_4,
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(identity,
                 multiply(multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11)))))),
                   multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))))))))),
             multiply(X_4,
               multiply(identity,
                 multiply(multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11)))))),
                   multiply(X_10,
                     multiply(identity,
                       multiply(multiply(multiply(X_4,
                             multiply(X_10, X_11)),
                           multiply(identity, multiply(X_11, X_11))),
                         multiply(multiply(X_4, multiply(X_10, X_11)),
                           multiply(identity,
                             multiply(X_11, X_11))))))))))))) !=
     multiply(X_4,
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(identity,
                 multiply(multiply(X_10,
                     multiply(identity,
                       multiply(multiply(X_4,
                           multiply(multiply(X_10, X_11),
                             multiply(X_11, X_11))),
                         multiply(X_4,
                           multiply(multiply(X_10, X_11),
                             multiply(X_11, X_11)))))),
                   multiply(X_10,
                     multiply(identity,
                       multiply(multiply(X_4,
                           multiply(multiply(X_10, X_11),
                             multiply(X_11, X_11))),
                         multiply(X_4,
                           multiply(multiply(X_10, X_11),
                             multiply(X_11, X_11))))))))),
             multiply(X_4,
               multiply(identity,
                 multiply(multiply(X_10,
                     multiply(identity,
                       multiply(multiply(X_4,
                           multiply(multiply(X_10, X_11),
                             multiply(X_11, X_11))),
                         multiply(X_4,
                           multiply(multiply(X_10, X_11),
                             multiply(X_11, X_11)))))),
                   multiply(X_10,
                     multiply(identity,
                       multiply(multiply(X_4,
                           multiply(multiply(X_10, X_11),
                             multiply(X_11, X_11))),
                         multiply(X_4,
                           multiply(multiply(X_10, X_11),
                             multiply(X_11, X_11))))))))))))) |
     multiply(X_4,
       multiply(X_4,
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11))))))))),
               multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11)))))))))))))) =
     multiply(X_4,
       multiply(X_4,
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))),
                           multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))),
                           multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))))))))),
               multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))),
                           multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))),
                           multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))))))))))))))),
    inference(resolve,
              [$cnf($equal(multiply(X_4,
                        multiply(X_4,
                          multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_4,
                                  multiply(identity,
                                    multiply(multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11)))))),
                                      multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11,
                                                  X_11))))))))),
                                multiply(X_4,
                                  multiply(identity,
                                    multiply(multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11)))))),
                                      multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11,
                                                  X_11)))))))))))))),
                      multiply(X_4,
                        multiply(X_4,
                          multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_4,
                                  multiply(identity,
                                    multiply(multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11)))))),
                                      multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11,
                                                  X_11))))))))),
                                multiply(X_4,
                                  multiply(identity,
                                    multiply(multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11)))))),
                                      multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11,
                                                  X_11))))))))))))))))],
              [refute_0_146, refute_0_147])).

cnf(refute_0_149, plain,
    (multiply(X_4,
       multiply(X_4,
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11))))))))),
               multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11)))))))))))))) =
     multiply(X_4,
       multiply(X_4,
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))),
                           multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))),
                           multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))))))))),
               multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))),
                           multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))),
                           multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))))))))))))))),
    inference(resolve,
              [$cnf($equal(multiply(X_4,
                        multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_4,
                                multiply(identity,
                                  multiply(multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))),
                                          multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11)))))),
                                    multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))),
                                          multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))))))))),
                              multiply(X_4,
                                multiply(identity,
                                  multiply(multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))),
                                          multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11)))))),
                                    multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11, X_11))),
                                          multiply(multiply(X_4,
                                              multiply(X_10, X_11)),
                                            multiply(identity,
                                              multiply(X_11,
                                                X_11))))))))))))),
                      multiply(X_4,
                        multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_4,
                                multiply(identity,
                                  multiply(multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(X_4,
                                            multiply(multiply(X_10, X_11),
                                              multiply(X_11, X_11))),
                                          multiply(X_4,
                                            multiply(multiply(X_10, X_11),
                                              multiply(X_11, X_11)))))),
                                    multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(X_4,
                                            multiply(multiply(X_10, X_11),
                                              multiply(X_11, X_11))),
                                          multiply(X_4,
                                            multiply(multiply(X_10, X_11),
                                              multiply(X_11, X_11))))))))),
                              multiply(X_4,
                                multiply(identity,
                                  multiply(multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(X_4,
                                            multiply(multiply(X_10, X_11),
                                              multiply(X_11, X_11))),
                                          multiply(X_4,
                                            multiply(multiply(X_10, X_11),
                                              multiply(X_11, X_11)))))),
                                    multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(X_4,
                                            multiply(multiply(X_10, X_11),
                                              multiply(X_11, X_11))),
                                          multiply(X_4,
                                            multiply(multiply(X_10, X_11),
                                              multiply(X_11,
                                                X_11)))))))))))))))],
              [refute_0_145, refute_0_148])).

cnf(refute_0_150, plain,
    (multiply(X_4,
       multiply(X_4,
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11))))))))),
               multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11)))))))))))))) != X_4 |
     multiply(X_4,
       multiply(X_4,
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11))))))))),
               multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11)))))))))))))) !=
     multiply(X_4,
       multiply(X_4,
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))),
                           multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))),
                           multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))))))))),
               multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))),
                           multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))),
                           multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11)))))))))))))) |
     multiply(X_4,
       multiply(X_4,
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))),
                           multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))),
                           multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))))))))),
               multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))),
                           multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))),
                           multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11)))))))))))))) = X_4),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(X_4,
                          multiply(X_4,
                            multiply(X_4,
                              multiply(identity,
                                multiply(multiply(X_4,
                                    multiply(identity,
                                      multiply(multiply(X_10,
                                          multiply(identity,
                                            multiply(multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11, X_11))),
                                              multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11,
                                                    X_11)))))),
                                        multiply(X_10,
                                          multiply(identity,
                                            multiply(multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11, X_11))),
                                              multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11,
                                                    X_11))))))))),
                                  multiply(X_4,
                                    multiply(identity,
                                      multiply(multiply(X_10,
                                          multiply(identity,
                                            multiply(multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11, X_11))),
                                              multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11,
                                                    X_11)))))),
                                        multiply(X_10,
                                          multiply(identity,
                                            multiply(multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11, X_11))),
                                              multiply(multiply(X_4,
                                                  multiply(X_10, X_11)),
                                                multiply(identity,
                                                  multiply(X_11,
                                                    X_11)))))))))))))),
                        X_4)), [0],
                 $fot(multiply(X_4,
                        multiply(X_4,
                          multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_4,
                                  multiply(identity,
                                    multiply(multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(X_4,
                                              multiply(multiply(X_10,
                                                  X_11),
                                                multiply(X_11, X_11))),
                                            multiply(X_4,
                                              multiply(multiply(X_10,
                                                  X_11),
                                                multiply(X_11, X_11)))))),
                                      multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(X_4,
                                              multiply(multiply(X_10,
                                                  X_11),
                                                multiply(X_11, X_11))),
                                            multiply(X_4,
                                              multiply(multiply(X_10,
                                                  X_11),
                                                multiply(X_11,
                                                  X_11))))))))),
                                multiply(X_4,
                                  multiply(identity,
                                    multiply(multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(X_4,
                                              multiply(multiply(X_10,
                                                  X_11),
                                                multiply(X_11, X_11))),
                                            multiply(X_4,
                                              multiply(multiply(X_10,
                                                  X_11),
                                                multiply(X_11, X_11)))))),
                                      multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(X_4,
                                              multiply(multiply(X_10,
                                                  X_11),
                                                multiply(X_11, X_11))),
                                            multiply(X_4,
                                              multiply(multiply(X_10,
                                                  X_11),
                                                multiply(X_11,
                                                  X_11)))))))))))))))]])).

cnf(refute_0_151, plain,
    (multiply(X_4,
       multiply(X_4,
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11))))))))),
               multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(multiply(X_4,
                               multiply(X_10, X_11)),
                             multiply(identity, multiply(X_11, X_11))),
                           multiply(multiply(X_4, multiply(X_10, X_11)),
                             multiply(identity,
                               multiply(X_11, X_11)))))))))))))) != X_4 |
     multiply(X_4,
       multiply(X_4,
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))),
                           multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))),
                           multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))))))))),
               multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))),
                           multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))),
                           multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11)))))))))))))) = X_4),
    inference(resolve,
              [$cnf($equal(multiply(X_4,
                        multiply(X_4,
                          multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_4,
                                  multiply(identity,
                                    multiply(multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11)))))),
                                      multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11,
                                                  X_11))))))))),
                                multiply(X_4,
                                  multiply(identity,
                                    multiply(multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11)))))),
                                      multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11,
                                                  X_11)))))))))))))),
                      multiply(X_4,
                        multiply(X_4,
                          multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_4,
                                  multiply(identity,
                                    multiply(multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(X_4,
                                              multiply(multiply(X_10,
                                                  X_11),
                                                multiply(X_11, X_11))),
                                            multiply(X_4,
                                              multiply(multiply(X_10,
                                                  X_11),
                                                multiply(X_11, X_11)))))),
                                      multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(X_4,
                                              multiply(multiply(X_10,
                                                  X_11),
                                                multiply(X_11, X_11))),
                                            multiply(X_4,
                                              multiply(multiply(X_10,
                                                  X_11),
                                                multiply(X_11,
                                                  X_11))))))))),
                                multiply(X_4,
                                  multiply(identity,
                                    multiply(multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(X_4,
                                              multiply(multiply(X_10,
                                                  X_11),
                                                multiply(X_11, X_11))),
                                            multiply(X_4,
                                              multiply(multiply(X_10,
                                                  X_11),
                                                multiply(X_11, X_11)))))),
                                      multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(X_4,
                                              multiply(multiply(X_10,
                                                  X_11),
                                                multiply(X_11, X_11))),
                                            multiply(X_4,
                                              multiply(multiply(X_10,
                                                  X_11),
                                                multiply(X_11,
                                                  X_11))))))))))))))))],
              [refute_0_149, refute_0_150])).

cnf(refute_0_152, plain,
    (multiply(X_4,
       multiply(X_4,
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))),
                           multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))),
                           multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))))))))),
               multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))),
                           multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))),
                           multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11)))))))))))))) = X_4),
    inference(resolve,
              [$cnf($equal(multiply(X_4,
                        multiply(X_4,
                          multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_4,
                                  multiply(identity,
                                    multiply(multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11)))))),
                                      multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11,
                                                  X_11))))))))),
                                multiply(X_4,
                                  multiply(identity,
                                    multiply(multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11)))))),
                                      multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11, X_11))),
                                            multiply(multiply(X_4,
                                                multiply(X_10, X_11)),
                                              multiply(identity,
                                                multiply(X_11,
                                                  X_11)))))))))))))),
                      X_4))], [refute_0_54, refute_0_151])).

cnf(refute_0_153, plain,
    (multiply(multiply(X, multiply(Y, Z)),
       multiply(identity, multiply(Z, Z))) =
     multiply(X, multiply(multiply(Y, Z), multiply(Z, Z)))),
    inference(subst, [],
              [refute_0_81 :
               [bind(X_1, $fot(multiply(Y, Z))), bind(X_14, $fot(Z))]])).

cnf(refute_0_154, plain,
    (multiply(X,
       multiply(multiply(X, multiply(Y, Z)),
         multiply(identity, multiply(Z, Z)))) =
     multiply(X,
       multiply(multiply(X, multiply(Y, Z)),
         multiply(identity, multiply(Z, Z))))),
    introduced(tautology,
               [refl,
                [$fot(multiply(X,
                        multiply(multiply(X, multiply(Y, Z)),
                          multiply(identity, multiply(Z, Z)))))]])).

cnf(refute_0_155, plain,
    (multiply(X,
       multiply(multiply(X, multiply(Y, Z)),
         multiply(identity, multiply(Z, Z)))) !=
     multiply(X,
       multiply(multiply(X, multiply(Y, Z)),
         multiply(identity, multiply(Z, Z)))) |
     multiply(multiply(X, multiply(Y, Z)),
       multiply(identity, multiply(Z, Z))) !=
     multiply(X, multiply(multiply(Y, Z), multiply(Z, Z))) |
     multiply(X,
       multiply(multiply(X, multiply(Y, Z)),
         multiply(identity, multiply(Z, Z)))) =
     multiply(X, multiply(X, multiply(multiply(Y, Z), multiply(Z, Z))))),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(X,
                          multiply(multiply(X, multiply(Y, Z)),
                            multiply(identity, multiply(Z, Z)))),
                        multiply(X,
                          multiply(multiply(X, multiply(Y, Z)),
                            multiply(identity, multiply(Z, Z)))))), [1, 1],
                 $fot(multiply(X,
                        multiply(multiply(Y, Z), multiply(Z, Z))))]])).

cnf(refute_0_156, plain,
    (multiply(multiply(X, multiply(Y, Z)),
       multiply(identity, multiply(Z, Z))) !=
     multiply(X, multiply(multiply(Y, Z), multiply(Z, Z))) |
     multiply(X,
       multiply(multiply(X, multiply(Y, Z)),
         multiply(identity, multiply(Z, Z)))) =
     multiply(X, multiply(X, multiply(multiply(Y, Z), multiply(Z, Z))))),
    inference(resolve,
              [$cnf($equal(multiply(X,
                        multiply(multiply(X, multiply(Y, Z)),
                          multiply(identity, multiply(Z, Z)))),
                      multiply(X,
                        multiply(multiply(X, multiply(Y, Z)),
                          multiply(identity, multiply(Z, Z))))))],
              [refute_0_154, refute_0_155])).

cnf(refute_0_157, plain,
    (multiply(X,
       multiply(multiply(X, multiply(Y, Z)),
         multiply(identity, multiply(Z, Z)))) =
     multiply(X, multiply(X, multiply(multiply(Y, Z), multiply(Z, Z))))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(X, multiply(Y, Z)),
                        multiply(identity, multiply(Z, Z))),
                      multiply(X,
                        multiply(multiply(Y, Z), multiply(Z, Z)))))],
              [refute_0_153, refute_0_156])).

cnf(refute_0_158, plain,
    (multiply(X,
       multiply(X,
         multiply(multiply(X, multiply(Y, Z)),
           multiply(identity, multiply(Z, Z))))) =
     multiply(X,
       multiply(X,
         multiply(multiply(X, multiply(Y, Z)),
           multiply(identity, multiply(Z, Z)))))),
    introduced(tautology,
               [refl,
                [$fot(multiply(X,
                        multiply(X,
                          multiply(multiply(X, multiply(Y, Z)),
                            multiply(identity, multiply(Z, Z))))))]])).

cnf(refute_0_159, plain,
    (multiply(X,
       multiply(X,
         multiply(multiply(X, multiply(Y, Z)),
           multiply(identity, multiply(Z, Z))))) !=
     multiply(X,
       multiply(X,
         multiply(multiply(X, multiply(Y, Z)),
           multiply(identity, multiply(Z, Z))))) |
     multiply(X,
       multiply(multiply(X, multiply(Y, Z)),
         multiply(identity, multiply(Z, Z)))) !=
     multiply(X, multiply(X, multiply(multiply(Y, Z), multiply(Z, Z)))) |
     multiply(X,
       multiply(X,
         multiply(multiply(X, multiply(Y, Z)),
           multiply(identity, multiply(Z, Z))))) =
     multiply(X,
       multiply(X,
         multiply(X, multiply(multiply(Y, Z), multiply(Z, Z)))))),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(X,
                          multiply(X,
                            multiply(multiply(X, multiply(Y, Z)),
                              multiply(identity, multiply(Z, Z))))),
                        multiply(X,
                          multiply(X,
                            multiply(multiply(X, multiply(Y, Z)),
                              multiply(identity, multiply(Z, Z))))))),
                 [1, 1],
                 $fot(multiply(X,
                        multiply(X,
                          multiply(multiply(Y, Z), multiply(Z, Z)))))]])).

cnf(refute_0_160, plain,
    (multiply(X,
       multiply(multiply(X, multiply(Y, Z)),
         multiply(identity, multiply(Z, Z)))) !=
     multiply(X, multiply(X, multiply(multiply(Y, Z), multiply(Z, Z)))) |
     multiply(X,
       multiply(X,
         multiply(multiply(X, multiply(Y, Z)),
           multiply(identity, multiply(Z, Z))))) =
     multiply(X,
       multiply(X,
         multiply(X, multiply(multiply(Y, Z), multiply(Z, Z)))))),
    inference(resolve,
              [$cnf($equal(multiply(X,
                        multiply(X,
                          multiply(multiply(X, multiply(Y, Z)),
                            multiply(identity, multiply(Z, Z))))),
                      multiply(X,
                        multiply(X,
                          multiply(multiply(X, multiply(Y, Z)),
                            multiply(identity, multiply(Z, Z)))))))],
              [refute_0_158, refute_0_159])).

cnf(refute_0_161, plain,
    (multiply(X,
       multiply(X,
         multiply(multiply(X, multiply(Y, Z)),
           multiply(identity, multiply(Z, Z))))) =
     multiply(X,
       multiply(X,
         multiply(X, multiply(multiply(Y, Z), multiply(Z, Z)))))),
    inference(resolve,
              [$cnf($equal(multiply(X,
                        multiply(multiply(X, multiply(Y, Z)),
                          multiply(identity, multiply(Z, Z)))),
                      multiply(X,
                        multiply(X,
                          multiply(multiply(Y, Z), multiply(Z, Z))))))],
              [refute_0_157, refute_0_160])).

cnf(refute_0_162, plain,
    (multiply(X,
       multiply(X,
         multiply(multiply(X, multiply(Y, Z)),
           multiply(identity, multiply(Z, Z))))) != Y |
     multiply(X,
       multiply(X,
         multiply(multiply(X, multiply(Y, Z)),
           multiply(identity, multiply(Z, Z))))) !=
     multiply(X,
       multiply(X,
         multiply(X, multiply(multiply(Y, Z), multiply(Z, Z))))) |
     multiply(X,
       multiply(X,
         multiply(X, multiply(multiply(Y, Z), multiply(Z, Z))))) = Y),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(X,
                          multiply(X,
                            multiply(multiply(X, multiply(Y, Z)),
                              multiply(identity, multiply(Z, Z))))), Y)),
                 [0],
                 $fot(multiply(X,
                        multiply(X,
                          multiply(X,
                            multiply(multiply(Y, Z),
                              multiply(Z, Z))))))]])).

cnf(refute_0_163, plain,
    (multiply(X,
       multiply(X,
         multiply(multiply(X, multiply(Y, Z)),
           multiply(identity, multiply(Z, Z))))) != Y |
     multiply(X,
       multiply(X,
         multiply(X, multiply(multiply(Y, Z), multiply(Z, Z))))) = Y),
    inference(resolve,
              [$cnf($equal(multiply(X,
                        multiply(X,
                          multiply(multiply(X, multiply(Y, Z)),
                            multiply(identity, multiply(Z, Z))))),
                      multiply(X,
                        multiply(X,
                          multiply(X,
                            multiply(multiply(Y, Z), multiply(Z, Z)))))))],
              [refute_0_161, refute_0_162])).

cnf(refute_0_164, plain,
    (multiply(X,
       multiply(X,
         multiply(X, multiply(multiply(Y, Z), multiply(Z, Z))))) = Y),
    inference(resolve,
              [$cnf($equal(multiply(X,
                        multiply(X,
                          multiply(multiply(X, multiply(Y, Z)),
                            multiply(identity, multiply(Z, Z))))), Y))],
              [refute_0_17, refute_0_163])).

cnf(refute_0_165, plain,
    (multiply(multiply(X_12, identity),
       multiply(multiply(X_12, identity),
         multiply(multiply(X_12, identity),
           multiply(multiply(X_18, X_19), multiply(X_19, X_19))))) = X_18),
    inference(subst, [],
              [refute_0_164 :
               [bind(X, $fot(multiply(X_12, identity))),
                bind(Y, $fot(X_18)), bind(Z, $fot(X_19))]])).

cnf(refute_0_166, plain,
    (multiply(X_12,
       multiply(identity,
         multiply(identity,
           multiply(multiply(identity, multiply(X_14, X_14)),
             multiply(identity, multiply(X_14, X_14)))))) =
     multiply(multiply(X_12, identity), X_14)),
    inference(subst, [],
              [refute_0_58 :
               [bind(X, $fot(X_12)), bind(X_1, $fot(identity)),
                bind(X_2, $fot(X_14))]])).

cnf(refute_0_167, plain,
    (multiply(X_12,
       multiply(identity,
         multiply(identity,
           multiply(multiply(identity, multiply(X_14, X_14)),
             multiply(identity, multiply(X_14, X_14)))))) !=
     multiply(multiply(X_12, identity), X_14) |
     multiply(identity,
       multiply(identity,
         multiply(multiply(identity, multiply(X_14, X_14)),
           multiply(identity, multiply(X_14, X_14))))) != X_14 |
     multiply(X_12, X_14) = multiply(multiply(X_12, identity), X_14)),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(X_12,
                          multiply(identity,
                            multiply(identity,
                              multiply(multiply(identity,
                                  multiply(X_14, X_14)),
                                multiply(identity,
                                  multiply(X_14, X_14)))))),
                        multiply(multiply(X_12, identity), X_14))), [0, 1],
                 $fot(X_14)]])).

cnf(refute_0_168, plain,
    (multiply(X_12,
       multiply(identity,
         multiply(identity,
           multiply(multiply(identity, multiply(X_14, X_14)),
             multiply(identity, multiply(X_14, X_14)))))) !=
     multiply(multiply(X_12, identity), X_14) |
     multiply(X_12, X_14) = multiply(multiply(X_12, identity), X_14)),
    inference(resolve,
              [$cnf($equal(multiply(identity,
                        multiply(identity,
                          multiply(multiply(identity,
                              multiply(X_14, X_14)),
                            multiply(identity, multiply(X_14, X_14))))),
                      X_14))], [refute_0_64, refute_0_167])).

cnf(refute_0_169, plain,
    (multiply(X_12, X_14) = multiply(multiply(X_12, identity), X_14)),
    inference(resolve,
              [$cnf($equal(multiply(X_12,
                        multiply(identity,
                          multiply(identity,
                            multiply(multiply(identity,
                                multiply(X_14, X_14)),
                              multiply(identity, multiply(X_14, X_14)))))),
                      multiply(multiply(X_12, identity), X_14)))],
              [refute_0_166, refute_0_168])).

cnf(refute_0_170, plain,
    (multiply(X_12, multiply(multiply(X_18, X_19), multiply(X_19, X_19))) =
     multiply(multiply(X_12, identity),
       multiply(multiply(X_18, X_19), multiply(X_19, X_19)))),
    inference(subst, [],
              [refute_0_169 :
               [bind(X_14,
                     $fot(multiply(multiply(X_18, X_19),
                            multiply(X_19, X_19))))]])).

cnf(refute_0_171, plain,
    (multiply(X_12,
       multiply(multiply(X_18, X_19), multiply(X_19, X_19))) !=
     multiply(multiply(X_12, identity),
       multiply(multiply(X_18, X_19), multiply(X_19, X_19))) |
     multiply(multiply(X_12, identity),
       multiply(multiply(X_18, X_19), multiply(X_19, X_19))) =
     multiply(X_12, multiply(multiply(X_18, X_19), multiply(X_19, X_19)))),
    inference(subst, [],
              [refute_0_7 :
               [bind(X0,
                     $fot(multiply(X_12,
                            multiply(multiply(X_18, X_19),
                              multiply(X_19, X_19))))),
                bind(Y0,
                     $fot(multiply(multiply(X_12, identity),
                            multiply(multiply(X_18, X_19),
                              multiply(X_19, X_19)))))]])).

cnf(refute_0_172, plain,
    (multiply(multiply(X_12, identity),
       multiply(multiply(X_18, X_19), multiply(X_19, X_19))) =
     multiply(X_12, multiply(multiply(X_18, X_19), multiply(X_19, X_19)))),
    inference(resolve,
              [$cnf($equal(multiply(X_12,
                        multiply(multiply(X_18, X_19),
                          multiply(X_19, X_19))),
                      multiply(multiply(X_12, identity),
                        multiply(multiply(X_18, X_19),
                          multiply(X_19, X_19)))))],
              [refute_0_170, refute_0_171])).

cnf(refute_0_173, plain,
    (multiply(multiply(X_12, identity),
       multiply(multiply(X_12, identity),
         multiply(multiply(X_12, identity),
           multiply(multiply(X_18, X_19), multiply(X_19, X_19))))) !=
     X_18 |
     multiply(multiply(X_12, identity),
       multiply(multiply(X_18, X_19), multiply(X_19, X_19))) !=
     multiply(X_12, multiply(multiply(X_18, X_19), multiply(X_19, X_19))) |
     multiply(multiply(X_12, identity),
       multiply(multiply(X_12, identity),
         multiply(X_12,
           multiply(multiply(X_18, X_19), multiply(X_19, X_19))))) = X_18),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(multiply(X_12, identity),
                          multiply(multiply(X_12, identity),
                            multiply(multiply(X_12, identity),
                              multiply(multiply(X_18, X_19),
                                multiply(X_19, X_19))))), X_18)),
                 [0, 1, 1],
                 $fot(multiply(X_12,
                        multiply(multiply(X_18, X_19),
                          multiply(X_19, X_19))))]])).

cnf(refute_0_174, plain,
    (multiply(multiply(X_12, identity),
       multiply(multiply(X_12, identity),
         multiply(multiply(X_12, identity),
           multiply(multiply(X_18, X_19), multiply(X_19, X_19))))) !=
     X_18 |
     multiply(multiply(X_12, identity),
       multiply(multiply(X_12, identity),
         multiply(X_12,
           multiply(multiply(X_18, X_19), multiply(X_19, X_19))))) = X_18),
    inference(resolve,
              [$cnf($equal(multiply(multiply(X_12, identity),
                        multiply(multiply(X_18, X_19),
                          multiply(X_19, X_19))),
                      multiply(X_12,
                        multiply(multiply(X_18, X_19),
                          multiply(X_19, X_19)))))],
              [refute_0_172, refute_0_173])).

cnf(refute_0_175, plain,
    (multiply(multiply(X_12, identity),
       multiply(multiply(X_12, identity),
         multiply(X_12,
           multiply(multiply(X_18, X_19), multiply(X_19, X_19))))) = X_18),
    inference(resolve,
              [$cnf($equal(multiply(multiply(X_12, identity),
                        multiply(multiply(X_12, identity),
                          multiply(multiply(X_12, identity),
                            multiply(multiply(X_18, X_19),
                              multiply(X_19, X_19))))), X_18))],
              [refute_0_165, refute_0_174])).

cnf(refute_0_176, plain,
    (multiply(X_17,
       multiply(X_17,
         multiply(X_17,
           multiply(multiply(X_18, identity),
             multiply(identity, identity))))) = X_18),
    inference(subst, [],
              [refute_0_164 :
               [bind(X, $fot(X_17)), bind(Y, $fot(X_18)),
                bind(Z, $fot(identity))]])).

cnf(refute_0_177, plain,
    (multiply(X_18, multiply(identity, identity)) =
     multiply(multiply(X_18, identity), multiply(identity, identity))),
    inference(subst, [],
              [refute_0_169 :
               [bind(X_12, $fot(X_18)),
                bind(X_14, $fot(multiply(identity, identity)))]])).

cnf(refute_0_178, plain,
    (multiply(X_18, multiply(identity, identity)) !=
     multiply(multiply(X_18, identity), multiply(identity, identity)) |
     multiply(multiply(X_18, identity), multiply(identity, identity)) =
     multiply(X_18, multiply(identity, identity))),
    inference(subst, [],
              [refute_0_7 :
               [bind(X0,
                     $fot(multiply(X_18, multiply(identity, identity)))),
                bind(Y0,
                     $fot(multiply(multiply(X_18, identity),
                            multiply(identity, identity))))]])).

cnf(refute_0_179, plain,
    (multiply(multiply(X_18, identity), multiply(identity, identity)) =
     multiply(X_18, multiply(identity, identity))),
    inference(resolve,
              [$cnf($equal(multiply(X_18, multiply(identity, identity)),
                      multiply(multiply(X_18, identity),
                        multiply(identity, identity))))],
              [refute_0_177, refute_0_178])).

cnf(refute_0_180, plain,
    (multiply(X_17,
       multiply(X_17,
         multiply(X_17,
           multiply(multiply(X_18, identity),
             multiply(identity, identity))))) != X_18 |
     multiply(multiply(X_18, identity), multiply(identity, identity)) !=
     multiply(X_18, multiply(identity, identity)) |
     multiply(X_17,
       multiply(X_17,
         multiply(X_17, multiply(X_18, multiply(identity, identity))))) =
     X_18),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(X_17,
                          multiply(X_17,
                            multiply(X_17,
                              multiply(multiply(X_18, identity),
                                multiply(identity, identity))))), X_18)),
                 [0, 1, 1, 1],
                 $fot(multiply(X_18, multiply(identity, identity)))]])).

cnf(refute_0_181, plain,
    (multiply(X_17,
       multiply(X_17,
         multiply(X_17,
           multiply(multiply(X_18, identity),
             multiply(identity, identity))))) != X_18 |
     multiply(X_17,
       multiply(X_17,
         multiply(X_17, multiply(X_18, multiply(identity, identity))))) =
     X_18),
    inference(resolve,
              [$cnf($equal(multiply(multiply(X_18, identity),
                        multiply(identity, identity)),
                      multiply(X_18, multiply(identity, identity))))],
              [refute_0_179, refute_0_180])).

cnf(refute_0_182, plain,
    (multiply(X_17,
       multiply(X_17,
         multiply(X_17, multiply(X_18, multiply(identity, identity))))) =
     X_18),
    inference(resolve,
              [$cnf($equal(multiply(X_17,
                        multiply(X_17,
                          multiply(X_17,
                            multiply(multiply(X_18, identity),
                              multiply(identity, identity))))), X_18))],
              [refute_0_176, refute_0_181])).

cnf(refute_0_183, plain,
    (multiply(X_17,
       multiply(X_17,
         multiply(X_17,
           multiply(multiply(multiply(X_12, identity), X_19),
             multiply(X_19, X_19))))) = multiply(X_12, identity)),
    inference(subst, [],
              [refute_0_164 :
               [bind(X, $fot(X_17)),
                bind(Y, $fot(multiply(X_12, identity))),
                bind(Z, $fot(X_19))]])).

cnf(refute_0_184, plain,
    (multiply(X_12, X_19) = multiply(multiply(X_12, identity), X_19)),
    inference(subst, [], [refute_0_169 : [bind(X_14, $fot(X_19))]])).

cnf(refute_0_185, plain,
    (multiply(X_12, X_19) != multiply(multiply(X_12, identity), X_19) |
     multiply(multiply(X_12, identity), X_19) = multiply(X_12, X_19)),
    inference(subst, [],
              [refute_0_7 :
               [bind(X0, $fot(multiply(X_12, X_19))),
                bind(Y0,
                     $fot(multiply(multiply(X_12, identity), X_19)))]])).

cnf(refute_0_186, plain,
    (multiply(multiply(X_12, identity), X_19) = multiply(X_12, X_19)),
    inference(resolve,
              [$cnf($equal(multiply(X_12, X_19),
                      multiply(multiply(X_12, identity), X_19)))],
              [refute_0_184, refute_0_185])).

cnf(refute_0_187, plain,
    (multiply(X_17,
       multiply(X_17,
         multiply(X_17,
           multiply(multiply(multiply(X_12, identity), X_19),
             multiply(X_19, X_19))))) != multiply(X_12, identity) |
     multiply(multiply(X_12, identity), X_19) != multiply(X_12, X_19) |
     multiply(X_17,
       multiply(X_17,
         multiply(X_17,
           multiply(multiply(X_12, X_19), multiply(X_19, X_19))))) =
     multiply(X_12, identity)),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(X_17,
                          multiply(X_17,
                            multiply(X_17,
                              multiply(multiply(multiply(X_12, identity),
                                  X_19), multiply(X_19, X_19))))),
                        multiply(X_12, identity))), [0, 1, 1, 1, 0],
                 $fot(multiply(X_12, X_19))]])).

cnf(refute_0_188, plain,
    (multiply(X_17,
       multiply(X_17,
         multiply(X_17,
           multiply(multiply(multiply(X_12, identity), X_19),
             multiply(X_19, X_19))))) != multiply(X_12, identity) |
     multiply(X_17,
       multiply(X_17,
         multiply(X_17,
           multiply(multiply(X_12, X_19), multiply(X_19, X_19))))) =
     multiply(X_12, identity)),
    inference(resolve,
              [$cnf($equal(multiply(multiply(X_12, identity), X_19),
                      multiply(X_12, X_19)))],
              [refute_0_186, refute_0_187])).

cnf(refute_0_189, plain,
    (multiply(X_17,
       multiply(X_17,
         multiply(X_17,
           multiply(multiply(X_12, X_19), multiply(X_19, X_19))))) =
     multiply(X_12, identity)),
    inference(resolve,
              [$cnf($equal(multiply(X_17,
                        multiply(X_17,
                          multiply(X_17,
                            multiply(multiply(multiply(X_12, identity),
                                X_19), multiply(X_19, X_19))))),
                      multiply(X_12, identity)))],
              [refute_0_183, refute_0_188])).

cnf(refute_0_190, plain,
    (multiply(X_17,
       multiply(X_17,
         multiply(X_17,
           multiply(multiply(X_12, X_19), multiply(X_19, X_19))))) = X_12),
    inference(subst, [],
              [refute_0_164 :
               [bind(X, $fot(X_17)), bind(Y, $fot(X_12)),
                bind(Z, $fot(X_19))]])).

cnf(refute_0_191, plain,
    (multiply(X_17,
       multiply(X_17,
         multiply(X_17,
           multiply(multiply(X_12, X_19), multiply(X_19, X_19))))) !=
     X_12 |
     multiply(X_17,
       multiply(X_17,
         multiply(X_17,
           multiply(multiply(X_12, X_19), multiply(X_19, X_19))))) !=
     multiply(X_12, identity) | X_12 = multiply(X_12, identity)),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(X_17,
                          multiply(X_17,
                            multiply(X_17,
                              multiply(multiply(X_12, X_19),
                                multiply(X_19, X_19))))),
                        multiply(X_12, identity))), [0], $fot(X_12)]])).

cnf(refute_0_192, plain,
    (multiply(X_17,
       multiply(X_17,
         multiply(X_17,
           multiply(multiply(X_12, X_19), multiply(X_19, X_19))))) !=
     multiply(X_12, identity) | X_12 = multiply(X_12, identity)),
    inference(resolve,
              [$cnf($equal(multiply(X_17,
                        multiply(X_17,
                          multiply(X_17,
                            multiply(multiply(X_12, X_19),
                              multiply(X_19, X_19))))), X_12))],
              [refute_0_190, refute_0_191])).

cnf(refute_0_193, plain, (X_12 = multiply(X_12, identity)),
    inference(resolve,
              [$cnf($equal(multiply(X_17,
                        multiply(X_17,
                          multiply(X_17,
                            multiply(multiply(X_12, X_19),
                              multiply(X_19, X_19))))),
                      multiply(X_12, identity)))],
              [refute_0_189, refute_0_192])).

cnf(refute_0_194, plain,
    (X_12 != multiply(X_12, identity) | multiply(X_12, identity) = X_12),
    inference(subst, [],
              [refute_0_7 :
               [bind(X0, $fot(X_12)),
                bind(Y0, $fot(multiply(X_12, identity)))]])).

cnf(refute_0_195, plain, (multiply(X_12, identity) = X_12),
    inference(resolve, [$cnf($equal(X_12, multiply(X_12, identity)))],
              [refute_0_193, refute_0_194])).

cnf(refute_0_196, plain, (multiply(X_18, identity) = X_18),
    inference(subst, [], [refute_0_195 : [bind(X_12, $fot(X_18))]])).

cnf(refute_0_197, plain, (multiply(identity, identity) = identity),
    inference(subst, [], [refute_0_195 : [bind(X_12, $fot(identity))]])).

cnf(refute_0_198, plain,
    (multiply(X_18, multiply(identity, identity)) =
     multiply(X_18, multiply(identity, identity))),
    introduced(tautology,
               [refl,
                [$fot(multiply(X_18, multiply(identity, identity)))]])).

cnf(refute_0_199, plain,
    (multiply(X_18, multiply(identity, identity)) !=
     multiply(X_18, multiply(identity, identity)) |
     multiply(identity, identity) != identity |
     multiply(X_18, multiply(identity, identity)) =
     multiply(X_18, identity)),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(X_18, multiply(identity, identity)),
                        multiply(X_18, multiply(identity, identity)))),
                 [1, 1], $fot(identity)]])).

cnf(refute_0_200, plain,
    (multiply(identity, identity) != identity |
     multiply(X_18, multiply(identity, identity)) =
     multiply(X_18, identity)),
    inference(resolve,
              [$cnf($equal(multiply(X_18, multiply(identity, identity)),
                      multiply(X_18, multiply(identity, identity))))],
              [refute_0_198, refute_0_199])).

cnf(refute_0_201, plain,
    (multiply(X_18, multiply(identity, identity)) =
     multiply(X_18, identity)),
    inference(resolve,
              [$cnf($equal(multiply(identity, identity), identity))],
              [refute_0_197, refute_0_200])).

cnf(refute_0_202, plain,
    (multiply(X_18, multiply(identity, identity)) !=
     multiply(X_18, identity) | multiply(X_18, identity) != X_18 |
     multiply(X_18, multiply(identity, identity)) = X_18),
    inference(subst, [],
              [refute_0_92 :
               [bind(X0,
                     $fot(multiply(X_18, multiply(identity, identity)))),
                bind(Y0, $fot(multiply(X_18, identity))),
                bind(Z0, $fot(X_18))]])).

cnf(refute_0_203, plain,
    (multiply(X_18, identity) != X_18 |
     multiply(X_18, multiply(identity, identity)) = X_18),
    inference(resolve,
              [$cnf($equal(multiply(X_18, multiply(identity, identity)),
                      multiply(X_18, identity)))],
              [refute_0_201, refute_0_202])).

cnf(refute_0_204, plain,
    (multiply(X_18, multiply(identity, identity)) = X_18),
    inference(resolve, [$cnf($equal(multiply(X_18, identity), X_18))],
              [refute_0_196, refute_0_203])).

cnf(refute_0_205, plain,
    (multiply(X_17, multiply(X_18, multiply(identity, identity))) =
     multiply(X_17, multiply(X_18, multiply(identity, identity)))),
    introduced(tautology,
               [refl,
                [$fot(multiply(X_17,
                        multiply(X_18, multiply(identity, identity))))]])).

cnf(refute_0_206, plain,
    (multiply(X_17, multiply(X_18, multiply(identity, identity))) !=
     multiply(X_17, multiply(X_18, multiply(identity, identity))) |
     multiply(X_18, multiply(identity, identity)) != X_18 |
     multiply(X_17, multiply(X_18, multiply(identity, identity))) =
     multiply(X_17, X_18)),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(X_17,
                          multiply(X_18, multiply(identity, identity))),
                        multiply(X_17,
                          multiply(X_18, multiply(identity, identity))))),
                 [1, 1], $fot(X_18)]])).

cnf(refute_0_207, plain,
    (multiply(X_18, multiply(identity, identity)) != X_18 |
     multiply(X_17, multiply(X_18, multiply(identity, identity))) =
     multiply(X_17, X_18)),
    inference(resolve,
              [$cnf($equal(multiply(X_17,
                        multiply(X_18, multiply(identity, identity))),
                      multiply(X_17,
                        multiply(X_18, multiply(identity, identity)))))],
              [refute_0_205, refute_0_206])).

cnf(refute_0_208, plain,
    (multiply(X_17, multiply(X_18, multiply(identity, identity))) =
     multiply(X_17, X_18)),
    inference(resolve,
              [$cnf($equal(multiply(X_18, multiply(identity, identity)),
                      X_18))], [refute_0_204, refute_0_207])).

cnf(refute_0_209, plain,
    (multiply(X_17,
       multiply(X_17, multiply(X_18, multiply(identity, identity)))) =
     multiply(X_17,
       multiply(X_17, multiply(X_18, multiply(identity, identity))))),
    introduced(tautology,
               [refl,
                [$fot(multiply(X_17,
                        multiply(X_17,
                          multiply(X_18,
                            multiply(identity, identity)))))]])).

cnf(refute_0_210, plain,
    (multiply(X_17,
       multiply(X_17, multiply(X_18, multiply(identity, identity)))) !=
     multiply(X_17,
       multiply(X_17, multiply(X_18, multiply(identity, identity)))) |
     multiply(X_17, multiply(X_18, multiply(identity, identity))) !=
     multiply(X_17, X_18) |
     multiply(X_17,
       multiply(X_17, multiply(X_18, multiply(identity, identity)))) =
     multiply(X_17, multiply(X_17, X_18))),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(X_17,
                          multiply(X_17,
                            multiply(X_18, multiply(identity, identity)))),
                        multiply(X_17,
                          multiply(X_17,
                            multiply(X_18,
                              multiply(identity, identity)))))), [1, 1],
                 $fot(multiply(X_17, X_18))]])).

cnf(refute_0_211, plain,
    (multiply(X_17, multiply(X_18, multiply(identity, identity))) !=
     multiply(X_17, X_18) |
     multiply(X_17,
       multiply(X_17, multiply(X_18, multiply(identity, identity)))) =
     multiply(X_17, multiply(X_17, X_18))),
    inference(resolve,
              [$cnf($equal(multiply(X_17,
                        multiply(X_17,
                          multiply(X_18, multiply(identity, identity)))),
                      multiply(X_17,
                        multiply(X_17,
                          multiply(X_18,
                            multiply(identity, identity))))))],
              [refute_0_209, refute_0_210])).

cnf(refute_0_212, plain,
    (multiply(X_17,
       multiply(X_17, multiply(X_18, multiply(identity, identity)))) =
     multiply(X_17, multiply(X_17, X_18))),
    inference(resolve,
              [$cnf($equal(multiply(X_17,
                        multiply(X_18, multiply(identity, identity))),
                      multiply(X_17, X_18)))],
              [refute_0_208, refute_0_211])).

cnf(refute_0_213, plain,
    (multiply(X_17,
       multiply(X_17,
         multiply(X_17, multiply(X_18, multiply(identity, identity))))) =
     multiply(X_17,
       multiply(X_17,
         multiply(X_17, multiply(X_18, multiply(identity, identity)))))),
    introduced(tautology,
               [refl,
                [$fot(multiply(X_17,
                        multiply(X_17,
                          multiply(X_17,
                            multiply(X_18,
                              multiply(identity, identity))))))]])).

cnf(refute_0_214, plain,
    (multiply(X_17,
       multiply(X_17,
         multiply(X_17, multiply(X_18, multiply(identity, identity))))) !=
     multiply(X_17,
       multiply(X_17,
         multiply(X_17, multiply(X_18, multiply(identity, identity))))) |
     multiply(X_17,
       multiply(X_17, multiply(X_18, multiply(identity, identity)))) !=
     multiply(X_17, multiply(X_17, X_18)) |
     multiply(X_17,
       multiply(X_17,
         multiply(X_17, multiply(X_18, multiply(identity, identity))))) =
     multiply(X_17, multiply(X_17, multiply(X_17, X_18)))),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(X_17,
                          multiply(X_17,
                            multiply(X_17,
                              multiply(X_18,
                                multiply(identity, identity))))),
                        multiply(X_17,
                          multiply(X_17,
                            multiply(X_17,
                              multiply(X_18,
                                multiply(identity, identity))))))), [1, 1],
                 $fot(multiply(X_17, multiply(X_17, X_18)))]])).

cnf(refute_0_215, plain,
    (multiply(X_17,
       multiply(X_17, multiply(X_18, multiply(identity, identity)))) !=
     multiply(X_17, multiply(X_17, X_18)) |
     multiply(X_17,
       multiply(X_17,
         multiply(X_17, multiply(X_18, multiply(identity, identity))))) =
     multiply(X_17, multiply(X_17, multiply(X_17, X_18)))),
    inference(resolve,
              [$cnf($equal(multiply(X_17,
                        multiply(X_17,
                          multiply(X_17,
                            multiply(X_18,
                              multiply(identity, identity))))),
                      multiply(X_17,
                        multiply(X_17,
                          multiply(X_17,
                            multiply(X_18,
                              multiply(identity, identity)))))))],
              [refute_0_213, refute_0_214])).

cnf(refute_0_216, plain,
    (multiply(X_17,
       multiply(X_17,
         multiply(X_17, multiply(X_18, multiply(identity, identity))))) =
     multiply(X_17, multiply(X_17, multiply(X_17, X_18)))),
    inference(resolve,
              [$cnf($equal(multiply(X_17,
                        multiply(X_17,
                          multiply(X_18, multiply(identity, identity)))),
                      multiply(X_17, multiply(X_17, X_18))))],
              [refute_0_212, refute_0_215])).

cnf(refute_0_217, plain,
    (multiply(X_17,
       multiply(X_17,
         multiply(X_17, multiply(X_18, multiply(identity, identity))))) !=
     X_18 |
     multiply(X_17,
       multiply(X_17,
         multiply(X_17, multiply(X_18, multiply(identity, identity))))) !=
     multiply(X_17, multiply(X_17, multiply(X_17, X_18))) |
     multiply(X_17, multiply(X_17, multiply(X_17, X_18))) = X_18),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(X_17,
                          multiply(X_17,
                            multiply(X_17,
                              multiply(X_18,
                                multiply(identity, identity))))), X_18)),
                 [0],
                 $fot(multiply(X_17,
                        multiply(X_17, multiply(X_17, X_18))))]])).

cnf(refute_0_218, plain,
    (multiply(X_17,
       multiply(X_17,
         multiply(X_17, multiply(X_18, multiply(identity, identity))))) !=
     X_18 | multiply(X_17, multiply(X_17, multiply(X_17, X_18))) = X_18),
    inference(resolve,
              [$cnf($equal(multiply(X_17,
                        multiply(X_17,
                          multiply(X_17,
                            multiply(X_18,
                              multiply(identity, identity))))),
                      multiply(X_17,
                        multiply(X_17, multiply(X_17, X_18)))))],
              [refute_0_216, refute_0_217])).

cnf(refute_0_219, plain,
    (multiply(X_17, multiply(X_17, multiply(X_17, X_18))) = X_18),
    inference(resolve,
              [$cnf($equal(multiply(X_17,
                        multiply(X_17,
                          multiply(X_17,
                            multiply(X_18,
                              multiply(identity, identity))))), X_18))],
              [refute_0_182, refute_0_218])).

cnf(refute_0_220, plain,
    (multiply(X_12,
       multiply(X_12,
         multiply(X_12,
           multiply(multiply(X_18, X_19), multiply(X_19, X_19))))) =
     multiply(multiply(X_18, X_19), multiply(X_19, X_19))),
    inference(subst, [],
              [refute_0_219 :
               [bind(X_17, $fot(X_12)),
                bind(X_18,
                     $fot(multiply(multiply(X_18, X_19),
                            multiply(X_19, X_19))))]])).

cnf(refute_0_221, plain,
    (multiply(X_12, X_14) != multiply(multiply(X_12, identity), X_14) |
     multiply(multiply(X_12, identity), X_14) = multiply(X_12, X_14)),
    inference(subst, [],
              [refute_0_7 :
               [bind(X0, $fot(multiply(X_12, X_14))),
                bind(Y0,
                     $fot(multiply(multiply(X_12, identity), X_14)))]])).

cnf(refute_0_222, plain,
    (multiply(multiply(X_12, identity), X_14) = multiply(X_12, X_14)),
    inference(resolve,
              [$cnf($equal(multiply(X_12, X_14),
                      multiply(multiply(X_12, identity), X_14)))],
              [refute_0_169, refute_0_221])).

cnf(refute_0_223, plain,
    (multiply(multiply(X_12, identity),
       multiply(X_12,
         multiply(multiply(X_18, X_19), multiply(X_19, X_19)))) =
     multiply(X_12,
       multiply(X_12,
         multiply(multiply(X_18, X_19), multiply(X_19, X_19))))),
    inference(subst, [],
              [refute_0_222 :
               [bind(X_14,
                     $fot(multiply(X_12,
                            multiply(multiply(X_18, X_19),
                              multiply(X_19, X_19)))))]])).

cnf(refute_0_224, plain,
    (multiply(X_12,
       multiply(multiply(X_12, identity),
         multiply(X_12,
           multiply(multiply(X_18, X_19), multiply(X_19, X_19))))) =
     multiply(X_12,
       multiply(multiply(X_12, identity),
         multiply(X_12,
           multiply(multiply(X_18, X_19), multiply(X_19, X_19)))))),
    introduced(tautology,
               [refl,
                [$fot(multiply(X_12,
                        multiply(multiply(X_12, identity),
                          multiply(X_12,
                            multiply(multiply(X_18, X_19),
                              multiply(X_19, X_19))))))]])).

cnf(refute_0_225, plain,
    (multiply(X_12,
       multiply(multiply(X_12, identity),
         multiply(X_12,
           multiply(multiply(X_18, X_19), multiply(X_19, X_19))))) !=
     multiply(X_12,
       multiply(multiply(X_12, identity),
         multiply(X_12,
           multiply(multiply(X_18, X_19), multiply(X_19, X_19))))) |
     multiply(multiply(X_12, identity),
       multiply(X_12,
         multiply(multiply(X_18, X_19), multiply(X_19, X_19)))) !=
     multiply(X_12,
       multiply(X_12,
         multiply(multiply(X_18, X_19), multiply(X_19, X_19)))) |
     multiply(X_12,
       multiply(multiply(X_12, identity),
         multiply(X_12,
           multiply(multiply(X_18, X_19), multiply(X_19, X_19))))) =
     multiply(X_12,
       multiply(X_12,
         multiply(X_12,
           multiply(multiply(X_18, X_19), multiply(X_19, X_19)))))),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(X_12,
                          multiply(multiply(X_12, identity),
                            multiply(X_12,
                              multiply(multiply(X_18, X_19),
                                multiply(X_19, X_19))))),
                        multiply(X_12,
                          multiply(multiply(X_12, identity),
                            multiply(X_12,
                              multiply(multiply(X_18, X_19),
                                multiply(X_19, X_19))))))), [1, 1],
                 $fot(multiply(X_12,
                        multiply(X_12,
                          multiply(multiply(X_18, X_19),
                            multiply(X_19, X_19)))))]])).

cnf(refute_0_226, plain,
    (multiply(multiply(X_12, identity),
       multiply(X_12,
         multiply(multiply(X_18, X_19), multiply(X_19, X_19)))) !=
     multiply(X_12,
       multiply(X_12,
         multiply(multiply(X_18, X_19), multiply(X_19, X_19)))) |
     multiply(X_12,
       multiply(multiply(X_12, identity),
         multiply(X_12,
           multiply(multiply(X_18, X_19), multiply(X_19, X_19))))) =
     multiply(X_12,
       multiply(X_12,
         multiply(X_12,
           multiply(multiply(X_18, X_19), multiply(X_19, X_19)))))),
    inference(resolve,
              [$cnf($equal(multiply(X_12,
                        multiply(multiply(X_12, identity),
                          multiply(X_12,
                            multiply(multiply(X_18, X_19),
                              multiply(X_19, X_19))))),
                      multiply(X_12,
                        multiply(multiply(X_12, identity),
                          multiply(X_12,
                            multiply(multiply(X_18, X_19),
                              multiply(X_19, X_19)))))))],
              [refute_0_224, refute_0_225])).

cnf(refute_0_227, plain,
    (multiply(X_12,
       multiply(multiply(X_12, identity),
         multiply(X_12,
           multiply(multiply(X_18, X_19), multiply(X_19, X_19))))) =
     multiply(X_12,
       multiply(X_12,
         multiply(X_12,
           multiply(multiply(X_18, X_19), multiply(X_19, X_19)))))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(X_12, identity),
                        multiply(X_12,
                          multiply(multiply(X_18, X_19),
                            multiply(X_19, X_19)))),
                      multiply(X_12,
                        multiply(X_12,
                          multiply(multiply(X_18, X_19),
                            multiply(X_19, X_19))))))],
              [refute_0_223, refute_0_226])).

cnf(refute_0_228, plain,
    (multiply(X_12,
       multiply(X_12,
         multiply(X_12,
           multiply(multiply(X_18, X_19), multiply(X_19, X_19))))) !=
     multiply(multiply(X_18, X_19), multiply(X_19, X_19)) |
     multiply(X_12,
       multiply(multiply(X_12, identity),
         multiply(X_12,
           multiply(multiply(X_18, X_19), multiply(X_19, X_19))))) !=
     multiply(X_12,
       multiply(X_12,
         multiply(X_12,
           multiply(multiply(X_18, X_19), multiply(X_19, X_19))))) |
     multiply(X_12,
       multiply(multiply(X_12, identity),
         multiply(X_12,
           multiply(multiply(X_18, X_19), multiply(X_19, X_19))))) =
     multiply(multiply(X_18, X_19), multiply(X_19, X_19))),
    inference(subst, [],
              [refute_0_92 :
               [bind(X0,
                     $fot(multiply(X_12,
                            multiply(multiply(X_12, identity),
                              multiply(X_12,
                                multiply(multiply(X_18, X_19),
                                  multiply(X_19, X_19))))))),
                bind(Y0,
                     $fot(multiply(X_12,
                            multiply(X_12,
                              multiply(X_12,
                                multiply(multiply(X_18, X_19),
                                  multiply(X_19, X_19))))))),
                bind(Z0,
                     $fot(multiply(multiply(X_18, X_19),
                            multiply(X_19, X_19))))]])).

cnf(refute_0_229, plain,
    (multiply(X_12,
       multiply(X_12,
         multiply(X_12,
           multiply(multiply(X_18, X_19), multiply(X_19, X_19))))) !=
     multiply(multiply(X_18, X_19), multiply(X_19, X_19)) |
     multiply(X_12,
       multiply(multiply(X_12, identity),
         multiply(X_12,
           multiply(multiply(X_18, X_19), multiply(X_19, X_19))))) =
     multiply(multiply(X_18, X_19), multiply(X_19, X_19))),
    inference(resolve,
              [$cnf($equal(multiply(X_12,
                        multiply(multiply(X_12, identity),
                          multiply(X_12,
                            multiply(multiply(X_18, X_19),
                              multiply(X_19, X_19))))),
                      multiply(X_12,
                        multiply(X_12,
                          multiply(X_12,
                            multiply(multiply(X_18, X_19),
                              multiply(X_19, X_19)))))))],
              [refute_0_227, refute_0_228])).

cnf(refute_0_230, plain,
    (multiply(X_12,
       multiply(multiply(X_12, identity),
         multiply(X_12,
           multiply(multiply(X_18, X_19), multiply(X_19, X_19))))) =
     multiply(multiply(X_18, X_19), multiply(X_19, X_19))),
    inference(resolve,
              [$cnf($equal(multiply(X_12,
                        multiply(X_12,
                          multiply(X_12,
                            multiply(multiply(X_18, X_19),
                              multiply(X_19, X_19))))),
                      multiply(multiply(X_18, X_19),
                        multiply(X_19, X_19))))],
              [refute_0_220, refute_0_229])).

cnf(refute_0_231, plain,
    (multiply(multiply(X_12, identity),
       multiply(multiply(X_12, identity),
         multiply(X_12,
           multiply(multiply(X_18, X_19), multiply(X_19, X_19))))) =
     multiply(X_12,
       multiply(multiply(X_12, identity),
         multiply(X_12,
           multiply(multiply(X_18, X_19), multiply(X_19, X_19)))))),
    inference(subst, [],
              [refute_0_222 :
               [bind(X_14,
                     $fot(multiply(multiply(X_12, identity),
                            multiply(X_12,
                              multiply(multiply(X_18, X_19),
                                multiply(X_19, X_19))))))]])).

cnf(refute_0_232, plain,
    (multiply(X_12,
       multiply(multiply(X_12, identity),
         multiply(X_12,
           multiply(multiply(X_18, X_19), multiply(X_19, X_19))))) !=
     multiply(multiply(X_18, X_19), multiply(X_19, X_19)) |
     multiply(multiply(X_12, identity),
       multiply(multiply(X_12, identity),
         multiply(X_12,
           multiply(multiply(X_18, X_19), multiply(X_19, X_19))))) !=
     multiply(X_12,
       multiply(multiply(X_12, identity),
         multiply(X_12,
           multiply(multiply(X_18, X_19), multiply(X_19, X_19))))) |
     multiply(multiply(X_12, identity),
       multiply(multiply(X_12, identity),
         multiply(X_12,
           multiply(multiply(X_18, X_19), multiply(X_19, X_19))))) =
     multiply(multiply(X_18, X_19), multiply(X_19, X_19))),
    inference(subst, [],
              [refute_0_92 :
               [bind(X0,
                     $fot(multiply(multiply(X_12, identity),
                            multiply(multiply(X_12, identity),
                              multiply(X_12,
                                multiply(multiply(X_18, X_19),
                                  multiply(X_19, X_19))))))),
                bind(Y0,
                     $fot(multiply(X_12,
                            multiply(multiply(X_12, identity),
                              multiply(X_12,
                                multiply(multiply(X_18, X_19),
                                  multiply(X_19, X_19))))))),
                bind(Z0,
                     $fot(multiply(multiply(X_18, X_19),
                            multiply(X_19, X_19))))]])).

cnf(refute_0_233, plain,
    (multiply(X_12,
       multiply(multiply(X_12, identity),
         multiply(X_12,
           multiply(multiply(X_18, X_19), multiply(X_19, X_19))))) !=
     multiply(multiply(X_18, X_19), multiply(X_19, X_19)) |
     multiply(multiply(X_12, identity),
       multiply(multiply(X_12, identity),
         multiply(X_12,
           multiply(multiply(X_18, X_19), multiply(X_19, X_19))))) =
     multiply(multiply(X_18, X_19), multiply(X_19, X_19))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(X_12, identity),
                        multiply(multiply(X_12, identity),
                          multiply(X_12,
                            multiply(multiply(X_18, X_19),
                              multiply(X_19, X_19))))),
                      multiply(X_12,
                        multiply(multiply(X_12, identity),
                          multiply(X_12,
                            multiply(multiply(X_18, X_19),
                              multiply(X_19, X_19)))))))],
              [refute_0_231, refute_0_232])).

cnf(refute_0_234, plain,
    (multiply(multiply(X_12, identity),
       multiply(multiply(X_12, identity),
         multiply(X_12,
           multiply(multiply(X_18, X_19), multiply(X_19, X_19))))) =
     multiply(multiply(X_18, X_19), multiply(X_19, X_19))),
    inference(resolve,
              [$cnf($equal(multiply(X_12,
                        multiply(multiply(X_12, identity),
                          multiply(X_12,
                            multiply(multiply(X_18, X_19),
                              multiply(X_19, X_19))))),
                      multiply(multiply(X_18, X_19),
                        multiply(X_19, X_19))))],
              [refute_0_230, refute_0_233])).

cnf(refute_0_235, plain,
    (multiply(multiply(X_12, identity),
       multiply(multiply(X_12, identity),
         multiply(X_12,
           multiply(multiply(X_18, X_19), multiply(X_19, X_19))))) !=
     X_18 |
     multiply(multiply(X_12, identity),
       multiply(multiply(X_12, identity),
         multiply(X_12,
           multiply(multiply(X_18, X_19), multiply(X_19, X_19))))) !=
     multiply(multiply(X_18, X_19), multiply(X_19, X_19)) |
     multiply(multiply(X_18, X_19), multiply(X_19, X_19)) = X_18),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(multiply(X_12, identity),
                          multiply(multiply(X_12, identity),
                            multiply(X_12,
                              multiply(multiply(X_18, X_19),
                                multiply(X_19, X_19))))), X_18)), [0],
                 $fot(multiply(multiply(X_18, X_19),
                        multiply(X_19, X_19)))]])).

cnf(refute_0_236, plain,
    (multiply(multiply(X_12, identity),
       multiply(multiply(X_12, identity),
         multiply(X_12,
           multiply(multiply(X_18, X_19), multiply(X_19, X_19))))) !=
     X_18 | multiply(multiply(X_18, X_19), multiply(X_19, X_19)) = X_18),
    inference(resolve,
              [$cnf($equal(multiply(multiply(X_12, identity),
                        multiply(multiply(X_12, identity),
                          multiply(X_12,
                            multiply(multiply(X_18, X_19),
                              multiply(X_19, X_19))))),
                      multiply(multiply(X_18, X_19),
                        multiply(X_19, X_19))))],
              [refute_0_234, refute_0_235])).

cnf(refute_0_237, plain,
    (multiply(multiply(X_18, X_19), multiply(X_19, X_19)) = X_18),
    inference(resolve,
              [$cnf($equal(multiply(multiply(X_12, identity),
                        multiply(multiply(X_12, identity),
                          multiply(X_12,
                            multiply(multiply(X_18, X_19),
                              multiply(X_19, X_19))))), X_18))],
              [refute_0_175, refute_0_236])).

cnf(refute_0_238, plain,
    (multiply(multiply(X_10, X_11), multiply(X_11, X_11)) = X_10),
    inference(subst, [],
              [refute_0_237 :
               [bind(X_18, $fot(X_10)), bind(X_19, $fot(X_11))]])).

cnf(refute_0_239, plain,
    (multiply(X_4, multiply(multiply(X_10, X_11), multiply(X_11, X_11))) =
     multiply(X_4, multiply(multiply(X_10, X_11), multiply(X_11, X_11)))),
    introduced(tautology,
               [refl,
                [$fot(multiply(X_4,
                        multiply(multiply(X_10, X_11),
                          multiply(X_11, X_11))))]])).

cnf(refute_0_240, plain,
    (multiply(X_4, multiply(multiply(X_10, X_11), multiply(X_11, X_11))) !=
     multiply(X_4, multiply(multiply(X_10, X_11), multiply(X_11, X_11))) |
     multiply(multiply(X_10, X_11), multiply(X_11, X_11)) != X_10 |
     multiply(X_4, multiply(multiply(X_10, X_11), multiply(X_11, X_11))) =
     multiply(X_4, X_10)),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(X_4,
                          multiply(multiply(X_10, X_11),
                            multiply(X_11, X_11))),
                        multiply(X_4,
                          multiply(multiply(X_10, X_11),
                            multiply(X_11, X_11))))), [1, 1],
                 $fot(X_10)]])).

cnf(refute_0_241, plain,
    (multiply(multiply(X_10, X_11), multiply(X_11, X_11)) != X_10 |
     multiply(X_4, multiply(multiply(X_10, X_11), multiply(X_11, X_11))) =
     multiply(X_4, X_10)),
    inference(resolve,
              [$cnf($equal(multiply(X_4,
                        multiply(multiply(X_10, X_11),
                          multiply(X_11, X_11))),
                      multiply(X_4,
                        multiply(multiply(X_10, X_11),
                          multiply(X_11, X_11)))))],
              [refute_0_239, refute_0_240])).

cnf(refute_0_242, plain,
    (multiply(X_4, multiply(multiply(X_10, X_11), multiply(X_11, X_11))) =
     multiply(X_4, X_10)),
    inference(resolve,
              [$cnf($equal(multiply(multiply(X_10, X_11),
                        multiply(X_11, X_11)), X_10))],
              [refute_0_238, refute_0_241])).

cnf(refute_0_243, plain,
    (multiply(multiply(X_4, X_10),
       multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11)))) =
     multiply(multiply(X_4, X_10),
       multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))))),
    introduced(tautology,
               [refl,
                [$fot(multiply(multiply(X_4, X_10),
                        multiply(X_4,
                          multiply(multiply(X_10, X_11),
                            multiply(X_11, X_11)))))]])).

cnf(refute_0_244, plain,
    (multiply(X_4, multiply(multiply(X_10, X_11), multiply(X_11, X_11))) !=
     multiply(X_4, X_10) |
     multiply(multiply(X_4, X_10),
       multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11)))) !=
     multiply(multiply(X_4, X_10),
       multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11)))) |
     multiply(multiply(X_4, X_10),
       multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11)))) =
     multiply(multiply(X_4, X_10), multiply(X_4, X_10))),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(multiply(X_4, X_10),
                          multiply(X_4,
                            multiply(multiply(X_10, X_11),
                              multiply(X_11, X_11)))),
                        multiply(multiply(X_4, X_10),
                          multiply(X_4,
                            multiply(multiply(X_10, X_11),
                              multiply(X_11, X_11)))))), [1, 1],
                 $fot(multiply(X_4, X_10))]])).

cnf(refute_0_245, plain,
    (multiply(X_4, multiply(multiply(X_10, X_11), multiply(X_11, X_11))) !=
     multiply(X_4, X_10) |
     multiply(multiply(X_4, X_10),
       multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11)))) =
     multiply(multiply(X_4, X_10), multiply(X_4, X_10))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(X_4, X_10),
                        multiply(X_4,
                          multiply(multiply(X_10, X_11),
                            multiply(X_11, X_11)))),
                      multiply(multiply(X_4, X_10),
                        multiply(X_4,
                          multiply(multiply(X_10, X_11),
                            multiply(X_11, X_11))))))],
              [refute_0_243, refute_0_244])).

cnf(refute_0_246, plain,
    (multiply(multiply(X_4, X_10),
       multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11)))) =
     multiply(multiply(X_4, X_10), multiply(X_4, X_10))),
    inference(resolve,
              [$cnf($equal(multiply(X_4,
                        multiply(multiply(X_10, X_11),
                          multiply(X_11, X_11))), multiply(X_4, X_10)))],
              [refute_0_242, refute_0_245])).

cnf(refute_0_247, plain,
    (multiply(multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11)))) =
     multiply(multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))))),
    introduced(tautology,
               [refl,
                [$fot(multiply(multiply(X_4,
                          multiply(multiply(X_10, X_11),
                            multiply(X_11, X_11))),
                        multiply(X_4,
                          multiply(multiply(X_10, X_11),
                            multiply(X_11, X_11)))))]])).

cnf(refute_0_248, plain,
    (multiply(X_4, multiply(multiply(X_10, X_11), multiply(X_11, X_11))) !=
     multiply(X_4, X_10) |
     multiply(multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11)))) !=
     multiply(multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11)))) |
     multiply(multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11)))) =
     multiply(multiply(X_4, X_10),
       multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))))),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(multiply(X_4,
                            multiply(multiply(X_10, X_11),
                              multiply(X_11, X_11))),
                          multiply(X_4,
                            multiply(multiply(X_10, X_11),
                              multiply(X_11, X_11)))),
                        multiply(multiply(X_4,
                            multiply(multiply(X_10, X_11),
                              multiply(X_11, X_11))),
                          multiply(X_4,
                            multiply(multiply(X_10, X_11),
                              multiply(X_11, X_11)))))), [1, 0],
                 $fot(multiply(X_4, X_10))]])).

cnf(refute_0_249, plain,
    (multiply(X_4, multiply(multiply(X_10, X_11), multiply(X_11, X_11))) !=
     multiply(X_4, X_10) |
     multiply(multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11)))) =
     multiply(multiply(X_4, X_10),
       multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(X_4,
                          multiply(multiply(X_10, X_11),
                            multiply(X_11, X_11))),
                        multiply(X_4,
                          multiply(multiply(X_10, X_11),
                            multiply(X_11, X_11)))),
                      multiply(multiply(X_4,
                          multiply(multiply(X_10, X_11),
                            multiply(X_11, X_11))),
                        multiply(X_4,
                          multiply(multiply(X_10, X_11),
                            multiply(X_11, X_11))))))],
              [refute_0_247, refute_0_248])).

cnf(refute_0_250, plain,
    (multiply(multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11)))) =
     multiply(multiply(X_4, X_10),
       multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))))),
    inference(resolve,
              [$cnf($equal(multiply(X_4,
                        multiply(multiply(X_10, X_11),
                          multiply(X_11, X_11))), multiply(X_4, X_10)))],
              [refute_0_242, refute_0_249])).

cnf(refute_0_251, plain,
    (multiply(multiply(X_4, X_10),
       multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11)))) !=
     multiply(multiply(X_4, X_10), multiply(X_4, X_10)) |
     multiply(multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11)))) !=
     multiply(multiply(X_4, X_10),
       multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11)))) |
     multiply(multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11)))) =
     multiply(multiply(X_4, X_10), multiply(X_4, X_10))),
    inference(subst, [],
              [refute_0_92 :
               [bind(X0,
                     $fot(multiply(multiply(X_4,
                              multiply(multiply(X_10, X_11),
                                multiply(X_11, X_11))),
                            multiply(X_4,
                              multiply(multiply(X_10, X_11),
                                multiply(X_11, X_11)))))),
                bind(Y0,
                     $fot(multiply(multiply(X_4, X_10),
                            multiply(X_4,
                              multiply(multiply(X_10, X_11),
                                multiply(X_11, X_11)))))),
                bind(Z0,
                     $fot(multiply(multiply(X_4, X_10),
                            multiply(X_4, X_10))))]])).

cnf(refute_0_252, plain,
    (multiply(multiply(X_4, X_10),
       multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11)))) !=
     multiply(multiply(X_4, X_10), multiply(X_4, X_10)) |
     multiply(multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11)))) =
     multiply(multiply(X_4, X_10), multiply(X_4, X_10))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(X_4,
                          multiply(multiply(X_10, X_11),
                            multiply(X_11, X_11))),
                        multiply(X_4,
                          multiply(multiply(X_10, X_11),
                            multiply(X_11, X_11)))),
                      multiply(multiply(X_4, X_10),
                        multiply(X_4,
                          multiply(multiply(X_10, X_11),
                            multiply(X_11, X_11))))))],
              [refute_0_250, refute_0_251])).

cnf(refute_0_253, plain,
    (multiply(multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11)))) =
     multiply(multiply(X_4, X_10), multiply(X_4, X_10))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(X_4, X_10),
                        multiply(X_4,
                          multiply(multiply(X_10, X_11),
                            multiply(X_11, X_11)))),
                      multiply(multiply(X_4, X_10),
                        multiply(X_4, X_10))))],
              [refute_0_246, refute_0_252])).

cnf(refute_0_254, plain,
    (multiply(identity,
       multiply(multiply(X_4,
           multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
         multiply(X_4,
           multiply(multiply(X_10, X_11), multiply(X_11, X_11))))) =
     multiply(identity,
       multiply(multiply(X_4,
           multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
         multiply(X_4,
           multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
    introduced(tautology,
               [refl,
                [$fot(multiply(identity,
                        multiply(multiply(X_4,
                            multiply(multiply(X_10, X_11),
                              multiply(X_11, X_11))),
                          multiply(X_4,
                            multiply(multiply(X_10, X_11),
                              multiply(X_11, X_11))))))]])).

cnf(refute_0_255, plain,
    (multiply(multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11)))) !=
     multiply(multiply(X_4, X_10), multiply(X_4, X_10)) |
     multiply(identity,
       multiply(multiply(X_4,
           multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
         multiply(X_4,
           multiply(multiply(X_10, X_11), multiply(X_11, X_11))))) !=
     multiply(identity,
       multiply(multiply(X_4,
           multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
         multiply(X_4,
           multiply(multiply(X_10, X_11), multiply(X_11, X_11))))) |
     multiply(identity,
       multiply(multiply(X_4,
           multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
         multiply(X_4,
           multiply(multiply(X_10, X_11), multiply(X_11, X_11))))) =
     multiply(identity,
       multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(identity,
                          multiply(multiply(X_4,
                              multiply(multiply(X_10, X_11),
                                multiply(X_11, X_11))),
                            multiply(X_4,
                              multiply(multiply(X_10, X_11),
                                multiply(X_11, X_11))))),
                        multiply(identity,
                          multiply(multiply(X_4,
                              multiply(multiply(X_10, X_11),
                                multiply(X_11, X_11))),
                            multiply(X_4,
                              multiply(multiply(X_10, X_11),
                                multiply(X_11, X_11))))))), [1, 1],
                 $fot(multiply(multiply(X_4, X_10),
                        multiply(X_4, X_10)))]])).

cnf(refute_0_256, plain,
    (multiply(multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(X_4,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11)))) !=
     multiply(multiply(X_4, X_10), multiply(X_4, X_10)) |
     multiply(identity,
       multiply(multiply(X_4,
           multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
         multiply(X_4,
           multiply(multiply(X_10, X_11), multiply(X_11, X_11))))) =
     multiply(identity,
       multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
    inference(resolve,
              [$cnf($equal(multiply(identity,
                        multiply(multiply(X_4,
                            multiply(multiply(X_10, X_11),
                              multiply(X_11, X_11))),
                          multiply(X_4,
                            multiply(multiply(X_10, X_11),
                              multiply(X_11, X_11))))),
                      multiply(identity,
                        multiply(multiply(X_4,
                            multiply(multiply(X_10, X_11),
                              multiply(X_11, X_11))),
                          multiply(X_4,
                            multiply(multiply(X_10, X_11),
                              multiply(X_11, X_11)))))))],
              [refute_0_254, refute_0_255])).

cnf(refute_0_257, plain,
    (multiply(identity,
       multiply(multiply(X_4,
           multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
         multiply(X_4,
           multiply(multiply(X_10, X_11), multiply(X_11, X_11))))) =
     multiply(identity,
       multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(X_4,
                          multiply(multiply(X_10, X_11),
                            multiply(X_11, X_11))),
                        multiply(X_4,
                          multiply(multiply(X_10, X_11),
                            multiply(X_11, X_11)))),
                      multiply(multiply(X_4, X_10),
                        multiply(X_4, X_10))))],
              [refute_0_253, refute_0_256])).

cnf(refute_0_258, plain,
    (multiply(X_10,
       multiply(identity,
         multiply(multiply(X_4,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
           multiply(X_4,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))) =
     multiply(X_10,
       multiply(identity,
         multiply(multiply(X_4,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
           multiply(X_4,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11))))))),
    introduced(tautology,
               [refl,
                [$fot(multiply(X_10,
                        multiply(identity,
                          multiply(multiply(X_4,
                              multiply(multiply(X_10, X_11),
                                multiply(X_11, X_11))),
                            multiply(X_4,
                              multiply(multiply(X_10, X_11),
                                multiply(X_11, X_11)))))))]])).

cnf(refute_0_259, plain,
    (multiply(X_10,
       multiply(identity,
         multiply(multiply(X_4,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
           multiply(X_4,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))) !=
     multiply(X_10,
       multiply(identity,
         multiply(multiply(X_4,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
           multiply(X_4,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))) |
     multiply(identity,
       multiply(multiply(X_4,
           multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
         multiply(X_4,
           multiply(multiply(X_10, X_11), multiply(X_11, X_11))))) !=
     multiply(identity,
       multiply(multiply(X_4, X_10), multiply(X_4, X_10))) |
     multiply(X_10,
       multiply(identity,
         multiply(multiply(X_4,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
           multiply(X_4,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))) =
     multiply(X_10,
       multiply(identity,
         multiply(multiply(X_4, X_10), multiply(X_4, X_10))))),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(X_10,
                          multiply(identity,
                            multiply(multiply(X_4,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))),
                              multiply(X_4,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11)))))),
                        multiply(X_10,
                          multiply(identity,
                            multiply(multiply(X_4,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))),
                              multiply(X_4,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11)))))))), [1, 1],
                 $fot(multiply(identity,
                        multiply(multiply(X_4, X_10),
                          multiply(X_4, X_10))))]])).

cnf(refute_0_260, plain,
    (multiply(identity,
       multiply(multiply(X_4,
           multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
         multiply(X_4,
           multiply(multiply(X_10, X_11), multiply(X_11, X_11))))) !=
     multiply(identity,
       multiply(multiply(X_4, X_10), multiply(X_4, X_10))) |
     multiply(X_10,
       multiply(identity,
         multiply(multiply(X_4,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
           multiply(X_4,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))) =
     multiply(X_10,
       multiply(identity,
         multiply(multiply(X_4, X_10), multiply(X_4, X_10))))),
    inference(resolve,
              [$cnf($equal(multiply(X_10,
                        multiply(identity,
                          multiply(multiply(X_4,
                              multiply(multiply(X_10, X_11),
                                multiply(X_11, X_11))),
                            multiply(X_4,
                              multiply(multiply(X_10, X_11),
                                multiply(X_11, X_11)))))),
                      multiply(X_10,
                        multiply(identity,
                          multiply(multiply(X_4,
                              multiply(multiply(X_10, X_11),
                                multiply(X_11, X_11))),
                            multiply(X_4,
                              multiply(multiply(X_10, X_11),
                                multiply(X_11, X_11))))))))],
              [refute_0_258, refute_0_259])).

cnf(refute_0_261, plain,
    (multiply(X_10,
       multiply(identity,
         multiply(multiply(X_4,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
           multiply(X_4,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))) =
     multiply(X_10,
       multiply(identity,
         multiply(multiply(X_4, X_10), multiply(X_4, X_10))))),
    inference(resolve,
              [$cnf($equal(multiply(identity,
                        multiply(multiply(X_4,
                            multiply(multiply(X_10, X_11),
                              multiply(X_11, X_11))),
                          multiply(X_4,
                            multiply(multiply(X_10, X_11),
                              multiply(X_11, X_11))))),
                      multiply(identity,
                        multiply(multiply(X_4, X_10),
                          multiply(X_4, X_10)))))],
              [refute_0_257, refute_0_260])).

cnf(refute_0_262, plain,
    (multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))))))) =
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))))),
    introduced(tautology,
               [refl,
                [$fot(multiply(multiply(X_10,
                          multiply(identity,
                            multiply(multiply(X_4, X_10),
                              multiply(X_4, X_10)))),
                        multiply(X_10,
                          multiply(identity,
                            multiply(multiply(X_4,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))),
                              multiply(X_4,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))))))))]])).

cnf(refute_0_263, plain,
    (multiply(X_10,
       multiply(identity,
         multiply(multiply(X_4,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
           multiply(X_4,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))) !=
     multiply(X_10,
       multiply(identity,
         multiply(multiply(X_4, X_10), multiply(X_4, X_10)))) |
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))))))) !=
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))))))) |
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))))))) =
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4, X_10), multiply(X_4, X_10)))))),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(multiply(X_10,
                            multiply(identity,
                              multiply(multiply(X_4, X_10),
                                multiply(X_4, X_10)))),
                          multiply(X_10,
                            multiply(identity,
                              multiply(multiply(X_4,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11))),
                                multiply(X_4,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11))))))),
                        multiply(multiply(X_10,
                            multiply(identity,
                              multiply(multiply(X_4, X_10),
                                multiply(X_4, X_10)))),
                          multiply(X_10,
                            multiply(identity,
                              multiply(multiply(X_4,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11))),
                                multiply(X_4,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11))))))))), [1, 1],
                 $fot(multiply(X_10,
                        multiply(identity,
                          multiply(multiply(X_4, X_10),
                            multiply(X_4, X_10)))))]])).

cnf(refute_0_264, plain,
    (multiply(X_10,
       multiply(identity,
         multiply(multiply(X_4,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
           multiply(X_4,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))) !=
     multiply(X_10,
       multiply(identity,
         multiply(multiply(X_4, X_10), multiply(X_4, X_10)))) |
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))))))) =
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4, X_10), multiply(X_4, X_10)))))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(X_10,
                          multiply(identity,
                            multiply(multiply(X_4, X_10),
                              multiply(X_4, X_10)))),
                        multiply(X_10,
                          multiply(identity,
                            multiply(multiply(X_4,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))),
                              multiply(X_4,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))))))),
                      multiply(multiply(X_10,
                          multiply(identity,
                            multiply(multiply(X_4, X_10),
                              multiply(X_4, X_10)))),
                        multiply(X_10,
                          multiply(identity,
                            multiply(multiply(X_4,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))),
                              multiply(X_4,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11)))))))))],
              [refute_0_262, refute_0_263])).

cnf(refute_0_265, plain,
    (multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))))))) =
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4, X_10), multiply(X_4, X_10)))))),
    inference(resolve,
              [$cnf($equal(multiply(X_10,
                        multiply(identity,
                          multiply(multiply(X_4,
                              multiply(multiply(X_10, X_11),
                                multiply(X_11, X_11))),
                            multiply(X_4,
                              multiply(multiply(X_10, X_11),
                                multiply(X_11, X_11)))))),
                      multiply(X_10,
                        multiply(identity,
                          multiply(multiply(X_4, X_10),
                            multiply(X_4, X_10))))))],
              [refute_0_261, refute_0_264])).

cnf(refute_0_266, plain,
    (multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))))))) =
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))))),
    introduced(tautology,
               [refl,
                [$fot(multiply(multiply(X_10,
                          multiply(identity,
                            multiply(multiply(X_4,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))),
                              multiply(X_4,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11)))))),
                        multiply(X_10,
                          multiply(identity,
                            multiply(multiply(X_4,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))),
                              multiply(X_4,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))))))))]])).

cnf(refute_0_267, plain,
    (multiply(X_10,
       multiply(identity,
         multiply(multiply(X_4,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
           multiply(X_4,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))) !=
     multiply(X_10,
       multiply(identity,
         multiply(multiply(X_4, X_10), multiply(X_4, X_10)))) |
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))))))) !=
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))))))) |
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))))))) =
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))))),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(multiply(X_10,
                            multiply(identity,
                              multiply(multiply(X_4,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11))),
                                multiply(X_4,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11)))))),
                          multiply(X_10,
                            multiply(identity,
                              multiply(multiply(X_4,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11))),
                                multiply(X_4,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11))))))),
                        multiply(multiply(X_10,
                            multiply(identity,
                              multiply(multiply(X_4,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11))),
                                multiply(X_4,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11)))))),
                          multiply(X_10,
                            multiply(identity,
                              multiply(multiply(X_4,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11))),
                                multiply(X_4,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11))))))))), [1, 0],
                 $fot(multiply(X_10,
                        multiply(identity,
                          multiply(multiply(X_4, X_10),
                            multiply(X_4, X_10)))))]])).

cnf(refute_0_268, plain,
    (multiply(X_10,
       multiply(identity,
         multiply(multiply(X_4,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
           multiply(X_4,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))) !=
     multiply(X_10,
       multiply(identity,
         multiply(multiply(X_4, X_10), multiply(X_4, X_10)))) |
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))))))) =
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(X_10,
                          multiply(identity,
                            multiply(multiply(X_4,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))),
                              multiply(X_4,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11)))))),
                        multiply(X_10,
                          multiply(identity,
                            multiply(multiply(X_4,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))),
                              multiply(X_4,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))))))),
                      multiply(multiply(X_10,
                          multiply(identity,
                            multiply(multiply(X_4,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))),
                              multiply(X_4,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11)))))),
                        multiply(X_10,
                          multiply(identity,
                            multiply(multiply(X_4,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))),
                              multiply(X_4,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11)))))))))],
              [refute_0_266, refute_0_267])).

cnf(refute_0_269, plain,
    (multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))))))) =
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))))),
    inference(resolve,
              [$cnf($equal(multiply(X_10,
                        multiply(identity,
                          multiply(multiply(X_4,
                              multiply(multiply(X_10, X_11),
                                multiply(X_11, X_11))),
                            multiply(X_4,
                              multiply(multiply(X_10, X_11),
                                multiply(X_11, X_11)))))),
                      multiply(X_10,
                        multiply(identity,
                          multiply(multiply(X_4, X_10),
                            multiply(X_4, X_10))))))],
              [refute_0_261, refute_0_268])).

cnf(refute_0_270, plain,
    (multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))))))) !=
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4, X_10), multiply(X_4, X_10))))) |
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))))))) !=
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))))))) |
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))))))) =
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4, X_10), multiply(X_4, X_10)))))),
    inference(subst, [],
              [refute_0_92 :
               [bind(X0,
                     $fot(multiply(multiply(X_10,
                              multiply(identity,
                                multiply(multiply(X_4,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11))),
                                  multiply(X_4,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11)))))),
                            multiply(X_10,
                              multiply(identity,
                                multiply(multiply(X_4,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11))),
                                  multiply(X_4,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11))))))))),
                bind(Y0,
                     $fot(multiply(multiply(X_10,
                              multiply(identity,
                                multiply(multiply(X_4, X_10),
                                  multiply(X_4, X_10)))),
                            multiply(X_10,
                              multiply(identity,
                                multiply(multiply(X_4,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11))),
                                  multiply(X_4,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11))))))))),
                bind(Z0,
                     $fot(multiply(multiply(X_10,
                              multiply(identity,
                                multiply(multiply(X_4, X_10),
                                  multiply(X_4, X_10)))),
                            multiply(X_10,
                              multiply(identity,
                                multiply(multiply(X_4, X_10),
                                  multiply(X_4, X_10)))))))]])).

cnf(refute_0_271, plain,
    (multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))))))) !=
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4, X_10), multiply(X_4, X_10))))) |
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))))))) =
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4, X_10), multiply(X_4, X_10)))))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(X_10,
                          multiply(identity,
                            multiply(multiply(X_4,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))),
                              multiply(X_4,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11)))))),
                        multiply(X_10,
                          multiply(identity,
                            multiply(multiply(X_4,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))),
                              multiply(X_4,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))))))),
                      multiply(multiply(X_10,
                          multiply(identity,
                            multiply(multiply(X_4, X_10),
                              multiply(X_4, X_10)))),
                        multiply(X_10,
                          multiply(identity,
                            multiply(multiply(X_4,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))),
                              multiply(X_4,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11)))))))))],
              [refute_0_269, refute_0_270])).

cnf(refute_0_272, plain,
    (multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))))))) =
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4, X_10), multiply(X_4, X_10)))))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(X_10,
                          multiply(identity,
                            multiply(multiply(X_4, X_10),
                              multiply(X_4, X_10)))),
                        multiply(X_10,
                          multiply(identity,
                            multiply(multiply(X_4,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))),
                              multiply(X_4,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))))))),
                      multiply(multiply(X_10,
                          multiply(identity,
                            multiply(multiply(X_4, X_10),
                              multiply(X_4, X_10)))),
                        multiply(X_10,
                          multiply(identity,
                            multiply(multiply(X_4, X_10),
                              multiply(X_4, X_10)))))))],
              [refute_0_265, refute_0_271])).

cnf(refute_0_273, plain,
    (multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
               multiply(X_4,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
               multiply(X_4,
                 multiply(multiply(X_10, X_11),
                   multiply(X_11, X_11)))))))) =
     multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
               multiply(X_4,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
               multiply(X_4,
                 multiply(multiply(X_10, X_11),
                   multiply(X_11, X_11))))))))),
    introduced(tautology,
               [refl,
                [$fot(multiply(identity,
                        multiply(multiply(X_10,
                            multiply(identity,
                              multiply(multiply(X_4,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11))),
                                multiply(X_4,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11)))))),
                          multiply(X_10,
                            multiply(identity,
                              multiply(multiply(X_4,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11))),
                                multiply(X_4,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11)))))))))]])).

cnf(refute_0_274, plain,
    (multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))))))) !=
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4, X_10), multiply(X_4, X_10))))) |
     multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
               multiply(X_4,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
               multiply(X_4,
                 multiply(multiply(X_10, X_11),
                   multiply(X_11, X_11)))))))) !=
     multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
               multiply(X_4,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
               multiply(X_4,
                 multiply(multiply(X_10, X_11),
                   multiply(X_11, X_11)))))))) |
     multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
               multiply(X_4,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
               multiply(X_4,
                 multiply(multiply(X_10, X_11),
                   multiply(X_11, X_11)))))))) =
     multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(identity,
                          multiply(multiply(X_10,
                              multiply(identity,
                                multiply(multiply(X_4,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11))),
                                  multiply(X_4,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11)))))),
                            multiply(X_10,
                              multiply(identity,
                                multiply(multiply(X_4,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11))),
                                  multiply(X_4,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11)))))))),
                        multiply(identity,
                          multiply(multiply(X_10,
                              multiply(identity,
                                multiply(multiply(X_4,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11))),
                                  multiply(X_4,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11)))))),
                            multiply(X_10,
                              multiply(identity,
                                multiply(multiply(X_4,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11))),
                                  multiply(X_4,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11)))))))))),
                 [1, 1],
                 $fot(multiply(multiply(X_10,
                          multiply(identity,
                            multiply(multiply(X_4, X_10),
                              multiply(X_4, X_10)))),
                        multiply(X_10,
                          multiply(identity,
                            multiply(multiply(X_4, X_10),
                              multiply(X_4, X_10))))))]])).

cnf(refute_0_275, plain,
    (multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(X_4,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))))))) !=
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(X_4, X_10), multiply(X_4, X_10))))) |
     multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
               multiply(X_4,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
               multiply(X_4,
                 multiply(multiply(X_10, X_11),
                   multiply(X_11, X_11)))))))) =
     multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))),
    inference(resolve,
              [$cnf($equal(multiply(identity,
                        multiply(multiply(X_10,
                            multiply(identity,
                              multiply(multiply(X_4,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11))),
                                multiply(X_4,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11)))))),
                          multiply(X_10,
                            multiply(identity,
                              multiply(multiply(X_4,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11))),
                                multiply(X_4,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11)))))))),
                      multiply(identity,
                        multiply(multiply(X_10,
                            multiply(identity,
                              multiply(multiply(X_4,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11))),
                                multiply(X_4,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11)))))),
                          multiply(X_10,
                            multiply(identity,
                              multiply(multiply(X_4,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11))),
                                multiply(X_4,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11))))))))))],
              [refute_0_273, refute_0_274])).

cnf(refute_0_276, plain,
    (multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
               multiply(X_4,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
               multiply(X_4,
                 multiply(multiply(X_10, X_11),
                   multiply(X_11, X_11)))))))) =
     multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(X_10,
                          multiply(identity,
                            multiply(multiply(X_4,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))),
                              multiply(X_4,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11)))))),
                        multiply(X_10,
                          multiply(identity,
                            multiply(multiply(X_4,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))),
                              multiply(X_4,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))))))),
                      multiply(multiply(X_10,
                          multiply(identity,
                            multiply(multiply(X_4, X_10),
                              multiply(X_4, X_10)))),
                        multiply(X_10,
                          multiply(identity,
                            multiply(multiply(X_4, X_10),
                              multiply(X_4, X_10)))))))],
              [refute_0_272, refute_0_275])).

cnf(refute_0_277, plain,
    (multiply(X_4,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4,
                   multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                 multiply(X_4,
                   multiply(multiply(X_10, X_11),
                     multiply(X_11, X_11)))))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4,
                   multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                 multiply(X_4,
                   multiply(multiply(X_10, X_11),
                     multiply(X_11, X_11))))))))) =
     multiply(X_4,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4,
                   multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                 multiply(X_4,
                   multiply(multiply(X_10, X_11),
                     multiply(X_11, X_11)))))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4,
                   multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                 multiply(X_4,
                   multiply(multiply(X_10, X_11),
                     multiply(X_11, X_11)))))))))),
    introduced(tautology,
               [refl,
                [$fot(multiply(X_4,
                        multiply(identity,
                          multiply(multiply(X_10,
                              multiply(identity,
                                multiply(multiply(X_4,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11))),
                                  multiply(X_4,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11)))))),
                            multiply(X_10,
                              multiply(identity,
                                multiply(multiply(X_4,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11))),
                                  multiply(X_4,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11))))))))))]])).

cnf(refute_0_278, plain,
    (multiply(X_4,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4,
                   multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                 multiply(X_4,
                   multiply(multiply(X_10, X_11),
                     multiply(X_11, X_11)))))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4,
                   multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                 multiply(X_4,
                   multiply(multiply(X_10, X_11),
                     multiply(X_11, X_11))))))))) !=
     multiply(X_4,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4,
                   multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                 multiply(X_4,
                   multiply(multiply(X_10, X_11),
                     multiply(X_11, X_11)))))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4,
                   multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                 multiply(X_4,
                   multiply(multiply(X_10, X_11),
                     multiply(X_11, X_11))))))))) |
     multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
               multiply(X_4,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
               multiply(X_4,
                 multiply(multiply(X_10, X_11),
                   multiply(X_11, X_11)))))))) !=
     multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(X_4, X_10), multiply(X_4, X_10)))))) |
     multiply(X_4,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4,
                   multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                 multiply(X_4,
                   multiply(multiply(X_10, X_11),
                     multiply(X_11, X_11)))))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4,
                   multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                 multiply(X_4,
                   multiply(multiply(X_10, X_11),
                     multiply(X_11, X_11))))))))) =
     multiply(X_4,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4, X_10), multiply(X_4, X_10)))))))),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))),
                                    multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))),
                                    multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))))))))),
                        multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))),
                                    multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))),
                                    multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))))))))))),
                 [1, 1],
                 $fot(multiply(identity,
                        multiply(multiply(X_10,
                            multiply(identity,
                              multiply(multiply(X_4, X_10),
                                multiply(X_4, X_10)))),
                          multiply(X_10,
                            multiply(identity,
                              multiply(multiply(X_4, X_10),
                                multiply(X_4, X_10)))))))]])).

cnf(refute_0_279, plain,
    (multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
               multiply(X_4,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
               multiply(X_4,
                 multiply(multiply(X_10, X_11),
                   multiply(X_11, X_11)))))))) !=
     multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(X_4, X_10), multiply(X_4, X_10)))))) |
     multiply(X_4,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4,
                   multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                 multiply(X_4,
                   multiply(multiply(X_10, X_11),
                     multiply(X_11, X_11)))))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4,
                   multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                 multiply(X_4,
                   multiply(multiply(X_10, X_11),
                     multiply(X_11, X_11))))))))) =
     multiply(X_4,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4, X_10), multiply(X_4, X_10)))))))),
    inference(resolve,
              [$cnf($equal(multiply(X_4,
                        multiply(identity,
                          multiply(multiply(X_10,
                              multiply(identity,
                                multiply(multiply(X_4,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11))),
                                  multiply(X_4,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11)))))),
                            multiply(X_10,
                              multiply(identity,
                                multiply(multiply(X_4,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11))),
                                  multiply(X_4,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11))))))))),
                      multiply(X_4,
                        multiply(identity,
                          multiply(multiply(X_10,
                              multiply(identity,
                                multiply(multiply(X_4,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11))),
                                  multiply(X_4,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11)))))),
                            multiply(X_10,
                              multiply(identity,
                                multiply(multiply(X_4,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11))),
                                  multiply(X_4,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11)))))))))))],
              [refute_0_277, refute_0_278])).

cnf(refute_0_280, plain,
    (multiply(X_4,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4,
                   multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                 multiply(X_4,
                   multiply(multiply(X_10, X_11),
                     multiply(X_11, X_11)))))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4,
                   multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                 multiply(X_4,
                   multiply(multiply(X_10, X_11),
                     multiply(X_11, X_11))))))))) =
     multiply(X_4,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4, X_10), multiply(X_4, X_10)))))))),
    inference(resolve,
              [$cnf($equal(multiply(identity,
                        multiply(multiply(X_10,
                            multiply(identity,
                              multiply(multiply(X_4,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11))),
                                multiply(X_4,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11)))))),
                          multiply(X_10,
                            multiply(identity,
                              multiply(multiply(X_4,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11))),
                                multiply(X_4,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11)))))))),
                      multiply(identity,
                        multiply(multiply(X_10,
                            multiply(identity,
                              multiply(multiply(X_4, X_10),
                                multiply(X_4, X_10)))),
                          multiply(X_10,
                            multiply(identity,
                              multiply(multiply(X_4, X_10),
                                multiply(X_4, X_10))))))))],
              [refute_0_276, refute_0_279])).

cnf(refute_0_281, plain,
    (multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))))))) =
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11))))))))))),
    introduced(tautology,
               [refl,
                [$fot(multiply(multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4, X_10),
                                    multiply(X_4, X_10)))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4, X_10),
                                    multiply(X_4, X_10))))))),
                        multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))),
                                    multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))),
                                    multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11)))))))))))]])).

cnf(refute_0_282, plain,
    (multiply(X_4,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4,
                   multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                 multiply(X_4,
                   multiply(multiply(X_10, X_11),
                     multiply(X_11, X_11)))))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4,
                   multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                 multiply(X_4,
                   multiply(multiply(X_10, X_11),
                     multiply(X_11, X_11))))))))) !=
     multiply(X_4,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))) |
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))))))) !=
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))))))) |
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))))))) =
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))))),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4, X_10),
                                      multiply(X_4, X_10)))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4, X_10),
                                      multiply(X_4, X_10))))))),
                          multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))),
                                      multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11)))))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))),
                                      multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11)))))))))),
                        multiply(multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4, X_10),
                                      multiply(X_4, X_10)))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4, X_10),
                                      multiply(X_4, X_10))))))),
                          multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))),
                                      multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11)))))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))),
                                      multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11)))))))))))),
                 [1, 1],
                 $fot(multiply(X_4,
                        multiply(identity,
                          multiply(multiply(X_10,
                              multiply(identity,
                                multiply(multiply(X_4, X_10),
                                  multiply(X_4, X_10)))),
                            multiply(X_10,
                              multiply(identity,
                                multiply(multiply(X_4, X_10),
                                  multiply(X_4, X_10))))))))]])).

cnf(refute_0_283, plain,
    (multiply(X_4,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4,
                   multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                 multiply(X_4,
                   multiply(multiply(X_10, X_11),
                     multiply(X_11, X_11)))))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4,
                   multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                 multiply(X_4,
                   multiply(multiply(X_10, X_11),
                     multiply(X_11, X_11))))))))) !=
     multiply(X_4,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))) |
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))))))) =
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4, X_10),
                                    multiply(X_4, X_10)))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4, X_10),
                                    multiply(X_4, X_10))))))),
                        multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))),
                                    multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))),
                                    multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11)))))))))),
                      multiply(multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4, X_10),
                                    multiply(X_4, X_10)))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4, X_10),
                                    multiply(X_4, X_10))))))),
                        multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))),
                                    multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))),
                                    multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))))))))))))],
              [refute_0_281, refute_0_282])).

cnf(refute_0_284, plain,
    (multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))))))) =
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))))),
    inference(resolve,
              [$cnf($equal(multiply(X_4,
                        multiply(identity,
                          multiply(multiply(X_10,
                              multiply(identity,
                                multiply(multiply(X_4,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11))),
                                  multiply(X_4,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11)))))),
                            multiply(X_10,
                              multiply(identity,
                                multiply(multiply(X_4,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11))),
                                  multiply(X_4,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11))))))))),
                      multiply(X_4,
                        multiply(identity,
                          multiply(multiply(X_10,
                              multiply(identity,
                                multiply(multiply(X_4, X_10),
                                  multiply(X_4, X_10)))),
                            multiply(X_10,
                              multiply(identity,
                                multiply(multiply(X_4, X_10),
                                  multiply(X_4, X_10)))))))))],
              [refute_0_280, refute_0_283])).

cnf(refute_0_285, plain,
    (multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11))))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))))))) =
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11))))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11))))))))))),
    introduced(tautology,
               [refl,
                [$fot(multiply(multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))),
                                    multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))),
                                    multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))))))))),
                        multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))),
                                    multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))),
                                    multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11)))))))))))]])).

cnf(refute_0_286, plain,
    (multiply(X_4,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4,
                   multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                 multiply(X_4,
                   multiply(multiply(X_10, X_11),
                     multiply(X_11, X_11)))))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4,
                   multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                 multiply(X_4,
                   multiply(multiply(X_10, X_11),
                     multiply(X_11, X_11))))))))) !=
     multiply(X_4,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))) |
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11))))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))))))) !=
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11))))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))))))) |
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11))))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))))))) =
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11))))))))))),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))),
                                      multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11)))))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))),
                                      multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))))))))),
                          multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))),
                                      multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11)))))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))),
                                      multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11)))))))))),
                        multiply(multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))),
                                      multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11)))))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))),
                                      multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))))))))),
                          multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))),
                                      multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11)))))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))),
                                      multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11)))))))))))),
                 [1, 0],
                 $fot(multiply(X_4,
                        multiply(identity,
                          multiply(multiply(X_10,
                              multiply(identity,
                                multiply(multiply(X_4, X_10),
                                  multiply(X_4, X_10)))),
                            multiply(X_10,
                              multiply(identity,
                                multiply(multiply(X_4, X_10),
                                  multiply(X_4, X_10))))))))]])).

cnf(refute_0_287, plain,
    (multiply(X_4,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4,
                   multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                 multiply(X_4,
                   multiply(multiply(X_10, X_11),
                     multiply(X_11, X_11)))))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4,
                   multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                 multiply(X_4,
                   multiply(multiply(X_10, X_11),
                     multiply(X_11, X_11))))))))) !=
     multiply(X_4,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))) |
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11))))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))))))) =
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11))))))))))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))),
                                    multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))),
                                    multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))))))))),
                        multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))),
                                    multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))),
                                    multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11)))))))))),
                      multiply(multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))),
                                    multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))),
                                    multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))))))))),
                        multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))),
                                    multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))),
                                    multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))))))))))))],
              [refute_0_285, refute_0_286])).

cnf(refute_0_288, plain,
    (multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11))))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))))))) =
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11))))))))))),
    inference(resolve,
              [$cnf($equal(multiply(X_4,
                        multiply(identity,
                          multiply(multiply(X_10,
                              multiply(identity,
                                multiply(multiply(X_4,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11))),
                                  multiply(X_4,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11)))))),
                            multiply(X_10,
                              multiply(identity,
                                multiply(multiply(X_4,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11))),
                                  multiply(X_4,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11))))))))),
                      multiply(X_4,
                        multiply(identity,
                          multiply(multiply(X_10,
                              multiply(identity,
                                multiply(multiply(X_4, X_10),
                                  multiply(X_4, X_10)))),
                            multiply(X_10,
                              multiply(identity,
                                multiply(multiply(X_4, X_10),
                                  multiply(X_4, X_10)))))))))],
              [refute_0_280, refute_0_287])).

cnf(refute_0_289, plain,
    (multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))))))) !=
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))))))) |
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11))))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))))))) !=
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))))))) |
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11))))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))))))) =
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))))),
    inference(subst, [],
              [refute_0_92 :
               [bind(X0,
                     $fot(multiply(multiply(X_4,
                              multiply(identity,
                                multiply(multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(X_4,
                                          multiply(multiply(X_10, X_11),
                                            multiply(X_11, X_11))),
                                        multiply(X_4,
                                          multiply(multiply(X_10, X_11),
                                            multiply(X_11, X_11)))))),
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(X_4,
                                          multiply(multiply(X_10, X_11),
                                            multiply(X_11, X_11))),
                                        multiply(X_4,
                                          multiply(multiply(X_10, X_11),
                                            multiply(X_11, X_11))))))))),
                            multiply(X_4,
                              multiply(identity,
                                multiply(multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(X_4,
                                          multiply(multiply(X_10, X_11),
                                            multiply(X_11, X_11))),
                                        multiply(X_4,
                                          multiply(multiply(X_10, X_11),
                                            multiply(X_11, X_11)))))),
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(X_4,
                                          multiply(multiply(X_10, X_11),
                                            multiply(X_11, X_11))),
                                        multiply(X_4,
                                          multiply(multiply(X_10, X_11),
                                            multiply(X_11,
                                              X_11)))))))))))),
                bind(Y0,
                     $fot(multiply(multiply(X_4,
                              multiply(identity,
                                multiply(multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(X_4, X_10),
                                        multiply(X_4, X_10)))),
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(X_4, X_10),
                                        multiply(X_4, X_10))))))),
                            multiply(X_4,
                              multiply(identity,
                                multiply(multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(X_4,
                                          multiply(multiply(X_10, X_11),
                                            multiply(X_11, X_11))),
                                        multiply(X_4,
                                          multiply(multiply(X_10, X_11),
                                            multiply(X_11, X_11)))))),
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(X_4,
                                          multiply(multiply(X_10, X_11),
                                            multiply(X_11, X_11))),
                                        multiply(X_4,
                                          multiply(multiply(X_10, X_11),
                                            multiply(X_11,
                                              X_11)))))))))))),
                bind(Z0,
                     $fot(multiply(multiply(X_4,
                              multiply(identity,
                                multiply(multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(X_4, X_10),
                                        multiply(X_4, X_10)))),
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(X_4, X_10),
                                        multiply(X_4, X_10))))))),
                            multiply(X_4,
                              multiply(identity,
                                multiply(multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(X_4, X_10),
                                        multiply(X_4, X_10)))),
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(X_4, X_10),
                                        multiply(X_4, X_10))))))))))]])).

cnf(refute_0_290, plain,
    (multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))))))) !=
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))))))) |
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11))))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))))))) =
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))),
                                    multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))),
                                    multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))))))))),
                        multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))),
                                    multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))),
                                    multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11)))))))))),
                      multiply(multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4, X_10),
                                    multiply(X_4, X_10)))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4, X_10),
                                    multiply(X_4, X_10))))))),
                        multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))),
                                    multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))),
                                    multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))))))))))))],
              [refute_0_288, refute_0_289])).

cnf(refute_0_291, plain,
    (multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11))))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))))))) =
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4, X_10),
                                    multiply(X_4, X_10)))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4, X_10),
                                    multiply(X_4, X_10))))))),
                        multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))),
                                    multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))),
                                    multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11)))))))))),
                      multiply(multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4, X_10),
                                    multiply(X_4, X_10)))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4, X_10),
                                    multiply(X_4, X_10))))))),
                        multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4, X_10),
                                    multiply(X_4, X_10)))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4, X_10),
                                    multiply(X_4, X_10))))))))))],
              [refute_0_284, refute_0_290])).

cnf(refute_0_292, plain,
    (multiply(identity,
       multiply(multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))))))))),
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))))))))))) =
     multiply(identity,
       multiply(multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))))))))),
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11)))))))))))),
    introduced(tautology,
               [refl,
                [$fot(multiply(identity,
                        multiply(multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))),
                                      multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11)))))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))),
                                      multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))))))))),
                          multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))),
                                      multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11)))))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))),
                                      multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11,
                                            X_11))))))))))))]])).

cnf(refute_0_293, plain,
    (multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11))))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))))))) !=
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))))))) |
     multiply(identity,
       multiply(multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))))))))),
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))))))))))) !=
     multiply(identity,
       multiply(multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))))))))),
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))))))))))) |
     multiply(identity,
       multiply(multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))))))))),
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))))))))))) =
     multiply(identity,
       multiply(multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))),
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10),
                     multiply(X_4, X_10)))))))))),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(identity,
                          multiply(multiply(X_4,
                              multiply(identity,
                                multiply(multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(X_4,
                                          multiply(multiply(X_10, X_11),
                                            multiply(X_11, X_11))),
                                        multiply(X_4,
                                          multiply(multiply(X_10, X_11),
                                            multiply(X_11, X_11)))))),
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(X_4,
                                          multiply(multiply(X_10, X_11),
                                            multiply(X_11, X_11))),
                                        multiply(X_4,
                                          multiply(multiply(X_10, X_11),
                                            multiply(X_11, X_11))))))))),
                            multiply(X_4,
                              multiply(identity,
                                multiply(multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(X_4,
                                          multiply(multiply(X_10, X_11),
                                            multiply(X_11, X_11))),
                                        multiply(X_4,
                                          multiply(multiply(X_10, X_11),
                                            multiply(X_11, X_11)))))),
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(X_4,
                                          multiply(multiply(X_10, X_11),
                                            multiply(X_11, X_11))),
                                        multiply(X_4,
                                          multiply(multiply(X_10, X_11),
                                            multiply(X_11, X_11))))))))))),
                        multiply(identity,
                          multiply(multiply(X_4,
                              multiply(identity,
                                multiply(multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(X_4,
                                          multiply(multiply(X_10, X_11),
                                            multiply(X_11, X_11))),
                                        multiply(X_4,
                                          multiply(multiply(X_10, X_11),
                                            multiply(X_11, X_11)))))),
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(X_4,
                                          multiply(multiply(X_10, X_11),
                                            multiply(X_11, X_11))),
                                        multiply(X_4,
                                          multiply(multiply(X_10, X_11),
                                            multiply(X_11, X_11))))))))),
                            multiply(X_4,
                              multiply(identity,
                                multiply(multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(X_4,
                                          multiply(multiply(X_10, X_11),
                                            multiply(X_11, X_11))),
                                        multiply(X_4,
                                          multiply(multiply(X_10, X_11),
                                            multiply(X_11, X_11)))))),
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(X_4,
                                          multiply(multiply(X_10, X_11),
                                            multiply(X_11, X_11))),
                                        multiply(X_4,
                                          multiply(multiply(X_10, X_11),
                                            multiply(X_11,
                                              X_11))))))))))))), [1, 1],
                 $fot(multiply(multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4, X_10),
                                    multiply(X_4, X_10)))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4, X_10),
                                    multiply(X_4, X_10))))))),
                        multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4, X_10),
                                    multiply(X_4, X_10)))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4, X_10),
                                    multiply(X_4, X_10)))))))))]])).

cnf(refute_0_294, plain,
    (multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11))))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(X_4,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))))))) !=
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))))))) |
     multiply(identity,
       multiply(multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))))))))),
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))))))))))) =
     multiply(identity,
       multiply(multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))),
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10),
                     multiply(X_4, X_10)))))))))),
    inference(resolve,
              [$cnf($equal(multiply(identity,
                        multiply(multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))),
                                      multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11)))))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))),
                                      multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))))))))),
                          multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))),
                                      multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11)))))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))),
                                      multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))))))))))),
                      multiply(identity,
                        multiply(multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))),
                                      multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11)))))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))),
                                      multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))))))))),
                          multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))),
                                      multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11)))))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))),
                                      multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11,
                                            X_11)))))))))))))],
              [refute_0_292, refute_0_293])).

cnf(refute_0_295, plain,
    (multiply(identity,
       multiply(multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))))))))),
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))))))))))) =
     multiply(identity,
       multiply(multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))),
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10),
                     multiply(X_4, X_10)))))))))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))),
                                    multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))),
                                    multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))))))))),
                        multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))),
                                    multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))),
                                    multiply(X_4,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11)))))))))),
                      multiply(multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4, X_10),
                                    multiply(X_4, X_10)))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4, X_10),
                                    multiply(X_4, X_10))))))),
                        multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4, X_10),
                                    multiply(X_4, X_10)))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4, X_10),
                                    multiply(X_4, X_10))))))))))],
              [refute_0_291, refute_0_294])).

cnf(refute_0_296, plain,
    (multiply(X_4,
       multiply(X_4,
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))),
                           multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))),
                           multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))))))))),
               multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))),
                           multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))),
                           multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11)))))))))))))) =
     multiply(identity,
       multiply(multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))))))))),
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11)))))))))))),
    inference(subst, [],
              [refute_0_219 :
               [bind(X_17, $fot(X_4)),
                bind(X_18,
                     $fot(multiply(identity,
                            multiply(multiply(X_4,
                                multiply(identity,
                                  multiply(multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(X_4,
                                            multiply(multiply(X_10, X_11),
                                              multiply(X_11, X_11))),
                                          multiply(X_4,
                                            multiply(multiply(X_10, X_11),
                                              multiply(X_11, X_11)))))),
                                    multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(X_4,
                                            multiply(multiply(X_10, X_11),
                                              multiply(X_11, X_11))),
                                          multiply(X_4,
                                            multiply(multiply(X_10, X_11),
                                              multiply(X_11, X_11))))))))),
                              multiply(X_4,
                                multiply(identity,
                                  multiply(multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(X_4,
                                            multiply(multiply(X_10, X_11),
                                              multiply(X_11, X_11))),
                                          multiply(X_4,
                                            multiply(multiply(X_10, X_11),
                                              multiply(X_11, X_11)))))),
                                    multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(X_4,
                                            multiply(multiply(X_10, X_11),
                                              multiply(X_11, X_11))),
                                          multiply(X_4,
                                            multiply(multiply(X_10, X_11),
                                              multiply(X_11,
                                                X_11)))))))))))))]])).

cnf(refute_0_297, plain,
    (multiply(X_4,
       multiply(X_4,
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))),
                           multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))),
                           multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))))))))),
               multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))),
                           multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))),
                           multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11)))))))))))))) !=
     multiply(identity,
       multiply(multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))))))))),
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))))))))))) |
     multiply(identity,
       multiply(multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))))))))),
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))))))))))) !=
     multiply(identity,
       multiply(multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))),
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10),
                     multiply(X_4, X_10))))))))) |
     multiply(X_4,
       multiply(X_4,
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))),
                           multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))),
                           multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))))))))),
               multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))),
                           multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))),
                           multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11)))))))))))))) =
     multiply(identity,
       multiply(multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))),
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10),
                     multiply(X_4, X_10)))))))))),
    inference(subst, [],
              [refute_0_92 :
               [bind(X0,
                     $fot(multiply(X_4,
                            multiply(X_4,
                              multiply(X_4,
                                multiply(identity,
                                  multiply(multiply(X_4,
                                      multiply(identity,
                                        multiply(multiply(X_10,
                                            multiply(identity,
                                              multiply(multiply(X_4,
                                                  multiply(multiply(X_10,
                                                      X_11),
                                                    multiply(X_11, X_11))),
                                                multiply(X_4,
                                                  multiply(multiply(X_10,
                                                      X_11),
                                                    multiply(X_11,
                                                      X_11)))))),
                                          multiply(X_10,
                                            multiply(identity,
                                              multiply(multiply(X_4,
                                                  multiply(multiply(X_10,
                                                      X_11),
                                                    multiply(X_11, X_11))),
                                                multiply(X_4,
                                                  multiply(multiply(X_10,
                                                      X_11),
                                                    multiply(X_11,
                                                      X_11))))))))),
                                    multiply(X_4,
                                      multiply(identity,
                                        multiply(multiply(X_10,
                                            multiply(identity,
                                              multiply(multiply(X_4,
                                                  multiply(multiply(X_10,
                                                      X_11),
                                                    multiply(X_11, X_11))),
                                                multiply(X_4,
                                                  multiply(multiply(X_10,
                                                      X_11),
                                                    multiply(X_11,
                                                      X_11)))))),
                                          multiply(X_10,
                                            multiply(identity,
                                              multiply(multiply(X_4,
                                                  multiply(multiply(X_10,
                                                      X_11),
                                                    multiply(X_11, X_11))),
                                                multiply(X_4,
                                                  multiply(multiply(X_10,
                                                      X_11),
                                                    multiply(X_11,
                                                      X_11)))))))))))))))),
                bind(Y0,
                     $fot(multiply(identity,
                            multiply(multiply(X_4,
                                multiply(identity,
                                  multiply(multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(X_4,
                                            multiply(multiply(X_10, X_11),
                                              multiply(X_11, X_11))),
                                          multiply(X_4,
                                            multiply(multiply(X_10, X_11),
                                              multiply(X_11, X_11)))))),
                                    multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(X_4,
                                            multiply(multiply(X_10, X_11),
                                              multiply(X_11, X_11))),
                                          multiply(X_4,
                                            multiply(multiply(X_10, X_11),
                                              multiply(X_11, X_11))))))))),
                              multiply(X_4,
                                multiply(identity,
                                  multiply(multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(X_4,
                                            multiply(multiply(X_10, X_11),
                                              multiply(X_11, X_11))),
                                          multiply(X_4,
                                            multiply(multiply(X_10, X_11),
                                              multiply(X_11, X_11)))))),
                                    multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(X_4,
                                            multiply(multiply(X_10, X_11),
                                              multiply(X_11, X_11))),
                                          multiply(X_4,
                                            multiply(multiply(X_10, X_11),
                                              multiply(X_11,
                                                X_11))))))))))))),
                bind(Z0,
                     $fot(multiply(identity,
                            multiply(multiply(X_4,
                                multiply(identity,
                                  multiply(multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(X_4, X_10),
                                          multiply(X_4, X_10)))),
                                    multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(X_4, X_10),
                                          multiply(X_4, X_10))))))),
                              multiply(X_4,
                                multiply(identity,
                                  multiply(multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(X_4, X_10),
                                          multiply(X_4, X_10)))),
                                    multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(X_4, X_10),
                                          multiply(X_4,
                                            X_10)))))))))))]])).

cnf(refute_0_298, plain,
    (multiply(identity,
       multiply(multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))))))))),
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(X_4,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))))))))))) !=
     multiply(identity,
       multiply(multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))),
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10),
                     multiply(X_4, X_10))))))))) |
     multiply(X_4,
       multiply(X_4,
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))),
                           multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))),
                           multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))))))))),
               multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))),
                           multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))),
                           multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11)))))))))))))) =
     multiply(identity,
       multiply(multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))),
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10),
                     multiply(X_4, X_10)))))))))),
    inference(resolve,
              [$cnf($equal(multiply(X_4,
                        multiply(X_4,
                          multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_4,
                                  multiply(identity,
                                    multiply(multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(X_4,
                                              multiply(multiply(X_10,
                                                  X_11),
                                                multiply(X_11, X_11))),
                                            multiply(X_4,
                                              multiply(multiply(X_10,
                                                  X_11),
                                                multiply(X_11, X_11)))))),
                                      multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(X_4,
                                              multiply(multiply(X_10,
                                                  X_11),
                                                multiply(X_11, X_11))),
                                            multiply(X_4,
                                              multiply(multiply(X_10,
                                                  X_11),
                                                multiply(X_11,
                                                  X_11))))))))),
                                multiply(X_4,
                                  multiply(identity,
                                    multiply(multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(X_4,
                                              multiply(multiply(X_10,
                                                  X_11),
                                                multiply(X_11, X_11))),
                                            multiply(X_4,
                                              multiply(multiply(X_10,
                                                  X_11),
                                                multiply(X_11, X_11)))))),
                                      multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(X_4,
                                              multiply(multiply(X_10,
                                                  X_11),
                                                multiply(X_11, X_11))),
                                            multiply(X_4,
                                              multiply(multiply(X_10,
                                                  X_11),
                                                multiply(X_11,
                                                  X_11)))))))))))))),
                      multiply(identity,
                        multiply(multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))),
                                      multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11)))))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))),
                                      multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))))))))),
                          multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))),
                                      multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11)))))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))),
                                      multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11,
                                            X_11)))))))))))))],
              [refute_0_296, refute_0_297])).

cnf(refute_0_299, plain,
    (multiply(X_4,
       multiply(X_4,
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))),
                           multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))),
                           multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))))))))),
               multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))),
                           multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))),
                           multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11)))))))))))))) =
     multiply(identity,
       multiply(multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))),
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10),
                     multiply(X_4, X_10)))))))))),
    inference(resolve,
              [$cnf($equal(multiply(identity,
                        multiply(multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))),
                                      multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11)))))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))),
                                      multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))))))))),
                          multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))),
                                      multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11)))))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))),
                                      multiply(X_4,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))))))))))),
                      multiply(identity,
                        multiply(multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4, X_10),
                                      multiply(X_4, X_10)))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4, X_10),
                                      multiply(X_4, X_10))))))),
                          multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4, X_10),
                                      multiply(X_4, X_10)))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4, X_10),
                                      multiply(X_4, X_10)))))))))))],
              [refute_0_295, refute_0_298])).

cnf(refute_0_300, plain,
    (multiply(X_4,
       multiply(X_4,
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))),
                           multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))),
                           multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))))))))),
               multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))),
                           multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))),
                           multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11)))))))))))))) != X_4 |
     multiply(X_4,
       multiply(X_4,
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))),
                           multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))),
                           multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))))))))),
               multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))),
                           multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))),
                           multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11)))))))))))))) !=
     multiply(identity,
       multiply(multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))),
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10),
                     multiply(X_4, X_10))))))))) |
     multiply(identity,
       multiply(multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))),
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10),
                     multiply(X_4, X_10))))))))) = X_4),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(X_4,
                          multiply(X_4,
                            multiply(X_4,
                              multiply(identity,
                                multiply(multiply(X_4,
                                    multiply(identity,
                                      multiply(multiply(X_10,
                                          multiply(identity,
                                            multiply(multiply(X_4,
                                                multiply(multiply(X_10,
                                                    X_11),
                                                  multiply(X_11, X_11))),
                                              multiply(X_4,
                                                multiply(multiply(X_10,
                                                    X_11),
                                                  multiply(X_11,
                                                    X_11)))))),
                                        multiply(X_10,
                                          multiply(identity,
                                            multiply(multiply(X_4,
                                                multiply(multiply(X_10,
                                                    X_11),
                                                  multiply(X_11, X_11))),
                                              multiply(X_4,
                                                multiply(multiply(X_10,
                                                    X_11),
                                                  multiply(X_11,
                                                    X_11))))))))),
                                  multiply(X_4,
                                    multiply(identity,
                                      multiply(multiply(X_10,
                                          multiply(identity,
                                            multiply(multiply(X_4,
                                                multiply(multiply(X_10,
                                                    X_11),
                                                  multiply(X_11, X_11))),
                                              multiply(X_4,
                                                multiply(multiply(X_10,
                                                    X_11),
                                                  multiply(X_11,
                                                    X_11)))))),
                                        multiply(X_10,
                                          multiply(identity,
                                            multiply(multiply(X_4,
                                                multiply(multiply(X_10,
                                                    X_11),
                                                  multiply(X_11, X_11))),
                                              multiply(X_4,
                                                multiply(multiply(X_10,
                                                    X_11),
                                                  multiply(X_11,
                                                    X_11)))))))))))))),
                        X_4)), [0],
                 $fot(multiply(identity,
                        multiply(multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4, X_10),
                                      multiply(X_4, X_10)))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4, X_10),
                                      multiply(X_4, X_10))))))),
                          multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4, X_10),
                                      multiply(X_4, X_10)))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4, X_10),
                                      multiply(X_4, X_10))))))))))]])).

cnf(refute_0_301, plain,
    (multiply(X_4,
       multiply(X_4,
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))),
                           multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))),
                           multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))))))))),
               multiply(X_4,
                 multiply(identity,
                   multiply(multiply(X_10,
                       multiply(identity,
                         multiply(multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))),
                           multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11)))))),
                     multiply(X_10,
                       multiply(identity,
                         multiply(multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11))),
                           multiply(X_4,
                             multiply(multiply(X_10, X_11),
                               multiply(X_11, X_11)))))))))))))) != X_4 |
     multiply(identity,
       multiply(multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))),
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10),
                     multiply(X_4, X_10))))))))) = X_4),
    inference(resolve,
              [$cnf($equal(multiply(X_4,
                        multiply(X_4,
                          multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_4,
                                  multiply(identity,
                                    multiply(multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(X_4,
                                              multiply(multiply(X_10,
                                                  X_11),
                                                multiply(X_11, X_11))),
                                            multiply(X_4,
                                              multiply(multiply(X_10,
                                                  X_11),
                                                multiply(X_11, X_11)))))),
                                      multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(X_4,
                                              multiply(multiply(X_10,
                                                  X_11),
                                                multiply(X_11, X_11))),
                                            multiply(X_4,
                                              multiply(multiply(X_10,
                                                  X_11),
                                                multiply(X_11,
                                                  X_11))))))))),
                                multiply(X_4,
                                  multiply(identity,
                                    multiply(multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(X_4,
                                              multiply(multiply(X_10,
                                                  X_11),
                                                multiply(X_11, X_11))),
                                            multiply(X_4,
                                              multiply(multiply(X_10,
                                                  X_11),
                                                multiply(X_11, X_11)))))),
                                      multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(X_4,
                                              multiply(multiply(X_10,
                                                  X_11),
                                                multiply(X_11, X_11))),
                                            multiply(X_4,
                                              multiply(multiply(X_10,
                                                  X_11),
                                                multiply(X_11,
                                                  X_11)))))))))))))),
                      multiply(identity,
                        multiply(multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4, X_10),
                                      multiply(X_4, X_10)))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4, X_10),
                                      multiply(X_4, X_10))))))),
                          multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4, X_10),
                                      multiply(X_4, X_10)))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4, X_10),
                                      multiply(X_4, X_10)))))))))))],
              [refute_0_299, refute_0_300])).

cnf(refute_0_302, plain,
    (multiply(identity,
       multiply(multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))),
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10),
                     multiply(X_4, X_10))))))))) = X_4),
    inference(resolve,
              [$cnf($equal(multiply(X_4,
                        multiply(X_4,
                          multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_4,
                                  multiply(identity,
                                    multiply(multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(X_4,
                                              multiply(multiply(X_10,
                                                  X_11),
                                                multiply(X_11, X_11))),
                                            multiply(X_4,
                                              multiply(multiply(X_10,
                                                  X_11),
                                                multiply(X_11, X_11)))))),
                                      multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(X_4,
                                              multiply(multiply(X_10,
                                                  X_11),
                                                multiply(X_11, X_11))),
                                            multiply(X_4,
                                              multiply(multiply(X_10,
                                                  X_11),
                                                multiply(X_11,
                                                  X_11))))))))),
                                multiply(X_4,
                                  multiply(identity,
                                    multiply(multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(X_4,
                                              multiply(multiply(X_10,
                                                  X_11),
                                                multiply(X_11, X_11))),
                                            multiply(X_4,
                                              multiply(multiply(X_10,
                                                  X_11),
                                                multiply(X_11, X_11)))))),
                                      multiply(X_10,
                                        multiply(identity,
                                          multiply(multiply(X_4,
                                              multiply(multiply(X_10,
                                                  X_11),
                                                multiply(X_11, X_11))),
                                            multiply(X_4,
                                              multiply(multiply(X_10,
                                                  X_11),
                                                multiply(X_11,
                                                  X_11)))))))))))))),
                      X_4))], [refute_0_152, refute_0_301])).

cnf(refute_0_303, plain,
    (multiply(multiply(X_4, X_4), multiply(X_4, X_4)) = X_4),
    inference(subst, [],
              [refute_0_237 :
               [bind(X_18, $fot(X_4)), bind(X_19, $fot(X_4))]])).

cnf(refute_0_304, plain,
    (multiply(Y,
       multiply(Y,
         multiply(multiply(Y,
             multiply(Y,
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))))),
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))))))))))) =
     Y),
    inference(subst, [],
              [refute_0_17 :
               [bind(X, $fot(Y)),
                bind(Z,
                     $fot(multiply(X_10,
                            multiply(identity,
                              multiply(multiply(multiply(Y,
                                    multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11))),
                                multiply(multiply(Y, multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11))))))))]])).

cnf(refute_0_305, plain,
    (multiply(Y,
       multiply(Y,
         multiply(X_10,
           multiply(identity,
             multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11))),
               multiply(multiply(Y, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11)))))))) = Y),
    inference(subst, [],
              [refute_0_22 :
               [bind(X_4, $fot(X_10)), bind(X_5, $fot(X_11))]])).

cnf(refute_0_306, plain,
    (multiply(Y,
       multiply(Y,
         multiply(X_10,
           multiply(identity,
             multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11))),
               multiply(multiply(Y, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11)))))))) != Y |
     multiply(Y,
       multiply(Y,
         multiply(multiply(Y,
             multiply(Y,
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))))),
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))))))))))) !=
     Y |
     multiply(Y,
       multiply(Y,
         multiply(Y,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))))))))))) =
     Y),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(Y,
                          multiply(Y,
                            multiply(multiply(Y,
                                multiply(Y,
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(Y,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(Y,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11)))))))),
                              multiply(identity,
                                multiply(multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(Y,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(Y,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11)))))),
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(Y,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(Y,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))))))))))),
                        Y)), [0, 1, 1, 0], $fot(Y)]])).

cnf(refute_0_307, plain,
    (multiply(Y,
       multiply(Y,
         multiply(multiply(Y,
             multiply(Y,
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))))),
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))))))))))) !=
     Y |
     multiply(Y,
       multiply(Y,
         multiply(Y,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))))))))))) =
     Y),
    inference(resolve,
              [$cnf($equal(multiply(Y,
                        multiply(Y,
                          multiply(X_10,
                            multiply(identity,
                              multiply(multiply(multiply(Y,
                                    multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11))),
                                multiply(multiply(Y, multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11)))))))), Y))],
              [refute_0_305, refute_0_306])).

cnf(refute_0_308, plain,
    (multiply(Y,
       multiply(Y,
         multiply(Y,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))))))))))) =
     Y),
    inference(resolve,
              [$cnf($equal(multiply(Y,
                        multiply(Y,
                          multiply(multiply(Y,
                              multiply(Y,
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(Y,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(Y,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11)))))))),
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(Y,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(Y,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11)))))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(Y,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(Y,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))))))))))),
                      Y))], [refute_0_304, refute_0_307])).

cnf(refute_0_309, plain,
    (multiply(multiply(Y, multiply(X_10, X_11)),
       multiply(identity, multiply(X_11, X_11))) =
     multiply(Y, multiply(multiply(X_10, X_11), multiply(X_11, X_11)))),
    inference(subst, [],
              [refute_0_81 :
               [bind(X, $fot(Y)), bind(X_1, $fot(multiply(X_10, X_11))),
                bind(X_14, $fot(X_11))]])).

cnf(refute_0_310, plain,
    (multiply(multiply(Y,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(multiply(Y, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11)))) =
     multiply(multiply(Y,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(multiply(Y, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11))))),
    introduced(tautology,
               [refl,
                [$fot(multiply(multiply(Y,
                          multiply(multiply(X_10, X_11),
                            multiply(X_11, X_11))),
                        multiply(multiply(Y, multiply(X_10, X_11)),
                          multiply(identity, multiply(X_11, X_11)))))]])).

cnf(refute_0_311, plain,
    (multiply(multiply(Y, multiply(X_10, X_11)),
       multiply(identity, multiply(X_11, X_11))) !=
     multiply(Y, multiply(multiply(X_10, X_11), multiply(X_11, X_11))) |
     multiply(multiply(Y,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(multiply(Y, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11)))) !=
     multiply(multiply(Y,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(multiply(Y, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11)))) |
     multiply(multiply(Y,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(multiply(Y, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11)))) =
     multiply(multiply(Y,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(Y, multiply(multiply(X_10, X_11), multiply(X_11, X_11))))),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(multiply(Y,
                            multiply(multiply(X_10, X_11),
                              multiply(X_11, X_11))),
                          multiply(multiply(Y, multiply(X_10, X_11)),
                            multiply(identity, multiply(X_11, X_11)))),
                        multiply(multiply(Y,
                            multiply(multiply(X_10, X_11),
                              multiply(X_11, X_11))),
                          multiply(multiply(Y, multiply(X_10, X_11)),
                            multiply(identity, multiply(X_11, X_11)))))),
                 [1, 1],
                 $fot(multiply(Y,
                        multiply(multiply(X_10, X_11),
                          multiply(X_11, X_11))))]])).

cnf(refute_0_312, plain,
    (multiply(multiply(Y, multiply(X_10, X_11)),
       multiply(identity, multiply(X_11, X_11))) !=
     multiply(Y, multiply(multiply(X_10, X_11), multiply(X_11, X_11))) |
     multiply(multiply(Y,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(multiply(Y, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11)))) =
     multiply(multiply(Y,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(Y, multiply(multiply(X_10, X_11), multiply(X_11, X_11))))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(Y,
                          multiply(multiply(X_10, X_11),
                            multiply(X_11, X_11))),
                        multiply(multiply(Y, multiply(X_10, X_11)),
                          multiply(identity, multiply(X_11, X_11)))),
                      multiply(multiply(Y,
                          multiply(multiply(X_10, X_11),
                            multiply(X_11, X_11))),
                        multiply(multiply(Y, multiply(X_10, X_11)),
                          multiply(identity, multiply(X_11, X_11))))))],
              [refute_0_310, refute_0_311])).

cnf(refute_0_313, plain,
    (multiply(multiply(Y,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(multiply(Y, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11)))) =
     multiply(multiply(Y,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(Y, multiply(multiply(X_10, X_11), multiply(X_11, X_11))))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(Y, multiply(X_10, X_11)),
                        multiply(identity, multiply(X_11, X_11))),
                      multiply(Y,
                        multiply(multiply(X_10, X_11),
                          multiply(X_11, X_11)))))],
              [refute_0_309, refute_0_312])).

cnf(refute_0_314, plain,
    (multiply(multiply(multiply(Y, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11))),
       multiply(multiply(Y, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11)))) =
     multiply(multiply(multiply(Y, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11))),
       multiply(multiply(Y, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11))))),
    introduced(tautology,
               [refl,
                [$fot(multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                          multiply(identity, multiply(X_11, X_11))),
                        multiply(multiply(Y, multiply(X_10, X_11)),
                          multiply(identity, multiply(X_11, X_11)))))]])).

cnf(refute_0_315, plain,
    (multiply(multiply(Y, multiply(X_10, X_11)),
       multiply(identity, multiply(X_11, X_11))) !=
     multiply(Y, multiply(multiply(X_10, X_11), multiply(X_11, X_11))) |
     multiply(multiply(multiply(Y, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11))),
       multiply(multiply(Y, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11)))) !=
     multiply(multiply(multiply(Y, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11))),
       multiply(multiply(Y, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11)))) |
     multiply(multiply(multiply(Y, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11))),
       multiply(multiply(Y, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11)))) =
     multiply(multiply(Y,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(multiply(Y, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11))))),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(multiply(multiply(Y,
                              multiply(X_10, X_11)),
                            multiply(identity, multiply(X_11, X_11))),
                          multiply(multiply(Y, multiply(X_10, X_11)),
                            multiply(identity, multiply(X_11, X_11)))),
                        multiply(multiply(multiply(Y,
                              multiply(X_10, X_11)),
                            multiply(identity, multiply(X_11, X_11))),
                          multiply(multiply(Y, multiply(X_10, X_11)),
                            multiply(identity, multiply(X_11, X_11)))))),
                 [1, 0],
                 $fot(multiply(Y,
                        multiply(multiply(X_10, X_11),
                          multiply(X_11, X_11))))]])).

cnf(refute_0_316, plain,
    (multiply(multiply(Y, multiply(X_10, X_11)),
       multiply(identity, multiply(X_11, X_11))) !=
     multiply(Y, multiply(multiply(X_10, X_11), multiply(X_11, X_11))) |
     multiply(multiply(multiply(Y, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11))),
       multiply(multiply(Y, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11)))) =
     multiply(multiply(Y,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(multiply(Y, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11))))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(multiply(Y,
                            multiply(X_10, X_11)),
                          multiply(identity, multiply(X_11, X_11))),
                        multiply(multiply(Y, multiply(X_10, X_11)),
                          multiply(identity, multiply(X_11, X_11)))),
                      multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                          multiply(identity, multiply(X_11, X_11))),
                        multiply(multiply(Y, multiply(X_10, X_11)),
                          multiply(identity, multiply(X_11, X_11))))))],
              [refute_0_314, refute_0_315])).

cnf(refute_0_317, plain,
    (multiply(multiply(multiply(Y, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11))),
       multiply(multiply(Y, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11)))) =
     multiply(multiply(Y,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(multiply(Y, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11))))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(Y, multiply(X_10, X_11)),
                        multiply(identity, multiply(X_11, X_11))),
                      multiply(Y,
                        multiply(multiply(X_10, X_11),
                          multiply(X_11, X_11)))))],
              [refute_0_309, refute_0_316])).

cnf(refute_0_318, plain,
    (multiply(multiply(Y,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(multiply(Y, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11)))) !=
     multiply(multiply(Y,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(Y, multiply(multiply(X_10, X_11), multiply(X_11, X_11)))) |
     multiply(multiply(multiply(Y, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11))),
       multiply(multiply(Y, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11)))) !=
     multiply(multiply(Y,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(multiply(Y, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11)))) |
     multiply(multiply(multiply(Y, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11))),
       multiply(multiply(Y, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11)))) =
     multiply(multiply(Y,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(Y, multiply(multiply(X_10, X_11), multiply(X_11, X_11))))),
    inference(subst, [],
              [refute_0_92 :
               [bind(X0,
                     $fot(multiply(multiply(multiply(Y,
                                multiply(X_10, X_11)),
                              multiply(identity, multiply(X_11, X_11))),
                            multiply(multiply(Y, multiply(X_10, X_11)),
                              multiply(identity, multiply(X_11, X_11)))))),
                bind(Y0,
                     $fot(multiply(multiply(Y,
                              multiply(multiply(X_10, X_11),
                                multiply(X_11, X_11))),
                            multiply(multiply(Y, multiply(X_10, X_11)),
                              multiply(identity, multiply(X_11, X_11)))))),
                bind(Z0,
                     $fot(multiply(multiply(Y,
                              multiply(multiply(X_10, X_11),
                                multiply(X_11, X_11))),
                            multiply(Y,
                              multiply(multiply(X_10, X_11),
                                multiply(X_11, X_11))))))]])).

cnf(refute_0_319, plain,
    (multiply(multiply(Y,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(multiply(Y, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11)))) !=
     multiply(multiply(Y,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(Y, multiply(multiply(X_10, X_11), multiply(X_11, X_11)))) |
     multiply(multiply(multiply(Y, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11))),
       multiply(multiply(Y, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11)))) =
     multiply(multiply(Y,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(Y, multiply(multiply(X_10, X_11), multiply(X_11, X_11))))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(multiply(Y,
                            multiply(X_10, X_11)),
                          multiply(identity, multiply(X_11, X_11))),
                        multiply(multiply(Y, multiply(X_10, X_11)),
                          multiply(identity, multiply(X_11, X_11)))),
                      multiply(multiply(Y,
                          multiply(multiply(X_10, X_11),
                            multiply(X_11, X_11))),
                        multiply(multiply(Y, multiply(X_10, X_11)),
                          multiply(identity, multiply(X_11, X_11))))))],
              [refute_0_317, refute_0_318])).

cnf(refute_0_320, plain,
    (multiply(multiply(multiply(Y, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11))),
       multiply(multiply(Y, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11)))) =
     multiply(multiply(Y,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(Y, multiply(multiply(X_10, X_11), multiply(X_11, X_11))))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(Y,
                          multiply(multiply(X_10, X_11),
                            multiply(X_11, X_11))),
                        multiply(multiply(Y, multiply(X_10, X_11)),
                          multiply(identity, multiply(X_11, X_11)))),
                      multiply(multiply(Y,
                          multiply(multiply(X_10, X_11),
                            multiply(X_11, X_11))),
                        multiply(Y,
                          multiply(multiply(X_10, X_11),
                            multiply(X_11, X_11))))))],
              [refute_0_313, refute_0_319])).

cnf(refute_0_321, plain,
    (multiply(identity,
       multiply(multiply(multiply(Y, multiply(X_10, X_11)),
           multiply(identity, multiply(X_11, X_11))),
         multiply(multiply(Y, multiply(X_10, X_11)),
           multiply(identity, multiply(X_11, X_11))))) =
     multiply(identity,
       multiply(multiply(multiply(Y, multiply(X_10, X_11)),
           multiply(identity, multiply(X_11, X_11))),
         multiply(multiply(Y, multiply(X_10, X_11)),
           multiply(identity, multiply(X_11, X_11)))))),
    introduced(tautology,
               [refl,
                [$fot(multiply(identity,
                        multiply(multiply(multiply(Y,
                              multiply(X_10, X_11)),
                            multiply(identity, multiply(X_11, X_11))),
                          multiply(multiply(Y, multiply(X_10, X_11)),
                            multiply(identity,
                              multiply(X_11, X_11))))))]])).

cnf(refute_0_322, plain,
    (multiply(multiply(multiply(Y, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11))),
       multiply(multiply(Y, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11)))) !=
     multiply(multiply(Y,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(Y, multiply(multiply(X_10, X_11), multiply(X_11, X_11)))) |
     multiply(identity,
       multiply(multiply(multiply(Y, multiply(X_10, X_11)),
           multiply(identity, multiply(X_11, X_11))),
         multiply(multiply(Y, multiply(X_10, X_11)),
           multiply(identity, multiply(X_11, X_11))))) !=
     multiply(identity,
       multiply(multiply(multiply(Y, multiply(X_10, X_11)),
           multiply(identity, multiply(X_11, X_11))),
         multiply(multiply(Y, multiply(X_10, X_11)),
           multiply(identity, multiply(X_11, X_11))))) |
     multiply(identity,
       multiply(multiply(multiply(Y, multiply(X_10, X_11)),
           multiply(identity, multiply(X_11, X_11))),
         multiply(multiply(Y, multiply(X_10, X_11)),
           multiply(identity, multiply(X_11, X_11))))) =
     multiply(identity,
       multiply(multiply(Y,
           multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
         multiply(Y,
           multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(identity,
                          multiply(multiply(multiply(Y,
                                multiply(X_10, X_11)),
                              multiply(identity, multiply(X_11, X_11))),
                            multiply(multiply(Y, multiply(X_10, X_11)),
                              multiply(identity, multiply(X_11, X_11))))),
                        multiply(identity,
                          multiply(multiply(multiply(Y,
                                multiply(X_10, X_11)),
                              multiply(identity, multiply(X_11, X_11))),
                            multiply(multiply(Y, multiply(X_10, X_11)),
                              multiply(identity,
                                multiply(X_11, X_11))))))), [1, 1],
                 $fot(multiply(multiply(Y,
                          multiply(multiply(X_10, X_11),
                            multiply(X_11, X_11))),
                        multiply(Y,
                          multiply(multiply(X_10, X_11),
                            multiply(X_11, X_11)))))]])).

cnf(refute_0_323, plain,
    (multiply(multiply(multiply(Y, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11))),
       multiply(multiply(Y, multiply(X_10, X_11)),
         multiply(identity, multiply(X_11, X_11)))) !=
     multiply(multiply(Y,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(Y, multiply(multiply(X_10, X_11), multiply(X_11, X_11)))) |
     multiply(identity,
       multiply(multiply(multiply(Y, multiply(X_10, X_11)),
           multiply(identity, multiply(X_11, X_11))),
         multiply(multiply(Y, multiply(X_10, X_11)),
           multiply(identity, multiply(X_11, X_11))))) =
     multiply(identity,
       multiply(multiply(Y,
           multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
         multiply(Y,
           multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
    inference(resolve,
              [$cnf($equal(multiply(identity,
                        multiply(multiply(multiply(Y,
                              multiply(X_10, X_11)),
                            multiply(identity, multiply(X_11, X_11))),
                          multiply(multiply(Y, multiply(X_10, X_11)),
                            multiply(identity, multiply(X_11, X_11))))),
                      multiply(identity,
                        multiply(multiply(multiply(Y,
                              multiply(X_10, X_11)),
                            multiply(identity, multiply(X_11, X_11))),
                          multiply(multiply(Y, multiply(X_10, X_11)),
                            multiply(identity, multiply(X_11, X_11)))))))],
              [refute_0_321, refute_0_322])).

cnf(refute_0_324, plain,
    (multiply(identity,
       multiply(multiply(multiply(Y, multiply(X_10, X_11)),
           multiply(identity, multiply(X_11, X_11))),
         multiply(multiply(Y, multiply(X_10, X_11)),
           multiply(identity, multiply(X_11, X_11))))) =
     multiply(identity,
       multiply(multiply(Y,
           multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
         multiply(Y,
           multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(multiply(Y,
                            multiply(X_10, X_11)),
                          multiply(identity, multiply(X_11, X_11))),
                        multiply(multiply(Y, multiply(X_10, X_11)),
                          multiply(identity, multiply(X_11, X_11)))),
                      multiply(multiply(Y,
                          multiply(multiply(X_10, X_11),
                            multiply(X_11, X_11))),
                        multiply(Y,
                          multiply(multiply(X_10, X_11),
                            multiply(X_11, X_11))))))],
              [refute_0_320, refute_0_323])).

cnf(refute_0_325, plain,
    (multiply(X_10,
       multiply(identity,
         multiply(multiply(multiply(Y, multiply(X_10, X_11)),
             multiply(identity, multiply(X_11, X_11))),
           multiply(multiply(Y, multiply(X_10, X_11)),
             multiply(identity, multiply(X_11, X_11)))))) =
     multiply(X_10,
       multiply(identity,
         multiply(multiply(multiply(Y, multiply(X_10, X_11)),
             multiply(identity, multiply(X_11, X_11))),
           multiply(multiply(Y, multiply(X_10, X_11)),
             multiply(identity, multiply(X_11, X_11))))))),
    introduced(tautology,
               [refl,
                [$fot(multiply(X_10,
                        multiply(identity,
                          multiply(multiply(multiply(Y,
                                multiply(X_10, X_11)),
                              multiply(identity, multiply(X_11, X_11))),
                            multiply(multiply(Y, multiply(X_10, X_11)),
                              multiply(identity,
                                multiply(X_11, X_11)))))))]])).

cnf(refute_0_326, plain,
    (multiply(X_10,
       multiply(identity,
         multiply(multiply(multiply(Y, multiply(X_10, X_11)),
             multiply(identity, multiply(X_11, X_11))),
           multiply(multiply(Y, multiply(X_10, X_11)),
             multiply(identity, multiply(X_11, X_11)))))) !=
     multiply(X_10,
       multiply(identity,
         multiply(multiply(multiply(Y, multiply(X_10, X_11)),
             multiply(identity, multiply(X_11, X_11))),
           multiply(multiply(Y, multiply(X_10, X_11)),
             multiply(identity, multiply(X_11, X_11)))))) |
     multiply(identity,
       multiply(multiply(multiply(Y, multiply(X_10, X_11)),
           multiply(identity, multiply(X_11, X_11))),
         multiply(multiply(Y, multiply(X_10, X_11)),
           multiply(identity, multiply(X_11, X_11))))) !=
     multiply(identity,
       multiply(multiply(Y,
           multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
         multiply(Y,
           multiply(multiply(X_10, X_11), multiply(X_11, X_11))))) |
     multiply(X_10,
       multiply(identity,
         multiply(multiply(multiply(Y, multiply(X_10, X_11)),
             multiply(identity, multiply(X_11, X_11))),
           multiply(multiply(Y, multiply(X_10, X_11)),
             multiply(identity, multiply(X_11, X_11)))))) =
     multiply(X_10,
       multiply(identity,
         multiply(multiply(Y,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
           multiply(Y,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11))))))),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(X_10,
                          multiply(identity,
                            multiply(multiply(multiply(Y,
                                  multiply(X_10, X_11)),
                                multiply(identity, multiply(X_11, X_11))),
                              multiply(multiply(Y, multiply(X_10, X_11)),
                                multiply(identity,
                                  multiply(X_11, X_11)))))),
                        multiply(X_10,
                          multiply(identity,
                            multiply(multiply(multiply(Y,
                                  multiply(X_10, X_11)),
                                multiply(identity, multiply(X_11, X_11))),
                              multiply(multiply(Y, multiply(X_10, X_11)),
                                multiply(identity,
                                  multiply(X_11, X_11)))))))), [1, 1],
                 $fot(multiply(identity,
                        multiply(multiply(Y,
                            multiply(multiply(X_10, X_11),
                              multiply(X_11, X_11))),
                          multiply(Y,
                            multiply(multiply(X_10, X_11),
                              multiply(X_11, X_11))))))]])).

cnf(refute_0_327, plain,
    (multiply(identity,
       multiply(multiply(multiply(Y, multiply(X_10, X_11)),
           multiply(identity, multiply(X_11, X_11))),
         multiply(multiply(Y, multiply(X_10, X_11)),
           multiply(identity, multiply(X_11, X_11))))) !=
     multiply(identity,
       multiply(multiply(Y,
           multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
         multiply(Y,
           multiply(multiply(X_10, X_11), multiply(X_11, X_11))))) |
     multiply(X_10,
       multiply(identity,
         multiply(multiply(multiply(Y, multiply(X_10, X_11)),
             multiply(identity, multiply(X_11, X_11))),
           multiply(multiply(Y, multiply(X_10, X_11)),
             multiply(identity, multiply(X_11, X_11)))))) =
     multiply(X_10,
       multiply(identity,
         multiply(multiply(Y,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
           multiply(Y,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11))))))),
    inference(resolve,
              [$cnf($equal(multiply(X_10,
                        multiply(identity,
                          multiply(multiply(multiply(Y,
                                multiply(X_10, X_11)),
                              multiply(identity, multiply(X_11, X_11))),
                            multiply(multiply(Y, multiply(X_10, X_11)),
                              multiply(identity, multiply(X_11, X_11)))))),
                      multiply(X_10,
                        multiply(identity,
                          multiply(multiply(multiply(Y,
                                multiply(X_10, X_11)),
                              multiply(identity, multiply(X_11, X_11))),
                            multiply(multiply(Y, multiply(X_10, X_11)),
                              multiply(identity,
                                multiply(X_11, X_11))))))))],
              [refute_0_325, refute_0_326])).

cnf(refute_0_328, plain,
    (multiply(X_10,
       multiply(identity,
         multiply(multiply(multiply(Y, multiply(X_10, X_11)),
             multiply(identity, multiply(X_11, X_11))),
           multiply(multiply(Y, multiply(X_10, X_11)),
             multiply(identity, multiply(X_11, X_11)))))) =
     multiply(X_10,
       multiply(identity,
         multiply(multiply(Y,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
           multiply(Y,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11))))))),
    inference(resolve,
              [$cnf($equal(multiply(identity,
                        multiply(multiply(multiply(Y,
                              multiply(X_10, X_11)),
                            multiply(identity, multiply(X_11, X_11))),
                          multiply(multiply(Y, multiply(X_10, X_11)),
                            multiply(identity, multiply(X_11, X_11))))),
                      multiply(identity,
                        multiply(multiply(Y,
                            multiply(multiply(X_10, X_11),
                              multiply(X_11, X_11))),
                          multiply(Y,
                            multiply(multiply(X_10, X_11),
                              multiply(X_11, X_11)))))))],
              [refute_0_324, refute_0_327])).

cnf(refute_0_329, plain,
    (multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))))))) =
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11)))))))),
    introduced(tautology,
               [refl,
                [$fot(multiply(multiply(X_10,
                          multiply(identity,
                            multiply(multiply(Y,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))),
                              multiply(Y,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11)))))),
                        multiply(X_10,
                          multiply(identity,
                            multiply(multiply(multiply(Y,
                                  multiply(X_10, X_11)),
                                multiply(identity, multiply(X_11, X_11))),
                              multiply(multiply(Y, multiply(X_10, X_11)),
                                multiply(identity,
                                  multiply(X_11, X_11))))))))]])).

cnf(refute_0_330, plain,
    (multiply(X_10,
       multiply(identity,
         multiply(multiply(multiply(Y, multiply(X_10, X_11)),
             multiply(identity, multiply(X_11, X_11))),
           multiply(multiply(Y, multiply(X_10, X_11)),
             multiply(identity, multiply(X_11, X_11)))))) !=
     multiply(X_10,
       multiply(identity,
         multiply(multiply(Y,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
           multiply(Y,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))) |
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))))))) !=
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))))))) |
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))))))) =
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))))),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(multiply(X_10,
                            multiply(identity,
                              multiply(multiply(Y,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11))),
                                multiply(Y,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11)))))),
                          multiply(X_10,
                            multiply(identity,
                              multiply(multiply(multiply(Y,
                                    multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11))),
                                multiply(multiply(Y, multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11))))))),
                        multiply(multiply(X_10,
                            multiply(identity,
                              multiply(multiply(Y,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11))),
                                multiply(Y,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11)))))),
                          multiply(X_10,
                            multiply(identity,
                              multiply(multiply(multiply(Y,
                                    multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11))),
                                multiply(multiply(Y, multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11))))))))), [1, 1],
                 $fot(multiply(X_10,
                        multiply(identity,
                          multiply(multiply(Y,
                              multiply(multiply(X_10, X_11),
                                multiply(X_11, X_11))),
                            multiply(Y,
                              multiply(multiply(X_10, X_11),
                                multiply(X_11, X_11)))))))]])).

cnf(refute_0_331, plain,
    (multiply(X_10,
       multiply(identity,
         multiply(multiply(multiply(Y, multiply(X_10, X_11)),
             multiply(identity, multiply(X_11, X_11))),
           multiply(multiply(Y, multiply(X_10, X_11)),
             multiply(identity, multiply(X_11, X_11)))))) !=
     multiply(X_10,
       multiply(identity,
         multiply(multiply(Y,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
           multiply(Y,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))) |
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))))))) =
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(X_10,
                          multiply(identity,
                            multiply(multiply(Y,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))),
                              multiply(Y,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11)))))),
                        multiply(X_10,
                          multiply(identity,
                            multiply(multiply(multiply(Y,
                                  multiply(X_10, X_11)),
                                multiply(identity, multiply(X_11, X_11))),
                              multiply(multiply(Y, multiply(X_10, X_11)),
                                multiply(identity,
                                  multiply(X_11, X_11))))))),
                      multiply(multiply(X_10,
                          multiply(identity,
                            multiply(multiply(Y,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))),
                              multiply(Y,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11)))))),
                        multiply(X_10,
                          multiply(identity,
                            multiply(multiply(multiply(Y,
                                  multiply(X_10, X_11)),
                                multiply(identity, multiply(X_11, X_11))),
                              multiply(multiply(Y, multiply(X_10, X_11)),
                                multiply(identity,
                                  multiply(X_11, X_11)))))))))],
              [refute_0_329, refute_0_330])).

cnf(refute_0_332, plain,
    (multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))))))) =
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))))),
    inference(resolve,
              [$cnf($equal(multiply(X_10,
                        multiply(identity,
                          multiply(multiply(multiply(Y,
                                multiply(X_10, X_11)),
                              multiply(identity, multiply(X_11, X_11))),
                            multiply(multiply(Y, multiply(X_10, X_11)),
                              multiply(identity, multiply(X_11, X_11)))))),
                      multiply(X_10,
                        multiply(identity,
                          multiply(multiply(Y,
                              multiply(multiply(X_10, X_11),
                                multiply(X_11, X_11))),
                            multiply(Y,
                              multiply(multiply(X_10, X_11),
                                multiply(X_11, X_11))))))))],
              [refute_0_328, refute_0_331])).

cnf(refute_0_333, plain,
    (multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))))))) =
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11)))))))),
    introduced(tautology,
               [refl,
                [$fot(multiply(multiply(X_10,
                          multiply(identity,
                            multiply(multiply(multiply(Y,
                                  multiply(X_10, X_11)),
                                multiply(identity, multiply(X_11, X_11))),
                              multiply(multiply(Y, multiply(X_10, X_11)),
                                multiply(identity,
                                  multiply(X_11, X_11)))))),
                        multiply(X_10,
                          multiply(identity,
                            multiply(multiply(multiply(Y,
                                  multiply(X_10, X_11)),
                                multiply(identity, multiply(X_11, X_11))),
                              multiply(multiply(Y, multiply(X_10, X_11)),
                                multiply(identity,
                                  multiply(X_11, X_11))))))))]])).

cnf(refute_0_334, plain,
    (multiply(X_10,
       multiply(identity,
         multiply(multiply(multiply(Y, multiply(X_10, X_11)),
             multiply(identity, multiply(X_11, X_11))),
           multiply(multiply(Y, multiply(X_10, X_11)),
             multiply(identity, multiply(X_11, X_11)))))) !=
     multiply(X_10,
       multiply(identity,
         multiply(multiply(Y,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
           multiply(Y,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))) |
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))))))) !=
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))))))) |
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))))))) =
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11)))))))),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(multiply(X_10,
                            multiply(identity,
                              multiply(multiply(multiply(Y,
                                    multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11))),
                                multiply(multiply(Y, multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11)))))),
                          multiply(X_10,
                            multiply(identity,
                              multiply(multiply(multiply(Y,
                                    multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11))),
                                multiply(multiply(Y, multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11))))))),
                        multiply(multiply(X_10,
                            multiply(identity,
                              multiply(multiply(multiply(Y,
                                    multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11))),
                                multiply(multiply(Y, multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11)))))),
                          multiply(X_10,
                            multiply(identity,
                              multiply(multiply(multiply(Y,
                                    multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11))),
                                multiply(multiply(Y, multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11))))))))), [1, 0],
                 $fot(multiply(X_10,
                        multiply(identity,
                          multiply(multiply(Y,
                              multiply(multiply(X_10, X_11),
                                multiply(X_11, X_11))),
                            multiply(Y,
                              multiply(multiply(X_10, X_11),
                                multiply(X_11, X_11)))))))]])).

cnf(refute_0_335, plain,
    (multiply(X_10,
       multiply(identity,
         multiply(multiply(multiply(Y, multiply(X_10, X_11)),
             multiply(identity, multiply(X_11, X_11))),
           multiply(multiply(Y, multiply(X_10, X_11)),
             multiply(identity, multiply(X_11, X_11)))))) !=
     multiply(X_10,
       multiply(identity,
         multiply(multiply(Y,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
           multiply(Y,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))) |
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))))))) =
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11)))))))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(X_10,
                          multiply(identity,
                            multiply(multiply(multiply(Y,
                                  multiply(X_10, X_11)),
                                multiply(identity, multiply(X_11, X_11))),
                              multiply(multiply(Y, multiply(X_10, X_11)),
                                multiply(identity,
                                  multiply(X_11, X_11)))))),
                        multiply(X_10,
                          multiply(identity,
                            multiply(multiply(multiply(Y,
                                  multiply(X_10, X_11)),
                                multiply(identity, multiply(X_11, X_11))),
                              multiply(multiply(Y, multiply(X_10, X_11)),
                                multiply(identity,
                                  multiply(X_11, X_11))))))),
                      multiply(multiply(X_10,
                          multiply(identity,
                            multiply(multiply(multiply(Y,
                                  multiply(X_10, X_11)),
                                multiply(identity, multiply(X_11, X_11))),
                              multiply(multiply(Y, multiply(X_10, X_11)),
                                multiply(identity,
                                  multiply(X_11, X_11)))))),
                        multiply(X_10,
                          multiply(identity,
                            multiply(multiply(multiply(Y,
                                  multiply(X_10, X_11)),
                                multiply(identity, multiply(X_11, X_11))),
                              multiply(multiply(Y, multiply(X_10, X_11)),
                                multiply(identity,
                                  multiply(X_11, X_11)))))))))],
              [refute_0_333, refute_0_334])).

cnf(refute_0_336, plain,
    (multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))))))) =
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11)))))))),
    inference(resolve,
              [$cnf($equal(multiply(X_10,
                        multiply(identity,
                          multiply(multiply(multiply(Y,
                                multiply(X_10, X_11)),
                              multiply(identity, multiply(X_11, X_11))),
                            multiply(multiply(Y, multiply(X_10, X_11)),
                              multiply(identity, multiply(X_11, X_11)))))),
                      multiply(X_10,
                        multiply(identity,
                          multiply(multiply(Y,
                              multiply(multiply(X_10, X_11),
                                multiply(X_11, X_11))),
                            multiply(Y,
                              multiply(multiply(X_10, X_11),
                                multiply(X_11, X_11))))))))],
              [refute_0_328, refute_0_335])).

cnf(refute_0_337, plain,
    (multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))))))) !=
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))))))) |
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))))))) !=
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))))))) |
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))))))) =
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))))),
    inference(subst, [],
              [refute_0_92 :
               [bind(X0,
                     $fot(multiply(multiply(X_10,
                              multiply(identity,
                                multiply(multiply(multiply(Y,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11))),
                                  multiply(multiply(Y,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11)))))),
                            multiply(X_10,
                              multiply(identity,
                                multiply(multiply(multiply(Y,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11))),
                                  multiply(multiply(Y,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11))))))))),
                bind(Y0,
                     $fot(multiply(multiply(X_10,
                              multiply(identity,
                                multiply(multiply(Y,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11))),
                                  multiply(Y,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11)))))),
                            multiply(X_10,
                              multiply(identity,
                                multiply(multiply(multiply(Y,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11))),
                                  multiply(multiply(Y,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11))))))))),
                bind(Z0,
                     $fot(multiply(multiply(X_10,
                              multiply(identity,
                                multiply(multiply(Y,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11))),
                                  multiply(Y,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11)))))),
                            multiply(X_10,
                              multiply(identity,
                                multiply(multiply(Y,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11))),
                                  multiply(Y,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11)))))))))]])).

cnf(refute_0_338, plain,
    (multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))))))) !=
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))))))) |
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))))))) =
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(X_10,
                          multiply(identity,
                            multiply(multiply(multiply(Y,
                                  multiply(X_10, X_11)),
                                multiply(identity, multiply(X_11, X_11))),
                              multiply(multiply(Y, multiply(X_10, X_11)),
                                multiply(identity,
                                  multiply(X_11, X_11)))))),
                        multiply(X_10,
                          multiply(identity,
                            multiply(multiply(multiply(Y,
                                  multiply(X_10, X_11)),
                                multiply(identity, multiply(X_11, X_11))),
                              multiply(multiply(Y, multiply(X_10, X_11)),
                                multiply(identity,
                                  multiply(X_11, X_11))))))),
                      multiply(multiply(X_10,
                          multiply(identity,
                            multiply(multiply(Y,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))),
                              multiply(Y,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11)))))),
                        multiply(X_10,
                          multiply(identity,
                            multiply(multiply(multiply(Y,
                                  multiply(X_10, X_11)),
                                multiply(identity, multiply(X_11, X_11))),
                              multiply(multiply(Y, multiply(X_10, X_11)),
                                multiply(identity,
                                  multiply(X_11, X_11)))))))))],
              [refute_0_336, refute_0_337])).

cnf(refute_0_339, plain,
    (multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))))))) =
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(X_10,
                          multiply(identity,
                            multiply(multiply(Y,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))),
                              multiply(Y,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11)))))),
                        multiply(X_10,
                          multiply(identity,
                            multiply(multiply(multiply(Y,
                                  multiply(X_10, X_11)),
                                multiply(identity, multiply(X_11, X_11))),
                              multiply(multiply(Y, multiply(X_10, X_11)),
                                multiply(identity,
                                  multiply(X_11, X_11))))))),
                      multiply(multiply(X_10,
                          multiply(identity,
                            multiply(multiply(Y,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))),
                              multiply(Y,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11)))))),
                        multiply(X_10,
                          multiply(identity,
                            multiply(multiply(Y,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))),
                              multiply(Y,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11)))))))))],
              [refute_0_332, refute_0_338])).

cnf(refute_0_340, plain,
    (multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11))),
               multiply(multiply(Y, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11)))))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11))),
               multiply(multiply(Y, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11)))))))) =
     multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11))),
               multiply(multiply(Y, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11)))))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11))),
               multiply(multiply(Y, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11))))))))),
    introduced(tautology,
               [refl,
                [$fot(multiply(identity,
                        multiply(multiply(X_10,
                            multiply(identity,
                              multiply(multiply(multiply(Y,
                                    multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11))),
                                multiply(multiply(Y, multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11)))))),
                          multiply(X_10,
                            multiply(identity,
                              multiply(multiply(multiply(Y,
                                    multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11))),
                                multiply(multiply(Y, multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11)))))))))]])).

cnf(refute_0_341, plain,
    (multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))))))) !=
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))))))) |
     multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11))),
               multiply(multiply(Y, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11)))))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11))),
               multiply(multiply(Y, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11)))))))) !=
     multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11))),
               multiply(multiply(Y, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11)))))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11))),
               multiply(multiply(Y, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11)))))))) |
     multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11))),
               multiply(multiply(Y, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11)))))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11))),
               multiply(multiply(Y, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11)))))))) =
     multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(Y,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
               multiply(Y,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(Y,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
               multiply(Y,
                 multiply(multiply(X_10, X_11),
                   multiply(X_11, X_11))))))))),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(identity,
                          multiply(multiply(X_10,
                              multiply(identity,
                                multiply(multiply(multiply(Y,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11))),
                                  multiply(multiply(Y,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11)))))),
                            multiply(X_10,
                              multiply(identity,
                                multiply(multiply(multiply(Y,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11))),
                                  multiply(multiply(Y,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11)))))))),
                        multiply(identity,
                          multiply(multiply(X_10,
                              multiply(identity,
                                multiply(multiply(multiply(Y,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11))),
                                  multiply(multiply(Y,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11)))))),
                            multiply(X_10,
                              multiply(identity,
                                multiply(multiply(multiply(Y,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11))),
                                  multiply(multiply(Y,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11)))))))))),
                 [1, 1],
                 $fot(multiply(multiply(X_10,
                          multiply(identity,
                            multiply(multiply(Y,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))),
                              multiply(Y,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11)))))),
                        multiply(X_10,
                          multiply(identity,
                            multiply(multiply(Y,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))),
                              multiply(Y,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))))))))]])).

cnf(refute_0_342, plain,
    (multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))),
             multiply(multiply(Y, multiply(X_10, X_11)),
               multiply(identity, multiply(X_11, X_11))))))) !=
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))))))) |
     multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11))),
               multiply(multiply(Y, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11)))))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11))),
               multiply(multiply(Y, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11)))))))) =
     multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(Y,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
               multiply(Y,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(Y,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
               multiply(Y,
                 multiply(multiply(X_10, X_11),
                   multiply(X_11, X_11))))))))),
    inference(resolve,
              [$cnf($equal(multiply(identity,
                        multiply(multiply(X_10,
                            multiply(identity,
                              multiply(multiply(multiply(Y,
                                    multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11))),
                                multiply(multiply(Y, multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11)))))),
                          multiply(X_10,
                            multiply(identity,
                              multiply(multiply(multiply(Y,
                                    multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11))),
                                multiply(multiply(Y, multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11)))))))),
                      multiply(identity,
                        multiply(multiply(X_10,
                            multiply(identity,
                              multiply(multiply(multiply(Y,
                                    multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11))),
                                multiply(multiply(Y, multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11)))))),
                          multiply(X_10,
                            multiply(identity,
                              multiply(multiply(multiply(Y,
                                    multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11))),
                                multiply(multiply(Y, multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11))))))))))],
              [refute_0_340, refute_0_341])).

cnf(refute_0_343, plain,
    (multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11))),
               multiply(multiply(Y, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11)))))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11))),
               multiply(multiply(Y, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11)))))))) =
     multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(Y,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
               multiply(Y,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(Y,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
               multiply(Y,
                 multiply(multiply(X_10, X_11),
                   multiply(X_11, X_11))))))))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(X_10,
                          multiply(identity,
                            multiply(multiply(multiply(Y,
                                  multiply(X_10, X_11)),
                                multiply(identity, multiply(X_11, X_11))),
                              multiply(multiply(Y, multiply(X_10, X_11)),
                                multiply(identity,
                                  multiply(X_11, X_11)))))),
                        multiply(X_10,
                          multiply(identity,
                            multiply(multiply(multiply(Y,
                                  multiply(X_10, X_11)),
                                multiply(identity, multiply(X_11, X_11))),
                              multiply(multiply(Y, multiply(X_10, X_11)),
                                multiply(identity,
                                  multiply(X_11, X_11))))))),
                      multiply(multiply(X_10,
                          multiply(identity,
                            multiply(multiply(Y,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))),
                              multiply(Y,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11)))))),
                        multiply(X_10,
                          multiply(identity,
                            multiply(multiply(Y,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))),
                              multiply(Y,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11)))))))))],
              [refute_0_339, refute_0_342])).

cnf(refute_0_344, plain,
    (multiply(Y,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(Y, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11)))))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(Y, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))))))))) =
     multiply(Y,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(Y, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11)))))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(Y, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11)))))))))),
    introduced(tautology,
               [refl,
                [$fot(multiply(Y,
                        multiply(identity,
                          multiply(multiply(X_10,
                              multiply(identity,
                                multiply(multiply(multiply(Y,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11))),
                                  multiply(multiply(Y,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11)))))),
                            multiply(X_10,
                              multiply(identity,
                                multiply(multiply(multiply(Y,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11))),
                                  multiply(multiply(Y,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11))))))))))]])).

cnf(refute_0_345, plain,
    (multiply(Y,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(Y, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11)))))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(Y, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))))))))) !=
     multiply(Y,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(Y, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11)))))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(Y, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))))))))) |
     multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11))),
               multiply(multiply(Y, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11)))))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11))),
               multiply(multiply(Y, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11)))))))) !=
     multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(Y,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
               multiply(Y,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(Y,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
               multiply(Y,
                 multiply(multiply(X_10, X_11),
                   multiply(X_11, X_11)))))))) |
     multiply(Y,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(Y, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11)))))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(Y, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))))))))) =
     multiply(Y,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(Y,
                   multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                 multiply(Y,
                   multiply(multiply(X_10, X_11),
                     multiply(X_11, X_11)))))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(Y,
                   multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                 multiply(Y,
                   multiply(multiply(X_10, X_11),
                     multiply(X_11, X_11)))))))))),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(Y,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(Y,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(Y,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(Y,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(Y,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))))))))),
                        multiply(Y,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(Y,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(Y,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(Y,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(Y,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))))))))))),
                 [1, 1],
                 $fot(multiply(identity,
                        multiply(multiply(X_10,
                            multiply(identity,
                              multiply(multiply(Y,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11))),
                                multiply(Y,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11)))))),
                          multiply(X_10,
                            multiply(identity,
                              multiply(multiply(Y,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11))),
                                multiply(Y,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11)))))))))]])).

cnf(refute_0_346, plain,
    (multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11))),
               multiply(multiply(Y, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11)))))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11))),
               multiply(multiply(Y, multiply(X_10, X_11)),
                 multiply(identity, multiply(X_11, X_11)))))))) !=
     multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(Y,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
               multiply(Y,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(Y,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
               multiply(Y,
                 multiply(multiply(X_10, X_11),
                   multiply(X_11, X_11)))))))) |
     multiply(Y,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(Y, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11)))))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(Y, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))))))))) =
     multiply(Y,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(Y,
                   multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                 multiply(Y,
                   multiply(multiply(X_10, X_11),
                     multiply(X_11, X_11)))))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(Y,
                   multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                 multiply(Y,
                   multiply(multiply(X_10, X_11),
                     multiply(X_11, X_11)))))))))),
    inference(resolve,
              [$cnf($equal(multiply(Y,
                        multiply(identity,
                          multiply(multiply(X_10,
                              multiply(identity,
                                multiply(multiply(multiply(Y,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11))),
                                  multiply(multiply(Y,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11)))))),
                            multiply(X_10,
                              multiply(identity,
                                multiply(multiply(multiply(Y,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11))),
                                  multiply(multiply(Y,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11))))))))),
                      multiply(Y,
                        multiply(identity,
                          multiply(multiply(X_10,
                              multiply(identity,
                                multiply(multiply(multiply(Y,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11))),
                                  multiply(multiply(Y,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11)))))),
                            multiply(X_10,
                              multiply(identity,
                                multiply(multiply(multiply(Y,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11))),
                                  multiply(multiply(Y,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11)))))))))))],
              [refute_0_344, refute_0_345])).

cnf(refute_0_347, plain,
    (multiply(Y,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(Y, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11)))))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(Y, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))))))))) =
     multiply(Y,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(Y,
                   multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                 multiply(Y,
                   multiply(multiply(X_10, X_11),
                     multiply(X_11, X_11)))))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(Y,
                   multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                 multiply(Y,
                   multiply(multiply(X_10, X_11),
                     multiply(X_11, X_11)))))))))),
    inference(resolve,
              [$cnf($equal(multiply(identity,
                        multiply(multiply(X_10,
                            multiply(identity,
                              multiply(multiply(multiply(Y,
                                    multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11))),
                                multiply(multiply(Y, multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11)))))),
                          multiply(X_10,
                            multiply(identity,
                              multiply(multiply(multiply(Y,
                                    multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11))),
                                multiply(multiply(Y, multiply(X_10, X_11)),
                                  multiply(identity,
                                    multiply(X_11, X_11)))))))),
                      multiply(identity,
                        multiply(multiply(X_10,
                            multiply(identity,
                              multiply(multiply(Y,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11))),
                                multiply(Y,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11)))))),
                          multiply(X_10,
                            multiply(identity,
                              multiply(multiply(Y,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11))),
                                multiply(Y,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11))))))))))],
              [refute_0_343, refute_0_346])).

cnf(refute_0_348, plain,
    (multiply(Y,
       multiply(Y,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(Y, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(Y, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))))))) =
     multiply(Y,
       multiply(Y,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(Y, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(Y, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))))))))))),
    introduced(tautology,
               [refl,
                [$fot(multiply(Y,
                        multiply(Y,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(Y,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(Y,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(Y,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(Y,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11)))))))))))]])).

cnf(refute_0_349, plain,
    (multiply(Y,
       multiply(Y,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(Y, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(Y, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))))))) !=
     multiply(Y,
       multiply(Y,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(Y, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(Y, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))))))) |
     multiply(Y,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(Y, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11)))))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(Y, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))))))))) !=
     multiply(Y,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(Y,
                   multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                 multiply(Y,
                   multiply(multiply(X_10, X_11),
                     multiply(X_11, X_11)))))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(Y,
                   multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                 multiply(Y,
                   multiply(multiply(X_10, X_11),
                     multiply(X_11, X_11))))))))) |
     multiply(Y,
       multiply(Y,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(Y, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(Y, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))))))) =
     multiply(Y,
       multiply(Y,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(Y,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(Y,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(Y,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(Y,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11))))))))))),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(Y,
                          multiply(Y,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(Y,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(Y,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11)))))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(Y,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(Y,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11)))))))))),
                        multiply(Y,
                          multiply(Y,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(Y,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(Y,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11)))))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(Y,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(Y,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11)))))))))))),
                 [1, 1],
                 $fot(multiply(Y,
                        multiply(identity,
                          multiply(multiply(X_10,
                              multiply(identity,
                                multiply(multiply(Y,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11))),
                                  multiply(Y,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11)))))),
                            multiply(X_10,
                              multiply(identity,
                                multiply(multiply(Y,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11))),
                                  multiply(Y,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11))))))))))]])).

cnf(refute_0_350, plain,
    (multiply(Y,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(Y, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11)))))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))),
                 multiply(multiply(Y, multiply(X_10, X_11)),
                   multiply(identity, multiply(X_11, X_11))))))))) !=
     multiply(Y,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(Y,
                   multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                 multiply(Y,
                   multiply(multiply(X_10, X_11),
                     multiply(X_11, X_11)))))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(Y,
                   multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                 multiply(Y,
                   multiply(multiply(X_10, X_11),
                     multiply(X_11, X_11))))))))) |
     multiply(Y,
       multiply(Y,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(Y, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(Y, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))))))) =
     multiply(Y,
       multiply(Y,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(Y,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(Y,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(Y,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(Y,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11))))))))))),
    inference(resolve,
              [$cnf($equal(multiply(Y,
                        multiply(Y,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(Y,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(Y,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(Y,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(Y,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11)))))))))),
                      multiply(Y,
                        multiply(Y,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(Y,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(Y,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(Y,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(Y,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))))))))))))],
              [refute_0_348, refute_0_349])).

cnf(refute_0_351, plain,
    (multiply(Y,
       multiply(Y,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(Y, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(Y, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))))))) =
     multiply(Y,
       multiply(Y,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(Y,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(Y,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(Y,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(Y,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11))))))))))),
    inference(resolve,
              [$cnf($equal(multiply(Y,
                        multiply(identity,
                          multiply(multiply(X_10,
                              multiply(identity,
                                multiply(multiply(multiply(Y,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11))),
                                  multiply(multiply(Y,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11)))))),
                            multiply(X_10,
                              multiply(identity,
                                multiply(multiply(multiply(Y,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11))),
                                  multiply(multiply(Y,
                                      multiply(X_10, X_11)),
                                    multiply(identity,
                                      multiply(X_11, X_11))))))))),
                      multiply(Y,
                        multiply(identity,
                          multiply(multiply(X_10,
                              multiply(identity,
                                multiply(multiply(Y,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11))),
                                  multiply(Y,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11)))))),
                            multiply(X_10,
                              multiply(identity,
                                multiply(multiply(Y,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11))),
                                  multiply(Y,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11)))))))))))],
              [refute_0_347, refute_0_350])).

cnf(refute_0_352, plain,
    (multiply(Y,
       multiply(Y,
         multiply(Y,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))))))))))) =
     multiply(Y,
       multiply(Y,
         multiply(Y,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))))))))),
    introduced(tautology,
               [refl,
                [$fot(multiply(Y,
                        multiply(Y,
                          multiply(Y,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(Y,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(Y,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11)))))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(Y,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(Y,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11,
                                            X_11))))))))))))]])).

cnf(refute_0_353, plain,
    (multiply(Y,
       multiply(Y,
         multiply(Y,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))))))))))) !=
     multiply(Y,
       multiply(Y,
         multiply(Y,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))))))))))) |
     multiply(Y,
       multiply(Y,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(Y, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(Y, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))))))) !=
     multiply(Y,
       multiply(Y,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(Y,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(Y,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(Y,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(Y,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))))))) |
     multiply(Y,
       multiply(Y,
         multiply(Y,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))))))))))) =
     multiply(Y,
       multiply(Y,
         multiply(Y,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(Y,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(Y,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(Y,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(Y,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11)))))))))))),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(Y,
                          multiply(Y,
                            multiply(Y,
                              multiply(identity,
                                multiply(multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(Y,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(Y,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11)))))),
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(Y,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(Y,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))))))))))),
                        multiply(Y,
                          multiply(Y,
                            multiply(Y,
                              multiply(identity,
                                multiply(multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(Y,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(Y,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11)))))),
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(Y,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(Y,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11,
                                              X_11))))))))))))), [1, 1],
                 $fot(multiply(Y,
                        multiply(Y,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(Y,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))),
                                    multiply(Y,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(Y,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))),
                                    multiply(Y,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11)))))))))))]])).

cnf(refute_0_354, plain,
    (multiply(Y,
       multiply(Y,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(Y, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11))),
                   multiply(multiply(Y, multiply(X_10, X_11)),
                     multiply(identity, multiply(X_11, X_11)))))))))) !=
     multiply(Y,
       multiply(Y,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(Y,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(Y,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(Y,
                     multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
                   multiply(Y,
                     multiply(multiply(X_10, X_11),
                       multiply(X_11, X_11)))))))))) |
     multiply(Y,
       multiply(Y,
         multiply(Y,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))))))))))) =
     multiply(Y,
       multiply(Y,
         multiply(Y,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(Y,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(Y,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(Y,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(Y,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11)))))))))))),
    inference(resolve,
              [$cnf($equal(multiply(Y,
                        multiply(Y,
                          multiply(Y,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(Y,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(Y,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11)))))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(Y,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(Y,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))))))))))),
                      multiply(Y,
                        multiply(Y,
                          multiply(Y,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(Y,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(Y,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11)))))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(Y,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(Y,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11,
                                            X_11)))))))))))))],
              [refute_0_352, refute_0_353])).

cnf(refute_0_355, plain,
    (multiply(Y,
       multiply(Y,
         multiply(Y,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))))))))))) =
     multiply(Y,
       multiply(Y,
         multiply(Y,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(Y,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(Y,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(Y,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(Y,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11)))))))))))),
    inference(resolve,
              [$cnf($equal(multiply(Y,
                        multiply(Y,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(Y,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(Y,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(multiply(Y,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11))),
                                    multiply(multiply(Y,
                                        multiply(X_10, X_11)),
                                      multiply(identity,
                                        multiply(X_11, X_11)))))))))),
                      multiply(Y,
                        multiply(Y,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(Y,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))),
                                    multiply(Y,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(Y,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))),
                                    multiply(Y,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))))))))))))],
              [refute_0_351, refute_0_354])).

cnf(refute_0_356, plain,
    (multiply(Y,
       multiply(Y,
         multiply(Y,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))))))))))) !=
     Y |
     multiply(Y,
       multiply(Y,
         multiply(Y,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))))))))))) !=
     multiply(Y,
       multiply(Y,
         multiply(Y,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(Y,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(Y,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(Y,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(Y,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))))))))))) |
     multiply(Y,
       multiply(Y,
         multiply(Y,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(Y,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(Y,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(Y,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(Y,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))))))))))) = Y),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(Y,
                          multiply(Y,
                            multiply(Y,
                              multiply(identity,
                                multiply(multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(Y,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(Y,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11)))))),
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(multiply(Y,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))),
                                        multiply(multiply(Y,
                                            multiply(X_10, X_11)),
                                          multiply(identity,
                                            multiply(X_11, X_11))))))))))),
                        Y)), [0],
                 $fot(multiply(Y,
                        multiply(Y,
                          multiply(Y,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(Y,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))),
                                      multiply(Y,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11)))))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(Y,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))),
                                      multiply(Y,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11,
                                            X_11))))))))))))]])).

cnf(refute_0_357, plain,
    (multiply(Y,
       multiply(Y,
         multiply(Y,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))),
                     multiply(multiply(Y, multiply(X_10, X_11)),
                       multiply(identity, multiply(X_11, X_11))))))))))) !=
     Y |
     multiply(Y,
       multiply(Y,
         multiply(Y,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(Y,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(Y,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(Y,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(Y,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))))))))))) = Y),
    inference(resolve,
              [$cnf($equal(multiply(Y,
                        multiply(Y,
                          multiply(Y,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(Y,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(Y,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11)))))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(Y,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(Y,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))))))))))),
                      multiply(Y,
                        multiply(Y,
                          multiply(Y,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(Y,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))),
                                      multiply(Y,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11)))))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(Y,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))),
                                      multiply(Y,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11,
                                            X_11)))))))))))))],
              [refute_0_355, refute_0_356])).

cnf(refute_0_358, plain,
    (multiply(Y,
       multiply(Y,
         multiply(Y,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(Y,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(Y,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(Y,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(Y,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))))))))))) = Y),
    inference(resolve,
              [$cnf($equal(multiply(Y,
                        multiply(Y,
                          multiply(Y,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(Y,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(Y,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11)))))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(multiply(Y,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))),
                                      multiply(multiply(Y,
                                          multiply(X_10, X_11)),
                                        multiply(identity,
                                          multiply(X_11, X_11))))))))))),
                      Y))], [refute_0_308, refute_0_357])).

cnf(refute_0_359, plain,
    (multiply(Y, multiply(multiply(X_10, X_11), multiply(X_11, X_11))) =
     multiply(Y, multiply(multiply(X_10, X_11), multiply(X_11, X_11)))),
    introduced(tautology,
               [refl,
                [$fot(multiply(Y,
                        multiply(multiply(X_10, X_11),
                          multiply(X_11, X_11))))]])).

cnf(refute_0_360, plain,
    (multiply(Y, multiply(multiply(X_10, X_11), multiply(X_11, X_11))) !=
     multiply(Y, multiply(multiply(X_10, X_11), multiply(X_11, X_11))) |
     multiply(multiply(X_10, X_11), multiply(X_11, X_11)) != X_10 |
     multiply(Y, multiply(multiply(X_10, X_11), multiply(X_11, X_11))) =
     multiply(Y, X_10)),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(Y,
                          multiply(multiply(X_10, X_11),
                            multiply(X_11, X_11))),
                        multiply(Y,
                          multiply(multiply(X_10, X_11),
                            multiply(X_11, X_11))))), [1, 1],
                 $fot(X_10)]])).

cnf(refute_0_361, plain,
    (multiply(multiply(X_10, X_11), multiply(X_11, X_11)) != X_10 |
     multiply(Y, multiply(multiply(X_10, X_11), multiply(X_11, X_11))) =
     multiply(Y, X_10)),
    inference(resolve,
              [$cnf($equal(multiply(Y,
                        multiply(multiply(X_10, X_11),
                          multiply(X_11, X_11))),
                      multiply(Y,
                        multiply(multiply(X_10, X_11),
                          multiply(X_11, X_11)))))],
              [refute_0_359, refute_0_360])).

cnf(refute_0_362, plain,
    (multiply(Y, multiply(multiply(X_10, X_11), multiply(X_11, X_11))) =
     multiply(Y, X_10)),
    inference(resolve,
              [$cnf($equal(multiply(multiply(X_10, X_11),
                        multiply(X_11, X_11)), X_10))],
              [refute_0_238, refute_0_361])).

cnf(refute_0_363, plain,
    (multiply(multiply(Y, X_10),
       multiply(Y, multiply(multiply(X_10, X_11), multiply(X_11, X_11)))) =
     multiply(multiply(Y, X_10),
       multiply(Y, multiply(multiply(X_10, X_11), multiply(X_11, X_11))))),
    introduced(tautology,
               [refl,
                [$fot(multiply(multiply(Y, X_10),
                        multiply(Y,
                          multiply(multiply(X_10, X_11),
                            multiply(X_11, X_11)))))]])).

cnf(refute_0_364, plain,
    (multiply(Y, multiply(multiply(X_10, X_11), multiply(X_11, X_11))) !=
     multiply(Y, X_10) |
     multiply(multiply(Y, X_10),
       multiply(Y,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11)))) !=
     multiply(multiply(Y, X_10),
       multiply(Y, multiply(multiply(X_10, X_11), multiply(X_11, X_11)))) |
     multiply(multiply(Y, X_10),
       multiply(Y, multiply(multiply(X_10, X_11), multiply(X_11, X_11)))) =
     multiply(multiply(Y, X_10), multiply(Y, X_10))),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(multiply(Y, X_10),
                          multiply(Y,
                            multiply(multiply(X_10, X_11),
                              multiply(X_11, X_11)))),
                        multiply(multiply(Y, X_10),
                          multiply(Y,
                            multiply(multiply(X_10, X_11),
                              multiply(X_11, X_11)))))), [1, 1],
                 $fot(multiply(Y, X_10))]])).

cnf(refute_0_365, plain,
    (multiply(Y, multiply(multiply(X_10, X_11), multiply(X_11, X_11))) !=
     multiply(Y, X_10) |
     multiply(multiply(Y, X_10),
       multiply(Y, multiply(multiply(X_10, X_11), multiply(X_11, X_11)))) =
     multiply(multiply(Y, X_10), multiply(Y, X_10))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(Y, X_10),
                        multiply(Y,
                          multiply(multiply(X_10, X_11),
                            multiply(X_11, X_11)))),
                      multiply(multiply(Y, X_10),
                        multiply(Y,
                          multiply(multiply(X_10, X_11),
                            multiply(X_11, X_11))))))],
              [refute_0_363, refute_0_364])).

cnf(refute_0_366, plain,
    (multiply(multiply(Y, X_10),
       multiply(Y, multiply(multiply(X_10, X_11), multiply(X_11, X_11)))) =
     multiply(multiply(Y, X_10), multiply(Y, X_10))),
    inference(resolve,
              [$cnf($equal(multiply(Y,
                        multiply(multiply(X_10, X_11),
                          multiply(X_11, X_11))), multiply(Y, X_10)))],
              [refute_0_362, refute_0_365])).

cnf(refute_0_367, plain,
    (multiply(multiply(Y,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(Y, multiply(multiply(X_10, X_11), multiply(X_11, X_11)))) =
     multiply(multiply(Y,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(Y, multiply(multiply(X_10, X_11), multiply(X_11, X_11))))),
    introduced(tautology,
               [refl,
                [$fot(multiply(multiply(Y,
                          multiply(multiply(X_10, X_11),
                            multiply(X_11, X_11))),
                        multiply(Y,
                          multiply(multiply(X_10, X_11),
                            multiply(X_11, X_11)))))]])).

cnf(refute_0_368, plain,
    (multiply(Y, multiply(multiply(X_10, X_11), multiply(X_11, X_11))) !=
     multiply(Y, X_10) |
     multiply(multiply(Y,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(Y,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11)))) !=
     multiply(multiply(Y,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(Y, multiply(multiply(X_10, X_11), multiply(X_11, X_11)))) |
     multiply(multiply(Y,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(Y, multiply(multiply(X_10, X_11), multiply(X_11, X_11)))) =
     multiply(multiply(Y, X_10),
       multiply(Y, multiply(multiply(X_10, X_11), multiply(X_11, X_11))))),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(multiply(Y,
                            multiply(multiply(X_10, X_11),
                              multiply(X_11, X_11))),
                          multiply(Y,
                            multiply(multiply(X_10, X_11),
                              multiply(X_11, X_11)))),
                        multiply(multiply(Y,
                            multiply(multiply(X_10, X_11),
                              multiply(X_11, X_11))),
                          multiply(Y,
                            multiply(multiply(X_10, X_11),
                              multiply(X_11, X_11)))))), [1, 0],
                 $fot(multiply(Y, X_10))]])).

cnf(refute_0_369, plain,
    (multiply(Y, multiply(multiply(X_10, X_11), multiply(X_11, X_11))) !=
     multiply(Y, X_10) |
     multiply(multiply(Y,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(Y, multiply(multiply(X_10, X_11), multiply(X_11, X_11)))) =
     multiply(multiply(Y, X_10),
       multiply(Y, multiply(multiply(X_10, X_11), multiply(X_11, X_11))))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(Y,
                          multiply(multiply(X_10, X_11),
                            multiply(X_11, X_11))),
                        multiply(Y,
                          multiply(multiply(X_10, X_11),
                            multiply(X_11, X_11)))),
                      multiply(multiply(Y,
                          multiply(multiply(X_10, X_11),
                            multiply(X_11, X_11))),
                        multiply(Y,
                          multiply(multiply(X_10, X_11),
                            multiply(X_11, X_11))))))],
              [refute_0_367, refute_0_368])).

cnf(refute_0_370, plain,
    (multiply(multiply(Y,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(Y, multiply(multiply(X_10, X_11), multiply(X_11, X_11)))) =
     multiply(multiply(Y, X_10),
       multiply(Y, multiply(multiply(X_10, X_11), multiply(X_11, X_11))))),
    inference(resolve,
              [$cnf($equal(multiply(Y,
                        multiply(multiply(X_10, X_11),
                          multiply(X_11, X_11))), multiply(Y, X_10)))],
              [refute_0_362, refute_0_369])).

cnf(refute_0_371, plain,
    (multiply(multiply(Y, X_10),
       multiply(Y,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11)))) !=
     multiply(multiply(Y, X_10), multiply(Y, X_10)) |
     multiply(multiply(Y,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(Y,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11)))) !=
     multiply(multiply(Y, X_10),
       multiply(Y, multiply(multiply(X_10, X_11), multiply(X_11, X_11)))) |
     multiply(multiply(Y,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(Y, multiply(multiply(X_10, X_11), multiply(X_11, X_11)))) =
     multiply(multiply(Y, X_10), multiply(Y, X_10))),
    inference(subst, [],
              [refute_0_92 :
               [bind(X0,
                     $fot(multiply(multiply(Y,
                              multiply(multiply(X_10, X_11),
                                multiply(X_11, X_11))),
                            multiply(Y,
                              multiply(multiply(X_10, X_11),
                                multiply(X_11, X_11)))))),
                bind(Y0,
                     $fot(multiply(multiply(Y, X_10),
                            multiply(Y,
                              multiply(multiply(X_10, X_11),
                                multiply(X_11, X_11)))))),
                bind(Z0,
                     $fot(multiply(multiply(Y, X_10),
                            multiply(Y, X_10))))]])).

cnf(refute_0_372, plain,
    (multiply(multiply(Y, X_10),
       multiply(Y,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11)))) !=
     multiply(multiply(Y, X_10), multiply(Y, X_10)) |
     multiply(multiply(Y,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(Y, multiply(multiply(X_10, X_11), multiply(X_11, X_11)))) =
     multiply(multiply(Y, X_10), multiply(Y, X_10))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(Y,
                          multiply(multiply(X_10, X_11),
                            multiply(X_11, X_11))),
                        multiply(Y,
                          multiply(multiply(X_10, X_11),
                            multiply(X_11, X_11)))),
                      multiply(multiply(Y, X_10),
                        multiply(Y,
                          multiply(multiply(X_10, X_11),
                            multiply(X_11, X_11))))))],
              [refute_0_370, refute_0_371])).

cnf(refute_0_373, plain,
    (multiply(multiply(Y,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(Y, multiply(multiply(X_10, X_11), multiply(X_11, X_11)))) =
     multiply(multiply(Y, X_10), multiply(Y, X_10))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(Y, X_10),
                        multiply(Y,
                          multiply(multiply(X_10, X_11),
                            multiply(X_11, X_11)))),
                      multiply(multiply(Y, X_10), multiply(Y, X_10))))],
              [refute_0_366, refute_0_372])).

cnf(refute_0_374, plain,
    (multiply(identity,
       multiply(multiply(Y,
           multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
         multiply(Y,
           multiply(multiply(X_10, X_11), multiply(X_11, X_11))))) =
     multiply(identity,
       multiply(multiply(Y,
           multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
         multiply(Y,
           multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
    introduced(tautology,
               [refl,
                [$fot(multiply(identity,
                        multiply(multiply(Y,
                            multiply(multiply(X_10, X_11),
                              multiply(X_11, X_11))),
                          multiply(Y,
                            multiply(multiply(X_10, X_11),
                              multiply(X_11, X_11))))))]])).

cnf(refute_0_375, plain,
    (multiply(multiply(Y,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(Y,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11)))) !=
     multiply(multiply(Y, X_10), multiply(Y, X_10)) |
     multiply(identity,
       multiply(multiply(Y,
           multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
         multiply(Y,
           multiply(multiply(X_10, X_11), multiply(X_11, X_11))))) !=
     multiply(identity,
       multiply(multiply(Y,
           multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
         multiply(Y,
           multiply(multiply(X_10, X_11), multiply(X_11, X_11))))) |
     multiply(identity,
       multiply(multiply(Y,
           multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
         multiply(Y,
           multiply(multiply(X_10, X_11), multiply(X_11, X_11))))) =
     multiply(identity, multiply(multiply(Y, X_10), multiply(Y, X_10)))),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(identity,
                          multiply(multiply(Y,
                              multiply(multiply(X_10, X_11),
                                multiply(X_11, X_11))),
                            multiply(Y,
                              multiply(multiply(X_10, X_11),
                                multiply(X_11, X_11))))),
                        multiply(identity,
                          multiply(multiply(Y,
                              multiply(multiply(X_10, X_11),
                                multiply(X_11, X_11))),
                            multiply(Y,
                              multiply(multiply(X_10, X_11),
                                multiply(X_11, X_11))))))), [1, 1],
                 $fot(multiply(multiply(Y, X_10), multiply(Y, X_10)))]])).

cnf(refute_0_376, plain,
    (multiply(multiply(Y,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
       multiply(Y,
         multiply(multiply(X_10, X_11), multiply(X_11, X_11)))) !=
     multiply(multiply(Y, X_10), multiply(Y, X_10)) |
     multiply(identity,
       multiply(multiply(Y,
           multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
         multiply(Y,
           multiply(multiply(X_10, X_11), multiply(X_11, X_11))))) =
     multiply(identity, multiply(multiply(Y, X_10), multiply(Y, X_10)))),
    inference(resolve,
              [$cnf($equal(multiply(identity,
                        multiply(multiply(Y,
                            multiply(multiply(X_10, X_11),
                              multiply(X_11, X_11))),
                          multiply(Y,
                            multiply(multiply(X_10, X_11),
                              multiply(X_11, X_11))))),
                      multiply(identity,
                        multiply(multiply(Y,
                            multiply(multiply(X_10, X_11),
                              multiply(X_11, X_11))),
                          multiply(Y,
                            multiply(multiply(X_10, X_11),
                              multiply(X_11, X_11)))))))],
              [refute_0_374, refute_0_375])).

cnf(refute_0_377, plain,
    (multiply(identity,
       multiply(multiply(Y,
           multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
         multiply(Y,
           multiply(multiply(X_10, X_11), multiply(X_11, X_11))))) =
     multiply(identity, multiply(multiply(Y, X_10), multiply(Y, X_10)))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(Y,
                          multiply(multiply(X_10, X_11),
                            multiply(X_11, X_11))),
                        multiply(Y,
                          multiply(multiply(X_10, X_11),
                            multiply(X_11, X_11)))),
                      multiply(multiply(Y, X_10), multiply(Y, X_10))))],
              [refute_0_373, refute_0_376])).

cnf(refute_0_378, plain,
    (multiply(X_10,
       multiply(identity,
         multiply(multiply(Y,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
           multiply(Y,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))) =
     multiply(X_10,
       multiply(identity,
         multiply(multiply(Y,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
           multiply(Y,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11))))))),
    introduced(tautology,
               [refl,
                [$fot(multiply(X_10,
                        multiply(identity,
                          multiply(multiply(Y,
                              multiply(multiply(X_10, X_11),
                                multiply(X_11, X_11))),
                            multiply(Y,
                              multiply(multiply(X_10, X_11),
                                multiply(X_11, X_11)))))))]])).

cnf(refute_0_379, plain,
    (multiply(X_10,
       multiply(identity,
         multiply(multiply(Y,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
           multiply(Y,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))) !=
     multiply(X_10,
       multiply(identity,
         multiply(multiply(Y,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
           multiply(Y,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))) |
     multiply(identity,
       multiply(multiply(Y,
           multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
         multiply(Y,
           multiply(multiply(X_10, X_11), multiply(X_11, X_11))))) !=
     multiply(identity, multiply(multiply(Y, X_10), multiply(Y, X_10))) |
     multiply(X_10,
       multiply(identity,
         multiply(multiply(Y,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
           multiply(Y,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))) =
     multiply(X_10,
       multiply(identity,
         multiply(multiply(Y, X_10), multiply(Y, X_10))))),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(X_10,
                          multiply(identity,
                            multiply(multiply(Y,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))),
                              multiply(Y,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11)))))),
                        multiply(X_10,
                          multiply(identity,
                            multiply(multiply(Y,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))),
                              multiply(Y,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11)))))))), [1, 1],
                 $fot(multiply(identity,
                        multiply(multiply(Y, X_10),
                          multiply(Y, X_10))))]])).

cnf(refute_0_380, plain,
    (multiply(identity,
       multiply(multiply(Y,
           multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
         multiply(Y,
           multiply(multiply(X_10, X_11), multiply(X_11, X_11))))) !=
     multiply(identity, multiply(multiply(Y, X_10), multiply(Y, X_10))) |
     multiply(X_10,
       multiply(identity,
         multiply(multiply(Y,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
           multiply(Y,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))) =
     multiply(X_10,
       multiply(identity,
         multiply(multiply(Y, X_10), multiply(Y, X_10))))),
    inference(resolve,
              [$cnf($equal(multiply(X_10,
                        multiply(identity,
                          multiply(multiply(Y,
                              multiply(multiply(X_10, X_11),
                                multiply(X_11, X_11))),
                            multiply(Y,
                              multiply(multiply(X_10, X_11),
                                multiply(X_11, X_11)))))),
                      multiply(X_10,
                        multiply(identity,
                          multiply(multiply(Y,
                              multiply(multiply(X_10, X_11),
                                multiply(X_11, X_11))),
                            multiply(Y,
                              multiply(multiply(X_10, X_11),
                                multiply(X_11, X_11))))))))],
              [refute_0_378, refute_0_379])).

cnf(refute_0_381, plain,
    (multiply(X_10,
       multiply(identity,
         multiply(multiply(Y,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
           multiply(Y,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))) =
     multiply(X_10,
       multiply(identity,
         multiply(multiply(Y, X_10), multiply(Y, X_10))))),
    inference(resolve,
              [$cnf($equal(multiply(identity,
                        multiply(multiply(Y,
                            multiply(multiply(X_10, X_11),
                              multiply(X_11, X_11))),
                          multiply(Y,
                            multiply(multiply(X_10, X_11),
                              multiply(X_11, X_11))))),
                      multiply(identity,
                        multiply(multiply(Y, X_10), multiply(Y, X_10)))))],
              [refute_0_377, refute_0_380])).

cnf(refute_0_382, plain,
    (multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(Y, X_10), multiply(Y, X_10)))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))))))) =
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(Y, X_10), multiply(Y, X_10)))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))))),
    introduced(tautology,
               [refl,
                [$fot(multiply(multiply(X_10,
                          multiply(identity,
                            multiply(multiply(Y, X_10),
                              multiply(Y, X_10)))),
                        multiply(X_10,
                          multiply(identity,
                            multiply(multiply(Y,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))),
                              multiply(Y,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))))))))]])).

cnf(refute_0_383, plain,
    (multiply(X_10,
       multiply(identity,
         multiply(multiply(Y,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
           multiply(Y,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))) !=
     multiply(X_10,
       multiply(identity,
         multiply(multiply(Y, X_10), multiply(Y, X_10)))) |
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(Y, X_10), multiply(Y, X_10)))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))))))) !=
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(Y, X_10), multiply(Y, X_10)))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))))))) |
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(Y, X_10), multiply(Y, X_10)))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))))))) =
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(Y, X_10), multiply(Y, X_10)))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(Y, X_10), multiply(Y, X_10)))))),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(multiply(X_10,
                            multiply(identity,
                              multiply(multiply(Y, X_10),
                                multiply(Y, X_10)))),
                          multiply(X_10,
                            multiply(identity,
                              multiply(multiply(Y,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11))),
                                multiply(Y,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11))))))),
                        multiply(multiply(X_10,
                            multiply(identity,
                              multiply(multiply(Y, X_10),
                                multiply(Y, X_10)))),
                          multiply(X_10,
                            multiply(identity,
                              multiply(multiply(Y,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11))),
                                multiply(Y,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11))))))))), [1, 1],
                 $fot(multiply(X_10,
                        multiply(identity,
                          multiply(multiply(Y, X_10),
                            multiply(Y, X_10)))))]])).

cnf(refute_0_384, plain,
    (multiply(X_10,
       multiply(identity,
         multiply(multiply(Y,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
           multiply(Y,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))) !=
     multiply(X_10,
       multiply(identity,
         multiply(multiply(Y, X_10), multiply(Y, X_10)))) |
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(Y, X_10), multiply(Y, X_10)))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))))))) =
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(Y, X_10), multiply(Y, X_10)))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(Y, X_10), multiply(Y, X_10)))))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(X_10,
                          multiply(identity,
                            multiply(multiply(Y, X_10),
                              multiply(Y, X_10)))),
                        multiply(X_10,
                          multiply(identity,
                            multiply(multiply(Y,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))),
                              multiply(Y,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))))))),
                      multiply(multiply(X_10,
                          multiply(identity,
                            multiply(multiply(Y, X_10),
                              multiply(Y, X_10)))),
                        multiply(X_10,
                          multiply(identity,
                            multiply(multiply(Y,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))),
                              multiply(Y,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11)))))))))],
              [refute_0_382, refute_0_383])).

cnf(refute_0_385, plain,
    (multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(Y, X_10), multiply(Y, X_10)))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))))))) =
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(Y, X_10), multiply(Y, X_10)))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(Y, X_10), multiply(Y, X_10)))))),
    inference(resolve,
              [$cnf($equal(multiply(X_10,
                        multiply(identity,
                          multiply(multiply(Y,
                              multiply(multiply(X_10, X_11),
                                multiply(X_11, X_11))),
                            multiply(Y,
                              multiply(multiply(X_10, X_11),
                                multiply(X_11, X_11)))))),
                      multiply(X_10,
                        multiply(identity,
                          multiply(multiply(Y, X_10),
                            multiply(Y, X_10))))))],
              [refute_0_381, refute_0_384])).

cnf(refute_0_386, plain,
    (multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))))))) =
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))))),
    introduced(tautology,
               [refl,
                [$fot(multiply(multiply(X_10,
                          multiply(identity,
                            multiply(multiply(Y,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))),
                              multiply(Y,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11)))))),
                        multiply(X_10,
                          multiply(identity,
                            multiply(multiply(Y,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))),
                              multiply(Y,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))))))))]])).

cnf(refute_0_387, plain,
    (multiply(X_10,
       multiply(identity,
         multiply(multiply(Y,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
           multiply(Y,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))) !=
     multiply(X_10,
       multiply(identity,
         multiply(multiply(Y, X_10), multiply(Y, X_10)))) |
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))))))) !=
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))))))) |
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))))))) =
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(Y, X_10), multiply(Y, X_10)))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))))),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(multiply(X_10,
                            multiply(identity,
                              multiply(multiply(Y,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11))),
                                multiply(Y,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11)))))),
                          multiply(X_10,
                            multiply(identity,
                              multiply(multiply(Y,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11))),
                                multiply(Y,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11))))))),
                        multiply(multiply(X_10,
                            multiply(identity,
                              multiply(multiply(Y,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11))),
                                multiply(Y,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11)))))),
                          multiply(X_10,
                            multiply(identity,
                              multiply(multiply(Y,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11))),
                                multiply(Y,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11))))))))), [1, 0],
                 $fot(multiply(X_10,
                        multiply(identity,
                          multiply(multiply(Y, X_10),
                            multiply(Y, X_10)))))]])).

cnf(refute_0_388, plain,
    (multiply(X_10,
       multiply(identity,
         multiply(multiply(Y,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
           multiply(Y,
             multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))) !=
     multiply(X_10,
       multiply(identity,
         multiply(multiply(Y, X_10), multiply(Y, X_10)))) |
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))))))) =
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(Y, X_10), multiply(Y, X_10)))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(X_10,
                          multiply(identity,
                            multiply(multiply(Y,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))),
                              multiply(Y,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11)))))),
                        multiply(X_10,
                          multiply(identity,
                            multiply(multiply(Y,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))),
                              multiply(Y,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))))))),
                      multiply(multiply(X_10,
                          multiply(identity,
                            multiply(multiply(Y,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))),
                              multiply(Y,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11)))))),
                        multiply(X_10,
                          multiply(identity,
                            multiply(multiply(Y,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))),
                              multiply(Y,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11)))))))))],
              [refute_0_386, refute_0_387])).

cnf(refute_0_389, plain,
    (multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))))))) =
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(Y, X_10), multiply(Y, X_10)))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))))),
    inference(resolve,
              [$cnf($equal(multiply(X_10,
                        multiply(identity,
                          multiply(multiply(Y,
                              multiply(multiply(X_10, X_11),
                                multiply(X_11, X_11))),
                            multiply(Y,
                              multiply(multiply(X_10, X_11),
                                multiply(X_11, X_11)))))),
                      multiply(X_10,
                        multiply(identity,
                          multiply(multiply(Y, X_10),
                            multiply(Y, X_10))))))],
              [refute_0_381, refute_0_388])).

cnf(refute_0_390, plain,
    (multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(Y, X_10), multiply(Y, X_10)))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))))))) !=
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(Y, X_10), multiply(Y, X_10)))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(Y, X_10), multiply(Y, X_10))))) |
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))))))) !=
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(Y, X_10), multiply(Y, X_10)))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))))))) |
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))))))) =
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(Y, X_10), multiply(Y, X_10)))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(Y, X_10), multiply(Y, X_10)))))),
    inference(subst, [],
              [refute_0_92 :
               [bind(X0,
                     $fot(multiply(multiply(X_10,
                              multiply(identity,
                                multiply(multiply(Y,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11))),
                                  multiply(Y,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11)))))),
                            multiply(X_10,
                              multiply(identity,
                                multiply(multiply(Y,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11))),
                                  multiply(Y,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11))))))))),
                bind(Y0,
                     $fot(multiply(multiply(X_10,
                              multiply(identity,
                                multiply(multiply(Y, X_10),
                                  multiply(Y, X_10)))),
                            multiply(X_10,
                              multiply(identity,
                                multiply(multiply(Y,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11))),
                                  multiply(Y,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11))))))))),
                bind(Z0,
                     $fot(multiply(multiply(X_10,
                              multiply(identity,
                                multiply(multiply(Y, X_10),
                                  multiply(Y, X_10)))),
                            multiply(X_10,
                              multiply(identity,
                                multiply(multiply(Y, X_10),
                                  multiply(Y, X_10)))))))]])).

cnf(refute_0_391, plain,
    (multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(Y, X_10), multiply(Y, X_10)))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))))))) !=
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(Y, X_10), multiply(Y, X_10)))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(Y, X_10), multiply(Y, X_10))))) |
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))))))) =
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(Y, X_10), multiply(Y, X_10)))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(Y, X_10), multiply(Y, X_10)))))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(X_10,
                          multiply(identity,
                            multiply(multiply(Y,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))),
                              multiply(Y,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11)))))),
                        multiply(X_10,
                          multiply(identity,
                            multiply(multiply(Y,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))),
                              multiply(Y,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))))))),
                      multiply(multiply(X_10,
                          multiply(identity,
                            multiply(multiply(Y, X_10),
                              multiply(Y, X_10)))),
                        multiply(X_10,
                          multiply(identity,
                            multiply(multiply(Y,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))),
                              multiply(Y,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11)))))))))],
              [refute_0_389, refute_0_390])).

cnf(refute_0_392, plain,
    (multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))))))) =
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(Y, X_10), multiply(Y, X_10)))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(Y, X_10), multiply(Y, X_10)))))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(X_10,
                          multiply(identity,
                            multiply(multiply(Y, X_10),
                              multiply(Y, X_10)))),
                        multiply(X_10,
                          multiply(identity,
                            multiply(multiply(Y,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))),
                              multiply(Y,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))))))),
                      multiply(multiply(X_10,
                          multiply(identity,
                            multiply(multiply(Y, X_10),
                              multiply(Y, X_10)))),
                        multiply(X_10,
                          multiply(identity,
                            multiply(multiply(Y, X_10),
                              multiply(Y, X_10)))))))],
              [refute_0_385, refute_0_391])).

cnf(refute_0_393, plain,
    (multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(Y,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
               multiply(Y,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(Y,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
               multiply(Y,
                 multiply(multiply(X_10, X_11),
                   multiply(X_11, X_11)))))))) =
     multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(Y,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
               multiply(Y,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(Y,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
               multiply(Y,
                 multiply(multiply(X_10, X_11),
                   multiply(X_11, X_11))))))))),
    introduced(tautology,
               [refl,
                [$fot(multiply(identity,
                        multiply(multiply(X_10,
                            multiply(identity,
                              multiply(multiply(Y,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11))),
                                multiply(Y,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11)))))),
                          multiply(X_10,
                            multiply(identity,
                              multiply(multiply(Y,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11))),
                                multiply(Y,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11)))))))))]])).

cnf(refute_0_394, plain,
    (multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))))))) !=
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(Y, X_10), multiply(Y, X_10)))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(Y, X_10), multiply(Y, X_10))))) |
     multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(Y,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
               multiply(Y,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(Y,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
               multiply(Y,
                 multiply(multiply(X_10, X_11),
                   multiply(X_11, X_11)))))))) !=
     multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(Y,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
               multiply(Y,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(Y,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
               multiply(Y,
                 multiply(multiply(X_10, X_11),
                   multiply(X_11, X_11)))))))) |
     multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(Y,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
               multiply(Y,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(Y,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
               multiply(Y,
                 multiply(multiply(X_10, X_11),
                   multiply(X_11, X_11)))))))) =
     multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(Y, X_10), multiply(Y, X_10)))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(Y, X_10), multiply(Y, X_10))))))),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(identity,
                          multiply(multiply(X_10,
                              multiply(identity,
                                multiply(multiply(Y,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11))),
                                  multiply(Y,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11)))))),
                            multiply(X_10,
                              multiply(identity,
                                multiply(multiply(Y,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11))),
                                  multiply(Y,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11)))))))),
                        multiply(identity,
                          multiply(multiply(X_10,
                              multiply(identity,
                                multiply(multiply(Y,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11))),
                                  multiply(Y,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11)))))),
                            multiply(X_10,
                              multiply(identity,
                                multiply(multiply(Y,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11))),
                                  multiply(Y,
                                    multiply(multiply(X_10, X_11),
                                      multiply(X_11, X_11)))))))))),
                 [1, 1],
                 $fot(multiply(multiply(X_10,
                          multiply(identity,
                            multiply(multiply(Y, X_10),
                              multiply(Y, X_10)))),
                        multiply(X_10,
                          multiply(identity,
                            multiply(multiply(Y, X_10),
                              multiply(Y, X_10))))))]])).

cnf(refute_0_395, plain,
    (multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
             multiply(Y,
               multiply(multiply(X_10, X_11), multiply(X_11, X_11))))))) !=
     multiply(multiply(X_10,
         multiply(identity,
           multiply(multiply(Y, X_10), multiply(Y, X_10)))),
       multiply(X_10,
         multiply(identity,
           multiply(multiply(Y, X_10), multiply(Y, X_10))))) |
     multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(Y,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
               multiply(Y,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(Y,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
               multiply(Y,
                 multiply(multiply(X_10, X_11),
                   multiply(X_11, X_11)))))))) =
     multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(Y, X_10), multiply(Y, X_10)))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(Y, X_10), multiply(Y, X_10))))))),
    inference(resolve,
              [$cnf($equal(multiply(identity,
                        multiply(multiply(X_10,
                            multiply(identity,
                              multiply(multiply(Y,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11))),
                                multiply(Y,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11)))))),
                          multiply(X_10,
                            multiply(identity,
                              multiply(multiply(Y,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11))),
                                multiply(Y,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11)))))))),
                      multiply(identity,
                        multiply(multiply(X_10,
                            multiply(identity,
                              multiply(multiply(Y,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11))),
                                multiply(Y,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11)))))),
                          multiply(X_10,
                            multiply(identity,
                              multiply(multiply(Y,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11))),
                                multiply(Y,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11))))))))))],
              [refute_0_393, refute_0_394])).

cnf(refute_0_396, plain,
    (multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(Y,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
               multiply(Y,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(Y,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
               multiply(Y,
                 multiply(multiply(X_10, X_11),
                   multiply(X_11, X_11)))))))) =
     multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(Y, X_10), multiply(Y, X_10)))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(Y, X_10), multiply(Y, X_10))))))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(X_10,
                          multiply(identity,
                            multiply(multiply(Y,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))),
                              multiply(Y,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11)))))),
                        multiply(X_10,
                          multiply(identity,
                            multiply(multiply(Y,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))),
                              multiply(Y,
                                multiply(multiply(X_10, X_11),
                                  multiply(X_11, X_11))))))),
                      multiply(multiply(X_10,
                          multiply(identity,
                            multiply(multiply(Y, X_10),
                              multiply(Y, X_10)))),
                        multiply(X_10,
                          multiply(identity,
                            multiply(multiply(Y, X_10),
                              multiply(Y, X_10)))))))],
              [refute_0_392, refute_0_395])).

cnf(refute_0_397, plain,
    (multiply(Y,
       multiply(Y,
         multiply(Y,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(Y,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(Y,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(Y,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(Y,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))))))))))) =
     multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(Y,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
               multiply(Y,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(Y,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
               multiply(Y,
                 multiply(multiply(X_10, X_11),
                   multiply(X_11, X_11))))))))),
    inference(subst, [],
              [refute_0_219 :
               [bind(X_17, $fot(Y)),
                bind(X_18,
                     $fot(multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(Y,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))),
                                    multiply(Y,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(Y,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))),
                                    multiply(Y,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))))))))))]])).

cnf(refute_0_398, plain,
    (multiply(Y,
       multiply(Y,
         multiply(Y,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(Y,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(Y,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(Y,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(Y,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))))))))))) !=
     multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(Y,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
               multiply(Y,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(Y,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
               multiply(Y,
                 multiply(multiply(X_10, X_11),
                   multiply(X_11, X_11)))))))) |
     multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(Y,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
               multiply(Y,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(Y,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
               multiply(Y,
                 multiply(multiply(X_10, X_11),
                   multiply(X_11, X_11)))))))) !=
     multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(Y, X_10), multiply(Y, X_10)))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(Y, X_10), multiply(Y, X_10)))))) |
     multiply(Y,
       multiply(Y,
         multiply(Y,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(Y,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(Y,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(Y,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(Y,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))))))))))) =
     multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(Y, X_10), multiply(Y, X_10)))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(Y, X_10), multiply(Y, X_10))))))),
    inference(subst, [],
              [refute_0_92 :
               [bind(X0,
                     $fot(multiply(Y,
                            multiply(Y,
                              multiply(Y,
                                multiply(identity,
                                  multiply(multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(Y,
                                            multiply(multiply(X_10, X_11),
                                              multiply(X_11, X_11))),
                                          multiply(Y,
                                            multiply(multiply(X_10, X_11),
                                              multiply(X_11, X_11)))))),
                                    multiply(X_10,
                                      multiply(identity,
                                        multiply(multiply(Y,
                                            multiply(multiply(X_10, X_11),
                                              multiply(X_11, X_11))),
                                          multiply(Y,
                                            multiply(multiply(X_10, X_11),
                                              multiply(X_11,
                                                X_11))))))))))))),
                bind(Y0,
                     $fot(multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(Y,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))),
                                    multiply(Y,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11)))))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(Y,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11))),
                                    multiply(Y,
                                      multiply(multiply(X_10, X_11),
                                        multiply(X_11, X_11)))))))))),
                bind(Z0,
                     $fot(multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(Y, X_10),
                                    multiply(Y, X_10)))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(Y, X_10),
                                    multiply(Y, X_10))))))))]])).

cnf(refute_0_399, plain,
    (multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(Y,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
               multiply(Y,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11)))))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(Y,
                 multiply(multiply(X_10, X_11), multiply(X_11, X_11))),
               multiply(Y,
                 multiply(multiply(X_10, X_11),
                   multiply(X_11, X_11)))))))) !=
     multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(Y, X_10), multiply(Y, X_10)))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(Y, X_10), multiply(Y, X_10)))))) |
     multiply(Y,
       multiply(Y,
         multiply(Y,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(Y,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(Y,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(Y,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(Y,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))))))))))) =
     multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(Y, X_10), multiply(Y, X_10)))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(Y, X_10), multiply(Y, X_10))))))),
    inference(resolve,
              [$cnf($equal(multiply(Y,
                        multiply(Y,
                          multiply(Y,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(Y,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))),
                                      multiply(Y,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11)))))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(Y,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))),
                                      multiply(Y,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))))))))))),
                      multiply(identity,
                        multiply(multiply(X_10,
                            multiply(identity,
                              multiply(multiply(Y,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11))),
                                multiply(Y,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11)))))),
                          multiply(X_10,
                            multiply(identity,
                              multiply(multiply(Y,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11))),
                                multiply(Y,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11))))))))))],
              [refute_0_397, refute_0_398])).

cnf(refute_0_400, plain,
    (multiply(Y,
       multiply(Y,
         multiply(Y,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(Y,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(Y,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(Y,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(Y,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))))))))))) =
     multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(Y, X_10), multiply(Y, X_10)))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(Y, X_10), multiply(Y, X_10))))))),
    inference(resolve,
              [$cnf($equal(multiply(identity,
                        multiply(multiply(X_10,
                            multiply(identity,
                              multiply(multiply(Y,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11))),
                                multiply(Y,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11)))))),
                          multiply(X_10,
                            multiply(identity,
                              multiply(multiply(Y,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11))),
                                multiply(Y,
                                  multiply(multiply(X_10, X_11),
                                    multiply(X_11, X_11)))))))),
                      multiply(identity,
                        multiply(multiply(X_10,
                            multiply(identity,
                              multiply(multiply(Y, X_10),
                                multiply(Y, X_10)))),
                          multiply(X_10,
                            multiply(identity,
                              multiply(multiply(Y, X_10),
                                multiply(Y, X_10))))))))],
              [refute_0_396, refute_0_399])).

cnf(refute_0_401, plain,
    (multiply(Y,
       multiply(Y,
         multiply(Y,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(Y,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(Y,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(Y,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(Y,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))))))))))) != Y |
     multiply(Y,
       multiply(Y,
         multiply(Y,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(Y,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(Y,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(Y,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(Y,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))))))))))) !=
     multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(Y, X_10), multiply(Y, X_10)))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(Y, X_10), multiply(Y, X_10)))))) |
     multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(Y, X_10), multiply(Y, X_10)))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(Y, X_10), multiply(Y, X_10)))))) = Y),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(Y,
                          multiply(Y,
                            multiply(Y,
                              multiply(identity,
                                multiply(multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(Y,
                                          multiply(multiply(X_10, X_11),
                                            multiply(X_11, X_11))),
                                        multiply(Y,
                                          multiply(multiply(X_10, X_11),
                                            multiply(X_11, X_11)))))),
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(Y,
                                          multiply(multiply(X_10, X_11),
                                            multiply(X_11, X_11))),
                                        multiply(Y,
                                          multiply(multiply(X_10, X_11),
                                            multiply(X_11, X_11))))))))))),
                        Y)), [0],
                 $fot(multiply(identity,
                        multiply(multiply(X_10,
                            multiply(identity,
                              multiply(multiply(Y, X_10),
                                multiply(Y, X_10)))),
                          multiply(X_10,
                            multiply(identity,
                              multiply(multiply(Y, X_10),
                                multiply(Y, X_10)))))))]])).

cnf(refute_0_402, plain,
    (multiply(Y,
       multiply(Y,
         multiply(Y,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(Y,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(Y,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11)))))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(Y,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))),
                     multiply(Y,
                       multiply(multiply(X_10, X_11),
                         multiply(X_11, X_11))))))))))) != Y |
     multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(Y, X_10), multiply(Y, X_10)))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(Y, X_10), multiply(Y, X_10)))))) = Y),
    inference(resolve,
              [$cnf($equal(multiply(Y,
                        multiply(Y,
                          multiply(Y,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(Y,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))),
                                      multiply(Y,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11)))))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(Y,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))),
                                      multiply(Y,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))))))))))),
                      multiply(identity,
                        multiply(multiply(X_10,
                            multiply(identity,
                              multiply(multiply(Y, X_10),
                                multiply(Y, X_10)))),
                          multiply(X_10,
                            multiply(identity,
                              multiply(multiply(Y, X_10),
                                multiply(Y, X_10))))))))],
              [refute_0_400, refute_0_401])).

cnf(refute_0_403, plain,
    (multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(Y, X_10), multiply(Y, X_10)))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(Y, X_10), multiply(Y, X_10)))))) = Y),
    inference(resolve,
              [$cnf($equal(multiply(Y,
                        multiply(Y,
                          multiply(Y,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(Y,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))),
                                      multiply(Y,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11)))))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(Y,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))),
                                      multiply(Y,
                                        multiply(multiply(X_10, X_11),
                                          multiply(X_11, X_11))))))))))),
                      Y))], [refute_0_358, refute_0_402])).

cnf(refute_0_404, plain,
    (multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(X_4, X_10), multiply(X_4, X_10)))))) = X_4),
    inference(subst, [], [refute_0_403 : [bind(Y, $fot(X_4))]])).

cnf(refute_0_405, plain,
    (multiply(X_4,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))) =
     multiply(X_4,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4, X_10), multiply(X_4, X_10)))))))),
    introduced(tautology,
               [refl,
                [$fot(multiply(X_4,
                        multiply(identity,
                          multiply(multiply(X_10,
                              multiply(identity,
                                multiply(multiply(X_4, X_10),
                                  multiply(X_4, X_10)))),
                            multiply(X_10,
                              multiply(identity,
                                multiply(multiply(X_4, X_10),
                                  multiply(X_4, X_10))))))))]])).

cnf(refute_0_406, plain,
    (multiply(X_4,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))) !=
     multiply(X_4,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))) |
     multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(X_4, X_10), multiply(X_4, X_10)))))) !=
     X_4 |
     multiply(X_4,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))) =
     multiply(X_4, X_4)),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4, X_10),
                                    multiply(X_4, X_10)))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4, X_10),
                                    multiply(X_4, X_10))))))),
                        multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4, X_10),
                                    multiply(X_4, X_10)))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4, X_10),
                                    multiply(X_4, X_10))))))))), [1, 1],
                 $fot(X_4)]])).

cnf(refute_0_407, plain,
    (multiply(identity,
       multiply(multiply(X_10,
           multiply(identity,
             multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
         multiply(X_10,
           multiply(identity,
             multiply(multiply(X_4, X_10), multiply(X_4, X_10)))))) !=
     X_4 |
     multiply(X_4,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))) =
     multiply(X_4, X_4)),
    inference(resolve,
              [$cnf($equal(multiply(X_4,
                        multiply(identity,
                          multiply(multiply(X_10,
                              multiply(identity,
                                multiply(multiply(X_4, X_10),
                                  multiply(X_4, X_10)))),
                            multiply(X_10,
                              multiply(identity,
                                multiply(multiply(X_4, X_10),
                                  multiply(X_4, X_10))))))),
                      multiply(X_4,
                        multiply(identity,
                          multiply(multiply(X_10,
                              multiply(identity,
                                multiply(multiply(X_4, X_10),
                                  multiply(X_4, X_10)))),
                            multiply(X_10,
                              multiply(identity,
                                multiply(multiply(X_4, X_10),
                                  multiply(X_4, X_10)))))))))],
              [refute_0_405, refute_0_406])).

cnf(refute_0_408, plain,
    (multiply(X_4,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))) =
     multiply(X_4, X_4)),
    inference(resolve,
              [$cnf($equal(multiply(identity,
                        multiply(multiply(X_10,
                            multiply(identity,
                              multiply(multiply(X_4, X_10),
                                multiply(X_4, X_10)))),
                          multiply(X_10,
                            multiply(identity,
                              multiply(multiply(X_4, X_10),
                                multiply(X_4, X_10)))))), X_4))],
              [refute_0_404, refute_0_407])).

cnf(refute_0_409, plain,
    (multiply(multiply(X_4, X_4),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))))))) =
     multiply(multiply(X_4, X_4),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))))),
    introduced(tautology,
               [refl,
                [$fot(multiply(multiply(X_4, X_4),
                        multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4, X_10),
                                    multiply(X_4, X_10)))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4, X_10),
                                    multiply(X_4, X_10)))))))))]])).

cnf(refute_0_410, plain,
    (multiply(X_4,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))) !=
     multiply(X_4, X_4) |
     multiply(multiply(X_4, X_4),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10),
                   multiply(X_4, X_10)))))))) !=
     multiply(multiply(X_4, X_4),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))))))) |
     multiply(multiply(X_4, X_4),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))))))) =
     multiply(multiply(X_4, X_4), multiply(X_4, X_4))),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(multiply(X_4, X_4),
                          multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4, X_10),
                                      multiply(X_4, X_10)))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4, X_10),
                                      multiply(X_4, X_10)))))))),
                        multiply(multiply(X_4, X_4),
                          multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4, X_10),
                                      multiply(X_4, X_10)))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4, X_10),
                                      multiply(X_4, X_10)))))))))), [1, 1],
                 $fot(multiply(X_4, X_4))]])).

cnf(refute_0_411, plain,
    (multiply(X_4,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))) !=
     multiply(X_4, X_4) |
     multiply(multiply(X_4, X_4),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))))))) =
     multiply(multiply(X_4, X_4), multiply(X_4, X_4))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(X_4, X_4),
                        multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4, X_10),
                                    multiply(X_4, X_10)))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4, X_10),
                                    multiply(X_4, X_10)))))))),
                      multiply(multiply(X_4, X_4),
                        multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4, X_10),
                                    multiply(X_4, X_10)))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4, X_10),
                                    multiply(X_4, X_10))))))))))],
              [refute_0_409, refute_0_410])).

cnf(refute_0_412, plain,
    (multiply(multiply(X_4, X_4),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))))))) =
     multiply(multiply(X_4, X_4), multiply(X_4, X_4))),
    inference(resolve,
              [$cnf($equal(multiply(X_4,
                        multiply(identity,
                          multiply(multiply(X_10,
                              multiply(identity,
                                multiply(multiply(X_4, X_10),
                                  multiply(X_4, X_10)))),
                            multiply(X_10,
                              multiply(identity,
                                multiply(multiply(X_4, X_10),
                                  multiply(X_4, X_10))))))),
                      multiply(X_4, X_4)))],
              [refute_0_408, refute_0_411])).

cnf(refute_0_413, plain,
    (multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))))))) =
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))))),
    introduced(tautology,
               [refl,
                [$fot(multiply(multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4, X_10),
                                    multiply(X_4, X_10)))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4, X_10),
                                    multiply(X_4, X_10))))))),
                        multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4, X_10),
                                    multiply(X_4, X_10)))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4, X_10),
                                    multiply(X_4, X_10)))))))))]])).

cnf(refute_0_414, plain,
    (multiply(X_4,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))) !=
     multiply(X_4, X_4) |
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10),
                   multiply(X_4, X_10)))))))) !=
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))))))) |
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))))))) =
     multiply(multiply(X_4, X_4),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))))),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4, X_10),
                                      multiply(X_4, X_10)))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4, X_10),
                                      multiply(X_4, X_10))))))),
                          multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4, X_10),
                                      multiply(X_4, X_10)))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4, X_10),
                                      multiply(X_4, X_10)))))))),
                        multiply(multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4, X_10),
                                      multiply(X_4, X_10)))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4, X_10),
                                      multiply(X_4, X_10))))))),
                          multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4, X_10),
                                      multiply(X_4, X_10)))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4, X_10),
                                      multiply(X_4, X_10)))))))))), [1, 0],
                 $fot(multiply(X_4, X_4))]])).

cnf(refute_0_415, plain,
    (multiply(X_4,
       multiply(identity,
         multiply(multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
           multiply(X_10,
             multiply(identity,
               multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))) !=
     multiply(X_4, X_4) |
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))))))) =
     multiply(multiply(X_4, X_4),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4, X_10),
                                    multiply(X_4, X_10)))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4, X_10),
                                    multiply(X_4, X_10))))))),
                        multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4, X_10),
                                    multiply(X_4, X_10)))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4, X_10),
                                    multiply(X_4, X_10)))))))),
                      multiply(multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4, X_10),
                                    multiply(X_4, X_10)))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4, X_10),
                                    multiply(X_4, X_10))))))),
                        multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4, X_10),
                                    multiply(X_4, X_10)))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4, X_10),
                                    multiply(X_4, X_10))))))))))],
              [refute_0_413, refute_0_414])).

cnf(refute_0_416, plain,
    (multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))))))) =
     multiply(multiply(X_4, X_4),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))))),
    inference(resolve,
              [$cnf($equal(multiply(X_4,
                        multiply(identity,
                          multiply(multiply(X_10,
                              multiply(identity,
                                multiply(multiply(X_4, X_10),
                                  multiply(X_4, X_10)))),
                            multiply(X_10,
                              multiply(identity,
                                multiply(multiply(X_4, X_10),
                                  multiply(X_4, X_10))))))),
                      multiply(X_4, X_4)))],
              [refute_0_408, refute_0_415])).

cnf(refute_0_417, plain,
    (multiply(multiply(X_4, X_4),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10),
                   multiply(X_4, X_10)))))))) !=
     multiply(multiply(X_4, X_4), multiply(X_4, X_4)) |
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10),
                   multiply(X_4, X_10)))))))) !=
     multiply(multiply(X_4, X_4),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))))))) |
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))))))) =
     multiply(multiply(X_4, X_4), multiply(X_4, X_4))),
    inference(subst, [],
              [refute_0_92 :
               [bind(X0,
                     $fot(multiply(multiply(X_4,
                              multiply(identity,
                                multiply(multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(X_4, X_10),
                                        multiply(X_4, X_10)))),
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(X_4, X_10),
                                        multiply(X_4, X_10))))))),
                            multiply(X_4,
                              multiply(identity,
                                multiply(multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(X_4, X_10),
                                        multiply(X_4, X_10)))),
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(X_4, X_10),
                                        multiply(X_4, X_10)))))))))),
                bind(Y0,
                     $fot(multiply(multiply(X_4, X_4),
                            multiply(X_4,
                              multiply(identity,
                                multiply(multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(X_4, X_10),
                                        multiply(X_4, X_10)))),
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(X_4, X_10),
                                        multiply(X_4, X_10)))))))))),
                bind(Z0,
                     $fot(multiply(multiply(X_4, X_4),
                            multiply(X_4, X_4))))]])).

cnf(refute_0_418, plain,
    (multiply(multiply(X_4, X_4),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10),
                   multiply(X_4, X_10)))))))) !=
     multiply(multiply(X_4, X_4), multiply(X_4, X_4)) |
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))))))) =
     multiply(multiply(X_4, X_4), multiply(X_4, X_4))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4, X_10),
                                    multiply(X_4, X_10)))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4, X_10),
                                    multiply(X_4, X_10))))))),
                        multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4, X_10),
                                    multiply(X_4, X_10)))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4, X_10),
                                    multiply(X_4, X_10)))))))),
                      multiply(multiply(X_4, X_4),
                        multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4, X_10),
                                    multiply(X_4, X_10)))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4, X_10),
                                    multiply(X_4, X_10))))))))))],
              [refute_0_416, refute_0_417])).

cnf(refute_0_419, plain,
    (multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))))))) =
     multiply(multiply(X_4, X_4), multiply(X_4, X_4))),
    inference(resolve,
              [$cnf($equal(multiply(multiply(X_4, X_4),
                        multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4, X_10),
                                    multiply(X_4, X_10)))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4, X_10),
                                    multiply(X_4, X_10)))))))),
                      multiply(multiply(X_4, X_4), multiply(X_4, X_4))))],
              [refute_0_412, refute_0_418])).

cnf(refute_0_420, plain,
    (multiply(multiply(X_4, X_4), multiply(X_4, X_4)) != X_4 |
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10),
                   multiply(X_4, X_10)))))))) !=
     multiply(multiply(X_4, X_4), multiply(X_4, X_4)) |
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))))))) =
     X_4),
    inference(subst, [],
              [refute_0_92 :
               [bind(X0,
                     $fot(multiply(multiply(X_4,
                              multiply(identity,
                                multiply(multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(X_4, X_10),
                                        multiply(X_4, X_10)))),
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(X_4, X_10),
                                        multiply(X_4, X_10))))))),
                            multiply(X_4,
                              multiply(identity,
                                multiply(multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(X_4, X_10),
                                        multiply(X_4, X_10)))),
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(X_4, X_10),
                                        multiply(X_4, X_10)))))))))),
                bind(Y0,
                     $fot(multiply(multiply(X_4, X_4),
                            multiply(X_4, X_4)))), bind(Z0, $fot(X_4))]])).

cnf(refute_0_421, plain,
    (multiply(multiply(X_4, X_4), multiply(X_4, X_4)) != X_4 |
     multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))))))) =
     X_4),
    inference(resolve,
              [$cnf($equal(multiply(multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4, X_10),
                                    multiply(X_4, X_10)))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4, X_10),
                                    multiply(X_4, X_10))))))),
                        multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4, X_10),
                                    multiply(X_4, X_10)))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4, X_10),
                                    multiply(X_4, X_10)))))))),
                      multiply(multiply(X_4, X_4), multiply(X_4, X_4))))],
              [refute_0_419, refute_0_420])).

cnf(refute_0_422, plain,
    (multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))))))) =
     X_4),
    inference(resolve,
              [$cnf($equal(multiply(multiply(X_4, X_4),
                        multiply(X_4, X_4)), X_4))],
              [refute_0_303, refute_0_421])).

cnf(refute_0_423, plain,
    (multiply(identity,
       multiply(multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))),
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10),
                     multiply(X_4, X_10))))))))) =
     multiply(identity,
       multiply(multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))),
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10),
                     multiply(X_4, X_10)))))))))),
    introduced(tautology,
               [refl,
                [$fot(multiply(identity,
                        multiply(multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4, X_10),
                                      multiply(X_4, X_10)))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4, X_10),
                                      multiply(X_4, X_10))))))),
                          multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4, X_10),
                                      multiply(X_4, X_10)))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4, X_10),
                                      multiply(X_4, X_10))))))))))]])).

cnf(refute_0_424, plain,
    (multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10),
                   multiply(X_4, X_10)))))))) != X_4 |
     multiply(identity,
       multiply(multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))),
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10),
                     multiply(X_4, X_10))))))))) !=
     multiply(identity,
       multiply(multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))),
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10),
                     multiply(X_4, X_10))))))))) |
     multiply(identity,
       multiply(multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))),
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10),
                     multiply(X_4, X_10))))))))) =
     multiply(identity, X_4)),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(identity,
                          multiply(multiply(X_4,
                              multiply(identity,
                                multiply(multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(X_4, X_10),
                                        multiply(X_4, X_10)))),
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(X_4, X_10),
                                        multiply(X_4, X_10))))))),
                            multiply(X_4,
                              multiply(identity,
                                multiply(multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(X_4, X_10),
                                        multiply(X_4, X_10)))),
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(X_4, X_10),
                                        multiply(X_4, X_10))))))))),
                        multiply(identity,
                          multiply(multiply(X_4,
                              multiply(identity,
                                multiply(multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(X_4, X_10),
                                        multiply(X_4, X_10)))),
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(X_4, X_10),
                                        multiply(X_4, X_10))))))),
                            multiply(X_4,
                              multiply(identity,
                                multiply(multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(X_4, X_10),
                                        multiply(X_4, X_10)))),
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(X_4, X_10),
                                        multiply(X_4, X_10))))))))))),
                 [1, 1], $fot(X_4)]])).

cnf(refute_0_425, plain,
    (multiply(multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))),
       multiply(X_4,
         multiply(identity,
           multiply(multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
             multiply(X_10,
               multiply(identity,
                 multiply(multiply(X_4, X_10),
                   multiply(X_4, X_10)))))))) != X_4 |
     multiply(identity,
       multiply(multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))),
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10),
                     multiply(X_4, X_10))))))))) =
     multiply(identity, X_4)),
    inference(resolve,
              [$cnf($equal(multiply(identity,
                        multiply(multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4, X_10),
                                      multiply(X_4, X_10)))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4, X_10),
                                      multiply(X_4, X_10))))))),
                          multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4, X_10),
                                      multiply(X_4, X_10)))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4, X_10),
                                      multiply(X_4, X_10))))))))),
                      multiply(identity,
                        multiply(multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4, X_10),
                                      multiply(X_4, X_10)))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4, X_10),
                                      multiply(X_4, X_10))))))),
                          multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4, X_10),
                                      multiply(X_4, X_10)))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4, X_10),
                                      multiply(X_4, X_10)))))))))))],
              [refute_0_423, refute_0_424])).

cnf(refute_0_426, plain,
    (multiply(identity,
       multiply(multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))),
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10),
                     multiply(X_4, X_10))))))))) =
     multiply(identity, X_4)),
    inference(resolve,
              [$cnf($equal(multiply(multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4, X_10),
                                    multiply(X_4, X_10)))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4, X_10),
                                    multiply(X_4, X_10))))))),
                        multiply(X_4,
                          multiply(identity,
                            multiply(multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4, X_10),
                                    multiply(X_4, X_10)))),
                              multiply(X_10,
                                multiply(identity,
                                  multiply(multiply(X_4, X_10),
                                    multiply(X_4, X_10)))))))), X_4))],
              [refute_0_422, refute_0_425])).

cnf(refute_0_427, plain,
    (multiply(identity,
       multiply(multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))),
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10),
                     multiply(X_4, X_10))))))))) != X_4 |
     multiply(identity,
       multiply(multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))),
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10),
                     multiply(X_4, X_10))))))))) !=
     multiply(identity, X_4) | multiply(identity, X_4) = X_4),
    introduced(tautology,
               [equality,
                [$cnf($equal(multiply(identity,
                          multiply(multiply(X_4,
                              multiply(identity,
                                multiply(multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(X_4, X_10),
                                        multiply(X_4, X_10)))),
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(X_4, X_10),
                                        multiply(X_4, X_10))))))),
                            multiply(X_4,
                              multiply(identity,
                                multiply(multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(X_4, X_10),
                                        multiply(X_4, X_10)))),
                                  multiply(X_10,
                                    multiply(identity,
                                      multiply(multiply(X_4, X_10),
                                        multiply(X_4, X_10))))))))), X_4)),
                 [0], $fot(multiply(identity, X_4))]])).

cnf(refute_0_428, plain,
    (multiply(identity,
       multiply(multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10))))))),
         multiply(X_4,
           multiply(identity,
             multiply(multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10), multiply(X_4, X_10)))),
               multiply(X_10,
                 multiply(identity,
                   multiply(multiply(X_4, X_10),
                     multiply(X_4, X_10))))))))) != X_4 |
     multiply(identity, X_4) = X_4),
    inference(resolve,
              [$cnf($equal(multiply(identity,
                        multiply(multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4, X_10),
                                      multiply(X_4, X_10)))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4, X_10),
                                      multiply(X_4, X_10))))))),
                          multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4, X_10),
                                      multiply(X_4, X_10)))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4, X_10),
                                      multiply(X_4, X_10))))))))),
                      multiply(identity, X_4)))],
              [refute_0_426, refute_0_427])).

cnf(refute_0_429, plain, (multiply(identity, X_4) = X_4),
    inference(resolve,
              [$cnf($equal(multiply(identity,
                        multiply(multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4, X_10),
                                      multiply(X_4, X_10)))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4, X_10),
                                      multiply(X_4, X_10))))))),
                          multiply(X_4,
                            multiply(identity,
                              multiply(multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4, X_10),
                                      multiply(X_4, X_10)))),
                                multiply(X_10,
                                  multiply(identity,
                                    multiply(multiply(X_4, X_10),
                                      multiply(X_4, X_10))))))))), X_4))],
              [refute_0_302, refute_0_428])).

cnf(refute_0_430, plain, (multiply(identity, a) = a),
    inference(subst, [], [refute_0_429 : [bind(X_4, $fot(a))]])).

cnf(refute_0_431, plain,
    (multiply(identity, a) != a | a != a | multiply(identity, a) = a),
    introduced(tautology,
               [equality,
                [$cnf(~ $equal(multiply(identity, a), a)), [0],
                 $fot(a)]])).

cnf(refute_0_432, plain, (a != a | multiply(identity, a) = a),
    inference(resolve, [$cnf($equal(multiply(identity, a), a))],
              [refute_0_430, refute_0_431])).

cnf(refute_0_433, plain, (a != a),
    inference(resolve, [$cnf($equal(multiply(identity, a), a))],
              [refute_0_432, prove_order3])).

cnf(refute_0_434, plain, (a = a),
    introduced(tautology, [refl, [$fot(a)]])).

cnf(refute_0_435, plain, ($false),
    inference(resolve, [$cnf($equal(a, a))],
              [refute_0_434, refute_0_433])).
% SZS output end CNFRefutation for GRP116-1.tptp

